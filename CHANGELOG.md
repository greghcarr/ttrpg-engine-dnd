# Changelog

Notable changes to this project. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). The bump policy and pre-release roadmap are documented in [VERSIONING.md](VERSIONING.md).

## Unreleased

**Content audit: Fighter Indomitable + Druid Wild Shape uses (slice 176)**

SRD 5.2.1 value-drift sweep continued.

- Fighter Indomitable: pack had `indomitable-3` (third long-rest use) at L20. SRD 5.2.1 says "three times before a Long Rest starting at level 17"; pack was off by three levels. Moved `indomitable-3` from L20 to L17 (now sharing the L17 row with `action-surge-2`). L20 now has only `extra-attack-3`.
- Druid Wild Shape uses: pack progression 2/3/4/5/6 at L2/L5/L9/L13/L17 didn't match the SRD 5.2.1 Druid Features table (2 at L2-L5, 3 at L6-L16, 4 at L17-L20). Net edits: dropped L5 `wild-shape-uses-3` (L2's 2 still applies through L5), moved the L6 step from L9 to L6 (RAW), dropped L9 / L13 entries (no Wild Shape bump at those levels), changed L17 from max=6 to max=4 (RAW cap).

Snapshot: features wired-class-features refreshed.
Tests: 1452 pass, tsc --noEmit clean.

**Content audit: Monk Ki + Sorcerer Sorcery Points linear scaling (slice 175)**

SRD 5.2.1 follow-up to slice 174. Both Monk Focus Points (Ki) and Sorcerer Sorcery Points scale linearly with class level per the Monk Features and Sorcerer Features tables (Ki = Monk level at L2+; Sorcery Points = Sorcerer level at L2+). The pack modeled both with hardcoded multi-tier grants at L2/L5/L10/L15/L20 (Monk) and L2/L5/L10/L15/L17/L20 (Sorcerer), which produced correct totals only at the granted levels and stale-low totals in between (e.g., L3 Monk had 2 Ki instead of 3).

- Monk: kept the L2 `monks-focus` grant but changed `max: 2` to `max: { kind: "level", classId: "monk" }`. Dropped the L5 `ki-uses-5`, L10 `ki-uses-10`, L15 `ki-uses-15`, and L20 `ki-uses-20` redundant entries.
- Sorcerer: kept the L2 `font-of-magic` grant but changed `max: 2` to `max: { kind: "level", classId: "sorcerer" }`. Dropped L5 `sorcery-points-5`, L10 `sorcery-points-10`, L15 `sorcery-points-15`, L17 `sorcery-points-17`, and L20 `sorcery-points-20` redundant entries.

Made possible by the slice 174 GrantResource Formula fix.

Snapshot: features wired-class-features refreshed (nine entries dropped).

Tests: 1452 pass, tsc --noEmit clean.

**Engine + content audit: class resource pool value drift (slice 174)**

SRD 5.2.1 value-drift sweep for class resource pools. The audit caught four real bugs in pool sizes that the pack hardcoded with 2014 PHB values (or with values that simply diverged from RAW).

Engine fix in [src/effects/builder.ts](src/effects/builder.ts): the `GrantResource` builder case previously only handled numeric `max` values and silently dropped Formula values. Two existing features in the pack (Wizard Arcane Recovery, Paladin Lay on Hands) were affected; they'd been defined with Formula `max` shapes since slice ~50 but the resource grant never reached the accumulator. Builder now evaluates the Formula via `ctx.formulaContext` (same path as `AddModifier`'s Formula branch). No new schema; just lifts the existing silent-drop.

Content fixes in [src/content/packs/starter-pack.json](src/content/packs/starter-pack.json):

- Cleric Channel Divinity: 1/2/3 to 2/3/4 at L2/L6/L18. SRD 5.2.1 Cleric Features table shows the Channel Divinity column starting at 2 and stepping up at L6 and L18; pack was off-by-one throughout.
- Paladin Channel Divinity: added L11 grant (max = 3). SRD says "You gain an additional use when you reach Paladin level 11"; pack had only the L3 grant (max = 2) and never bumped.
- Fighter Second Wind: added L4 (max = 3) and L10 (max = 4) grants. SRD Fighter Features table shows the Second Wind column at 2 (L1-3), 3 (L4-9), and 4 (L10-20); pack only set L1's 2.
- Bardic Inspiration uses: hardcoded 3 (all four level rows) to formula `max(1, abilityMod CHA)`. SRD: "You can confer a Bardic Inspiration die a number of times equal to your Charisma modifier (minimum of once)." Applied to all four Bardic Inspiration grants (L1, L5, L10, L15). Made possible by the GrantResource Formula fix above.

Snapshot: features-test wired-class-features refreshed for the four new wired entries (Fighter L4 / L10 Second Wind bumps, Paladin L11 Channel Divinity bump). The Bardic Inspiration L1/L5/L10/L15 entries were already wired and stay in place.

Tests: 1452 pass, tsc --noEmit clean.

**Content audit: class feature 2014-flavor sweep (slice 173)**

SRD 5.2.1 follow-up to slice 172. Four more entries closed from the class-audit's pack-only-features table.

- Paladin L11: Radiant Strike renamed to Radiant Strikes (SRD spelling, plural). Pack id `radiant-strike` to `radiant-strikes`.
- Monk L18: Empty Body (2014 capstone) renamed to Superior Defense (SRD 5.2.1 L18). Pack id `empty-body` to `superior-defense`. Effects array remains empty (schema-only on both sides).
- Monk L7: Step of the Wind: Heightened Mobility dropped. 2014-flavored feature with no SRD 5.2.1 analog; SRD has Evasion as the L7 grant, which the pack already wires.
- Warlock L3: Pact Boon dropped. 2014 PHB feature; SRD 5.2.1 handles Pact Boon as an Eldritch Invocation option starting at L1 and reserves L3 for Warlock subclass selection (already modeled via the subclass machinery, not a feature entry).

Remaining pack-only-features entries (Cleric L2 Divine Spark as a separate feature; Sorcerer L20 Sorcery Points (20)) are schema / modeling differences, not drift; kept as-is. None of the renames or drops were referenced in tests, src, or docs outside the pack itself.

Tests: 1452 pass, tsc --noEmit clean. No snapshot refresh needed (all four edits touch only schema-only features absent from the wired-feature catalog).

**Content audit: class feature level placements (slice 172)**

SRD 5.2.1 audit follow-up to slice 153's class-feature drift list. Twelve placement fixes across six classes plus one rename, all from data-side edits to starter-pack.json class `levelGrants`. No engine code touched.

- Fighter: Tactical Mind L5 to L2, Tactical Shift L9 to L5, Tactical Master L11 to L9.
- Bard: Expertise L3 to L2 (now sits alongside Jack of All Trades), Superior Inspiration L20 to L18.
- Cleric: Channel Divinity L1 to L2 (now shares L2 with Divine Spark), Improved Blessed Strikes L17 to L14, renamed Improved Divine Intervention to Greater Divine Intervention at L20.
- Rogue: Reliable Talent L11 to L7 (now shares L7 with Evasion), Improved Cunning Strike L14 to L11, Devious Strikes L18 to L14.
- Ranger: Nature's Veil L9 to L14.
- Evoker (Wizard subclass): Sculpt Spells L3 to L6 (now lives in its own L6 `levelGrants` entry separate from Evocation Savant at L3).

Slice 153's audit also flagged Barbarian Improved Brutal Strike L13 to L17. That entry was incorrect: SRD 5.2.1 has Improved Brutal Strike at BOTH L13 (first additional Brutal Strike option) AND L17 (second additional option), as two separate features. The pack already places them at L13 and L17, so no edit was needed.

Tests: features-test wired-class-features snapshot refreshed (Bard L2 expertise, Cleric L2 channel-divinity replacing L1, plus the three Rogue moves) by `npx vitest run tests/coverage/features.test.ts -u`. All 1452 tests pass; tsc --noEmit clean.

**Content authoring: monsters batch 1.21**

2 Humanoid combat tier-up entries added (closes the explicitly-called-out humanoid combat gap from the gaps-doc tail): Gladiator (CR 5), Assassin (CR 8). Gladiator extends the humanoid combatant ladder (Bandit Captain / Knight / Veteran / Berserker / Thug) up through CR 5 with three-attack Multiattack, Parry reaction, Brave save advantage, and shield-bash-with-prone; Assassin closes the high-CR rogue-archetype slot with Sneak Attack, Evasion, Assassinate, poisoned shortsword / light crossbow, and poison resistance. Both ship `traits: []`. One new mechanic-level deferral: Assassinate (composite of first-turn-advantage-on-untaken-turn-target + auto-crit-on-surprised-target; first composite needs an initiative-tracking predicate, second needs a brand-new "attack-against-surprised-target promotes to critical" attack-resolution rewrite). Folds Gladiator into Multiattack, Parry reaction, Brave save advantage, and on-hit save-or-condition (Shield Bash with STR-vs-prone); folds Assassin into Multiattack, Sneak Attack / Cunning Action / Evasion class-feature-borrow gap (Evasion adds a save-result-rewrite analogous to Legendary Resistance's install point), and poison-damage-rider-on-attack. Monster count grows from 116 to 118. The gaps-doc "notable gaps still open" tail entry has been updated: with this batch all previously-flagged tutorial / starter-encounter humanoid gaps are now closed.

**Content authoring: monsters batch 1.20**

6 Beast megafauna tier-up entries added: Pteranodon (CR 1/4), Plesiosaurus (CR 2), Allosaurus (CR 2), Triceratops (CR 5), Mammoth (CR 6), Tyrannosaurus Rex (CR 8). The canonical SRD dinosaurs (Pteranodon, Plesiosaurus, Allosaurus, Triceratops, T-Rex) plus Mammoth as the prehistoric-megafauna companion. Closes the high-CR Beast gap — Beast cohort previously capped at CR 1 (Brown Bear, Dire Wolf); the pack now ranges through CR 8 in Beasts. Tyrannosaurus Rex is the first Beast with Multiattack. All six ship `traits: []`. Monster count: 110 to 116. Two new mechanic-level deferrals: Flyby (Pteranodon; no opportunity attacks when flying out of reach — per-statblock movement-vs-reaction exemption predicate) and Stomp prone-target-only attack (Triceratops, Mammoth; target-condition-precondition primitive — same family as Otyugh Tentacle Slam's grapple-precondition but predicated on target state rather than attacker state). Folds Allosaurus Pounce + Triceratops Trampling Charge + Mammoth Trampling Charge into the existing Charge entry (now called Charge / Pounce / Trampling Charge with shared bonus-action-on-prone-follow-up rider); folds T-Rex bite-grapple-with-restricted-target-of-bite into the existing Ankheg-shaped auto-grapple entry; folds Plesiosaurus Hold Breath into the narrative-traits entry; folds T-Rex into Multiattack.

**Content authoring: monsters batch 1.19**

5 Devil ladder tier-up entries added (closes the mid-tier Devil hierarchy started by Imp and Lemure in batch 1.7): Spined Devil (CR 2), Bearded Devil (CR 3), Barbed Devil (CR 5), Chain Devil (CR 8), Bone Devil (CR 9). All five share the canonical Devil damage profile: cold resistance, fire / poison immunity, slice-112 `GrantResistance` B/P/S nonmagical traits (with the "not silvered" qualifier deferred per existing Wight / Imp gap), poisoned condition immunity, darkvision 120, Infernal + telepathy 120 ft, Magic Resistance, Devil's Sight. Bone Devil is the first Large devil in the pack. Three new mechanic-level deferrals introduced: Steadfast (Bearded Devil; conditional condition-immunity gated on an allied-statblock proximity-and-conscious-state predicate — useful primitive that later supports pack tactics / fanatic auras), Wound-no-heal rider on glaive (Bearded Devil; prevent-HP-regain stamp until magical healing — same family as Aboleth's diseased "can only be healed by magic" and Wraith Life Drain max-HP-penalty), and Damage-on-grappling-self recurring (Barbed Devil Barbed Hide; start-of-turn rider on bearer that damages grapplers — same shape as Fire Elemental Fire Form with different trigger timing). Folds Spined Devil into Tail Spike ammunition (joining Manticore); folds Spined Devil fork / tail rider and Bone Devil sting into the poison-damage-rider-on-attack entry; folds Chain Devil Animate Chains into the renamed "Animate Trees / Animate Chains" spawn-named-statblock entry; folds Chain Devil Unnerving Mask into Frightful Presence; folds all five into Multiattack, Magic Resistance, and Devil's Sight existing entries. Monster count grows from 105 to 110.

**Content authoring: monsters batch 1.18**

5 Adult Chromatic Dragon tier-up entries added: Adult White Dragon (CR 13), Adult Black Dragon (CR 14), Adult Green Dragon (CR 15), Adult Blue Dragon (CR 16), Adult Red Dragon (CR 17). Closes the entire chromatic Adult Dragon ladder in a single batch. Each is Huge size, blindsight 60 + darkvision 120, expertise on Perception, canonical elemental immunity, and Common + Draconic. Adult Green additionally gains poisoned condition immunity. Adult Red's proficiency bonus +6 is the second-highest in the pack (after Solar's +7). The five Adult Chromatics share an exact statblock template scaling only by CR, ability scores, breath shape / DC / dice, bite-rider die, and Frightful Presence DC. All five ship `traits: []`. First batch where every entry uses Legendary Actions, Legendary Resistance (3/day each), and Frightful Presence. One new mechanic-level deferral: Frightful Presence (aura WIS save vs frightened with per-creature 24-hour immunity stamp on save success and per-turn-end recurring save to end). Folds the five Adult Dragons into the existing Multiattack and Legendary Actions / Resistance entries; folds their bite riders (+1d8 cold / acid / +2d6 poison / +1d10 lightning / +4d6 fire) into the unconditional-elemental-rider-on-bite entry; folds the five breath weapons into the Recharge area-save Breath Weapons entry (now eleven entries on one parameterized template); folds Adult Black + Adult Green Amphibious and Adult White Ice Walk into the narrative-movement deferral. Monster count grows from 100 to 105.

**Content authoring: monsters batch 1.17**

4 Young Chromatic Dragon tier-up entries added (first tier-up-within-existing-type batch — every prior batch opened a new creature type or expanded the humanoid roster): Young White Dragon (CR 6), Young Black Dragon (CR 7), Young Green Dragon (CR 8), Young Blue Dragon (CR 9). Closes the Young Chromatic Dragon set started by the seed's Young Red Dragon (CR 10), mirroring its statblock shape: Large Dragon, blindsight 30 + darkvision 120, expertise on Perception, multi-mode speed appropriate to each color's environment (White: walk / burrow / fly / swim; Black + Green: walk / fly / swim; Blue: walk / burrow / fly), Common + Draconic. Each ships its canonical elemental immunity (cold / acid / poison / lightning), and Young Green also gains the poisoned condition immunity (only Young Chromatic with one). All four ship `traits: []`. The pack reaches one hundred statblocks with this batch — a round-number milestone. Folds Young Dragon bite elemental riders (+1d8 cold / +1d8 acid / +1d6 poison / +1d10 lightning) into the renamed "Unconditional elemental rider on bite" deferral entry (previously "Angelic Weapons"); folds the four Breath Weapon recharges into the renamed "Recharge area-save Breath Weapons" entry, which now also documents that dragon breath weapons share a parameterized template — once one wires, all six entries on the list land for free as data. Folds Young Black + Young Green Amphibious and Young White's Ice Walk into the narrative-movement deferral.

**Content authoring: monsters batch 1.16**

4 Plant cohort entries added (closes the Plant creature type with the canonical SRD four, and completes coverage of every MM creature type that has SRD-licensed entries — all 14 are now in the pack): Awakened Shrub (CR 0), Awakened Tree (CR 2), Shambling Mound (CR 5), Treant (CR 9). Three of four (Awakened Shrub, Awakened Tree, Treant) ship `damageVulnerabilities: ["fire"]` — the largest single-batch population of the vulnerability field and the third-through-fifth post-seed entries (joining Skeleton's bludgeoning, Earth Elemental's thunder, Rug of Smothering's slashing). Three of four also carry plain (unqualified) `damageResistances` for bludgeoning / piercing rather than the slice-112 `GrantResistance.qualifier` form, since RAW Plant resistance is unconditional, not source-qualified. Shambling Mound exercises lightning immunity (with Lightning Absorption healing deferred separately) plus cold / fire resistance and blindsight 60. All four ship `traits: []`. Two new mechanic-level deferrals: Animate Trees (Treant 1/day; per-day spawn-named-statblock variant of the existing summon shape, plus a "spawn-with-feature-omission" wrinkle and duration cap) and Lightning Absorption (folds into the existing Fire Absorption damage-as-heal-by-type entry). Folds Shambling Mound's Engulf-after-double-grapple-hit into the existing Engulf gap; folds Awakened Shrub / Awakened Tree / Treant False Appearance + Treant Siege Monster into the narrative-traits entry; folds Awakened Shrub / Awakened Tree creator-language inheritance into the comprehended-but-not-spoken languages entry. Monster count grows from 92 to 96.

**Content authoring: monsters batch 1.15**

6 Celestial cohort entries added (opens a wholly-unstarted creature type across the widest CR band yet, 2-21): Pegasus (CR 2), Couatl (CR 4), Unicorn (CR 5), Deva (CR 10), Planetar (CR 16), Solar (CR 21). Solar is the new top of the pack — highest CR (21), highest XP (33000), highest proficiency bonus (+7), and the first appearance of CR 21. The four angels (Couatl, Deva, Planetar, Solar) carry slice-112 `GrantResistance` B/P/S nonmagical traits, radiant resistance, and the canonical Celestial condition immunities (charmed / exhaustion / frightened, plus poison on Solar). Couatl is the first statblock to exercise the truesight sense key. Folds extensive innate-spellcasting lists for all four angels plus Couatl and Unicorn into the existing per-spell envelope deferral (now covering 11 monsters across Giants, Aberrations, Fey, and Celestials). New mechanic-level deferrals: Angelic Weapons (unconditional radiant rider on weapon attacks, +4d8 / +5d8 / +6d8 by tier), Healing Touch (per-day-budget heal-on-touch action with multi-condition cleanse), Change Shape (Deva polymorph; folds into existing Shapechanger gap with broader form-target rules), Shielded Mind (Couatl; spell-school immunity predicate same family as Immutable Form), Slaying Longbow (Solar; save-vs-extra-damage rider plus soul-departure narrative flag), Flying Sword animated-weapon bonus action (Solar; weapon-instance-as-creature with duration-bound autonomous attacker), Couatl's bite (sleep-poison shares Sprite's install path with 24-hour duration), Couatl's Constrict (auto-grapple + restrained), Unicorn's Charge with horn knockdown (folds into existing Charge gap), Unicorn's 1/day Teleport (long-range location-targeted teleport with "must have seen" predicate), and Divine Awareness (narrative). Folds Unicorn / Deva / Planetar / Solar into Magic Resistance; folds Stone Golem / Iron Golem / Couatl / Unicorn into the Magic Weapons primitive gap. Monster count grows from 86 to 92.

**Content authoring: monsters batch 1.14**

5 Fey cohort entries added (opens a wholly-unstarted creature type with the canonical low-CR roster): Blink Dog (CR 1/4), Dryad (CR 1), Pixie (CR 1/4), Satyr (CR 1/2), Sprite (CR 1/4). Three of five (Dryad, Pixie, Satyr) carry Magic Resistance (folded into existing entry). Pixie ships HP 1 / formula `1d4-1` — the lowest-HP statblock in the pack. Two casters (Dryad with Druid casting flavor; Pixie with a ten-spell 1/day list including Polymorph and Confusion) substantially expand the per-spell envelope cohort; Pixie's list is now the densest single-monster spell list in the pack and the canonical proof that the envelope shape needs to scale. All five ship `traits: []`. New mechanic-level deferrals: Bonus-action teleport non-spell (Blink Dog Blink; needs a generic teleport primitive shared with misty-step / dimension door), Fey Charm (Dryad; same family as Enslave / Quasit Scare with three new wrinkles — flat duration, one-active-target slot, charm ends when caster damages target), Permanent magical Invisibility concentration-based (Pixie Superior Invisibility; distinct install path from Imp at-will and Invisible Stalker always-on), Speak with Beasts and Plants / Tree Stride / Mirthful Leaps / Reveler / Heart Sight (narrative), and Sleep-poison bow (Sprite; on-hit save-vs-poisoned + sleep-substitution-at-zero-HP rider). Folds Sprite into the existing at-will Invisibility entry, Blink Dog into the existing Keen senses entry, and Satyr into the existing Multiattack list. Monster count grows from 81 to 86.

**Content authoring: monsters batch 1.13**

5 Aberration opener entries added (opens a wholly-unstarted creature type with five iconic high-impact entries across CR 2-13): Aboleth (CR 10), Beholder (CR 13), Gibbering Mouther (CR 2), Mind Flayer (CR 7), Otyugh (CR 5). Three of five (Aboleth, Beholder, Mind Flayer) carry telepathy as a free-text `languages` array entry pending a proper language-vs-sense schema split (new structural deferral). Beholder and Gibbering Mouther are immune to the prone condition (the floating / amorphous biology). All five ship `traits: []`. Heaviest deferral-discovery batch yet: opens Legendary Actions (Aboleth, Beholder; new turn-cycle phase, per-statblock action menu, per-turn budget tracker), Legendary Resistance (Beholder 3/day; save-result rewrite primitive with per-day budget), Antimagic Cone (Beholder; area antimagic + directional cone-following-facing), Eye Rays (Beholder; random-selection-from-table action with 10 distinct sub-effects), Mind Blast (Mind Flayer; recharge + cone save + stun + recurring-save-to-end), Enslave (Aboleth; per-day-cadence recurring save + range-based condition expiry), Disease on hit (Aboleth, Otyugh; needs a `diseased` condition with parameterized effects), Mucous Cloud (Aboleth; damage-on-melee-hit + save-or-disease + environmental gate), Tentacle stun on grapple (Mind Flayer), Extract Brain (Mind Flayer; state-conditional instant-kill), Aberrant Ground (Gibbering Mouther; terrain modifier), Gibbering (per-turn-start save vs random-action chaos picker), Blinding Spittle Recharge (folds into existing recharge-area gap with condition-rider), Tentacle Slam (Otyugh; multi-target save with grapple-precondition), Telepathy as a sense (new structural gap), and Probing Telepathy (narrative). Folds Mind Flayer into the existing Magic Resistance entry; folds Mind Flayer Psionics into the existing per-spell envelope spellcasting entry. Monster count grows from 76 to 81.

**Content authoring: monsters batch 1.12**

6 Construct ladder entries added (opens a wholly-unstarted creature type across the CR 1/4-16 band, first time the pack carries a CR 16 statblock): Animated Armor (CR 1), Flying Sword (CR 1/4), Rug of Smothering (CR 2), Shield Guardian (CR 7), Stone Golem (CR 10), Iron Golem (CR 16). The three low-tier animated objects share the canonical condition-immunity suite (blinded, charmed, deafened, exhaustion, frightened, paralyzed, petrified, poisoned), poison / psychic damage immunity, and blindsight 60 (blind beyond). Both Golems carry slice-112 `GrantResistance` B/P/S nonmagical traits (RAW "not adamantine" qualifier deferred, same gap as Wight's silvered qualifier in batch 1.6); Iron Golem also gains fire immunity (with Fire Absorption healing deferred separately). Rug of Smothering ships slashing vulnerability — the second post-seed `damageVulnerabilities` entry, joining Earth Elemental's thunder. Twelve new mechanic-level deferrals: Antimagic Susceptibility (dispel-magic-specific trigger), Damage Transfer while grappling (Rug; half-damage-redirect), Smother / suffocation rider (Rug; composite auto-grapple + multi-condition + recurring damage + suffocation), Bound amulet (Shield Guardian; cross-entity binding), Regeneration on partial HP (Shield Guardian; unconditional turn-start HP regain), Spell Storing (Shield Guardian; spell-as-data carrier), Damage redirect to self reaction (Shield Guardian's Shield), Immutable Form (Stone / Iron Golem; immune to alter-form effects), Magic Weapons (Stone / Iron Golem; attack-side `event.isMagical`), Slow Recharge (Stone Golem; recharge + multi-target save + spell-as-monster-action), Fire Absorption (Iron Golem; damage-as-heal primitive), Adamantine-qualified resistance (same gap as Wight's silvered), and Comprehended-but-not-spoken languages (Shield Guardian, Stone / Iron Golem). Folds Animated Armor / Shield Guardian / Stone Golem / Iron Golem into the existing Multiattack list; folds Stone / Iron Golem into the existing Magic Resistance entry; folds Iron Golem's Poison Breath into the recharge-area entry (now covering line and cone shapes). Monster count grows from 70 to 76.

**Content authoring: monsters batch 1.11**

4 Ooze cohort entries added (closes a wholly-unstarted creature type with the canonical four): Black Pudding (CR 4), Gelatinous Cube (CR 2), Gray Ooze (CR 1/2), Ochre Jelly (CR 2). Every entry shares the ooze condition-immunity profile (blinded, charmed, deafened, exhaustion, frightened, prone) and blindsight 60 ft, with divergent damage profiles: Black Pudding (immune acid / cold / lightning / slashing), Gray Ooze (resist acid / cold / fire), Ochre Jelly (resist acid; immune lightning / slashing), Gelatinous Cube (no resistances). First time the pack carries `damageImmunities: ["slashing"]` and `damageImmunities: ["lightning"]`. All four ship `traits: []`. Five new mechanic-level deferrals: Split on damage-type (Black Pudding, Ochre Jelly — spawn-copy-of-self triggered by lightning / slashing damage received, even at 0 effective damage), Engulf (Gelatinous Cube — movement + multi-target save + recurring damage + escape-by-check composite), Transparent (Gelatinous Cube — perception-DC-against-creature gap), Corrosive Form on melee hit (Black Pudding — attack-against-self trigger with damage-back rider), and Item degradation on hit (Black Pudding, Gray Ooze — weapon / armor durability primitives don't exist). Folds Black Pudding / Gelatinous Cube / Gray Ooze / Ochre Jelly Amorphous and Spider Climb traits, plus Gray Ooze's False Appearance, into the existing narrative-movement deferral entry. Monster count grows from 66 to 70.

**Content authoring: monsters batch 1.10**

6 Monstrosity opener entries added (opens a wholly-unstarted creature type with the canonical adventure-staple roster): Ankheg (CR 2), Bulette (CR 5), Griffon (CR 2), Manticore (CR 3), Minotaur (CR 3), Owlbear (CR 3). All six sit in the CR 2-5 sweet spot. Three of six (Ankheg, Bulette, Griffon) carry the burrow / fly / tremorsense speed and sense kit; only Manticore (Common) and Minotaur (Abyssal) speak. All ship `traits: []` pending Multiattack and on-hit save / damage rider primitives. Five new mechanic-level deferrals: Deadly Leap (Bulette; multi-target save bonus-action with position-resolution step), Acid Spray Recharge (Ankheg; folds into the existing recharge-N gap with an area-line save shape), Tail Spike ammunition (Manticore; depletable per-attack ammo on a statblock), Variable AC by posture (Ankheg prone-AC; schema carries a single `ac` value), and an on-hit self-targeting attack-restriction rider (Ankheg's "while a creature is grappled, the ankheg has advantage and can only bite that creature"). Folds Minotaur into the existing Charge (Boar) and Reckless (Berserker) deferrals; folds Griffon's Keen Sight, Owlbear's Keen Sight and Smell into the existing keen-senses-family deferral; folds Ankheg's acid bite rider into the existing damage-rider-on-attack deferral; folds Ankheg's grapple-on-bite into the existing auto-grapple deferral. Monster count grows from 60 to 66.

**Content authoring: monsters batch 1.9**

5 Elemental cohort entries added (opens a wholly-unstarted creature type): Air Elemental (CR 5), Earth Elemental (CR 5), Fire Elemental (CR 5), Water Elemental (CR 5), Invisible Stalker (CR 6). All four base elementals plus the canonical CR 6 air-elemental variant. Every entry carries the slice-112 `GrantResistance` B/P/S nonmagical traits and a rich damage profile: Air resists lightning + thunder; Earth takes thunder vulnerability (first post-seed `damageVulnerabilities` entry); Fire is immune to fire; Water resists acid. Every entry is also immune to poison plus a near-complete suite of physical-state conditions (exhaustion, grappled, paralyzed, petrified, poisoned, prone, restrained, unconscious). First time `tremorsense` is exercised (Earth Elemental 60 ft) and first time the Auran / Aquan / Ignan / Terran elemental languages appear. Six new mechanic-level deferrals introduced: Elemental Form movement modes (Air / Water / Fire / Earth Glide; mostly narrative, surfaces movement-mode-vs-grapple interaction), Damage-on-contact + on-grapple recurring damage (Fire Elemental's Fire Form), Environmental damage susceptibility (Fire Elemental's Water Susceptibility; consumer territory), Freeze on cold damage (Water Elemental, needs `event.damageType` dispatcher fact), Permanent magical Invisibility (Invisible Stalker; always-on-from-spawn install path differs from at-will Invisibility on Imp / Quasit), and Faultless Tracker (Invisible Stalker; narrative). Monster count grows from 55 to 60.

**Content authoring: monsters batch 1.8**

6 Giant cohort entries added (opens a wholly-unstarted creature type with a single coherent set): Hill Giant (CR 5), Stone Giant (CR 7), Frost Giant (CR 8), Fire Giant (CR 9), Cloud Giant (CR 9), Storm Giant (CR 13). First time the pack carries a CR 13 statblock. Exercises elemental-affinity damage modifiers across the cohort: Frost (cold immunity), Fire (fire immunity), Storm (lightning + thunder immunity, cold resistance). All six ship `traits: []`; the damage-immunity / resistance data is RAW-accurate and will wire for free when a future engine slice consumes `monster.damageImmunities` / `damageResistances`. Adds five new mechanic-level deferrals: Rock thrown attack (all six; subset of the monster-action vocabulary gap), Rock Catching reaction (Stone Giant; same gap shape as Parry), Lightning Strike (Storm Giant; recharge primitive + monster-action vocabulary), and Innate Spellcasting with per-spell at-will / per-day usage envelopes (Cloud Giant: Detect Magic / Fog Cloud / Feather Fall / Fly / Misty Step / Telekinesis / Control Weather / Gaseous Form; Storm Giant: Detect Magic / Feather Fall / Levitate / Light / Control Weather / Water Breathing), which complements the existing prepared-list spellcasting deferral. Stone Giant's Stone Camouflage folds into the existing environment-conditional advantage gap. Cloud Giant's Keen Smell folds into the existing keen-senses-family gap. Monster count grows from 49 to 55.

**Content authoring: items batch 1.20**

8 more charges-bearing wands added to the starter pack, extending the batch-1.11 wand cohort with primarily save-or-suffer spell-grant variants: Wand of Fear (rare, 7 + 1d6+1 / dawn), Wand of Lightning Bolts (rare, Spellcaster, 7 + 1d6+1 / dawn), Wand of Paralysis (rare, Spellcaster, 7 + 1d6+1 / dawn), Wand of Polymorph (very rare, Spellcaster, 7 + 1d6+1 / dawn), Wand of Binding (rare, Spellcaster, 7 + 1d6+1 / dawn), Wand of Enemy Detection (rare, 3 + 1d3 / dawn), Wand of Secrets (uncommon, no attunement, 3 + 1d3 / dawn), Wand of Pyrotechnics (uncommon, no attunement, 7 + 1d6+1 / dawn). All eight ship with canonical RAW charges shapes; snapshot refresh bundled in this commit; diff is exactly eight new IDs added to `withChargesIds`, alphabetized, bringing the charges-exercising item count from 23 to 31. Most route through the existing `ItemSpellGrant` primitive batch 1.11 named for Staff of Charming; the batch introduces two new sub-primitives: `GrantSense { sense: 'enemy-detection' }` with an intent-based hostile-creature predicate distinct from alignment-detect (Wand of Enemy Detection), and a discoverable-object schema primitive for hidden doors / traps (Wand of Secrets, currently pure narrative until the engine models hidden-object state). Wand of Pyrotechnics adds a target-selection-at-activation arm with two distinct sub-effect sets (pyrotechnics vs smoke cloud), distinct from Oil of Slipperiness's bearer-vs-surface branch from batch 1.12. Swapped Wand of the War Mage +1 from the proposed batch (it's actually pure stub per RAW — passive +1 spell attack, no charges) for Wand of Pyrotechnics to keep all eight items charges-bearing. Magic-item count bumped from 130 to 138.

**Content authoring: items batch 1.19**

8 charges-bearing rings added to the starter pack, the queued followup to batch 1.15's pure-stub rings cohort: Ring of Spell Storing (rare, 5 / never), Ring of Three Wishes (legendary, 3 / never), Ring of X-Ray Vision (rare, 1 / long rest), Ring of the Ram (rare, 3 + 1d3 / dawn), Ring of Evasion (rare, 3 + 1d3 / dawn), Ring of Animal Influence (rare, 3 + 1d3 / dawn), Ring of Shooting Stars (very rare, 6 / dawn), Ring of Spell Turning (legendary, 3 + 1d3 / dawn). All eight ship with the canonical RAW charges shape; snapshot refresh bundled in this commit; the diff is exactly eight new IDs added to `withChargesIds`, alphabetized, bringing the charges-exercising item count from 15 to 23. First batch to exercise the `longRest` recharge variant (Ring of X-Ray Vision) and the second batch to use the `never` variant (Gem of Brightness was first). Per-ring action wiring routes through the same `UseItem` planner already deferred for batches 1.7 and 1.11; gaps doc gained a parent bullet with eight per-ring shapes including five new sub-primitives: spell-level-pool with `CastSpellIntoItem` planner (Spell Storing, distinct from `RegainSpellSlot`), variable-charges-per-cast (Ram's 1-3 charge tiers, Animal Influence's per-spell costs, Spell Turning's redirect cost) which generalizes Wand of Fireballs' shape, `RewriteSaveResult { from: 'fail', to: 'success' }` for Evasion (distinct from the modifier-side save bonus), time-of-day predicate (`when: 'dusk-or-darker'` for Shooting Stars), and spell-reflection (Spell Turning, no existing engine analog). Magic-item count bumped from 122 to 130.

**Content authoring: items batch 1.18**

8 cloak-and-small-slot-accessory items added to the starter pack: Cloak of the Manta Ray (uncommon), Amulet of Proof Against Detection and Location (uncommon, attunement), Periapt of Health (uncommon), Talisman of the Sphere (legendary, attunement), Eyes of Minute Seeing (uncommon), Helm of Comprehending Languages (uncommon), Gloves of Thievery (uncommon), Boots of the Winterlands (uncommon, attunement). Widened framing because the canonical 5e/2024 cloak slot is thin once batch 1.17 absorbed Displacement and Billowing — only Cloak of the Manta Ray remained as pure stub (Cloak of Arachnida and Cape of the Mountebank are both charges-bearing, deferred to a future batch); the batch pulls the natural slot-adjacent cohort (neck-slot amulets / periapts / talismans, face / head / hands / feet small accessories) so the cloak shipment doesn't ship as a singleton. All eight ship as `itemKind: 'magic'` with `effects: []` and no charges; snapshot-clean. The Deferred-wiring subsection gained a parent bullet with eight per-item shapes including six new sub-predicates (spell-school-scoped immunity for Amulet of Proof Against Detection, a disease-condition vocabulary entry for Periapt of Health, proficiency-doubling-on-predicate for Talisman of the Sphere, range-gated conditional advantage for Eyes of Minute Seeing, spoken-word predicate for Helm of Comprehending Languages, per-check-kind modifier predicate for Gloves of Thievery's Sleight-of-Hand-vs-lockpicking split, per-source difficult-terrain-ignore for Boots of the Winterlands). Magic-item count bumped from 114 to 122.

**Content authoring: items batch 1.17**

8 robe-and-outer-garment items added to the starter pack: Robe of the Archmagi (legendary, Sorcerer / Warlock / Wizard attunement), Robe of Eyes (rare, attunement), Cloak of Displacement (rare, attunement), Cloak of Billowing (common, no attunement, cosmetic), Bracers of Defense (rare, attunement), Boots of Levitation (rare, attunement), Boots of Speed (rare, attunement), Smoldering Armor (common, no attunement, cosmetic). The canonical 5e/2024 DMG robe catalog is short (four robes total in core, two of which are charges-bearing and deferred to a future charges-bearing batch — Robe of Stars and Robe of Scintillating Colors); this batch extends to thematically-adjacent outer-garment slot items to fill out the cohort honestly. All eight ship as `itemKind: 'magic'` with `effects: []` and no charges; snapshot-clean. Third batch to exercise the schema's `attunementCondition` field after Pearl of Power, Holy Avenger / Dwarven Thrower, and the rod / staff cohort; this one ships Robe of the Archmagi's "Sorcerer, Warlock, or Wizard" multi-class lock. The Deferred-wiring subsection gained a parent bullet covering: Robe of the Archmagi's composite caster-bonus passive (composing `AddModifier` on AC / spell DC / spell attack with `SetAdvantage` on saves-vs-spells), Robe of Eyes' composite passive-sense bundle (composing `GrantSense` at multiple sense entries with surprise-immunity from Ioun Stone Awareness and perception advantage from Sentinel Shield, plus a new action-tagged-as-looking trigger for the sunlight-blinded sub-arm), Cloak of Displacement's bearer-protective disadvantage on incoming weapon attacks (plus a new "effect goes dormant for N rounds on damage trigger" primitive that generalizes to several other items), Bracers of Defense's conditional +2 AC gated on equipment state, Boots of Levitation's at-will Levitate via `ItemSpellGrant`, and Boots of Speed composing the multiply-speed primitive with the OA-disadvantage shape Cloak of Displacement wants plus the per-day time-budget Winged Boots wanted plus the `ToggleItemEffect` planner already deferred multiple times. Magic-item count bumped from 106 to 114.

**Content authoring: items batch 1.16**

10 entries added to the starter pack completing the canonical Giant Strength tier extension that batches 1.2 and 1.12 began with Hill Giant Strength: five belts (Stone very rare / Frost very rare / Fire very rare / Cloud legendary / Storm legendary, all attunement-required) and five potions (Stone rare / Frost rare / Fire rare / Cloud very rare / Storm legendary). Belt rarities follow RAW (Hill rare, Stone / Frost / Fire very rare, Cloud / Storm legendary); potion rarities are one tier lower across the board per RAW reflecting the 1-hour duration limit. All ten ship as pure stub (`effects: []` on the magic belts, `onConsume: []` on the consumable potions); snapshot-clean. Mechanically the entire family wires through a single primitive at six different value targets (21 / 23 / 23 / 25 / 27 / 29) — the `OverrideAbilityScore` shape batch 1.2's Belt of Hill Giant Strength named. Frost and Stone tiers ship at the same numeric target (STR 23) and are distinct items per RAW. No new primitives this batch; the cross-cutting projection blocker is unchanged from batch 1.2 for the belts, and the consume-side `ConsumeItem` blocker is unchanged from batch 1.3 for the potions. Magic-item count bumped from 101 to 106; consumable count from 25 to 30. First batch larger than 8 entries; the family-completion narrative justifies the larger size (the five belt + five potion symmetry is canonical RAW).

**Content authoring: items batch 1.15**

8 rings added to the starter pack, extending the existing Ring of Protection / Swimming / Jumping set with a uncommon-through-legendary cohort: Ring of Feather Falling (uncommon), Ring of Mind Shielding (uncommon), Ring of Warmth (uncommon), Ring of Water Walking (uncommon, no attunement), Ring of Free Action (rare), Ring of Resistance (rare), Ring of Regeneration (very rare), Ring of Invisibility (legendary). All eight ship as `itemKind: 'magic'` with `effects: []` and no charges; snapshot-clean. The charges-bearing rings (Evasion, Shooting Stars, Spell Storing, Spell Turning, Telekinesis, the Ram, Three Wishes, X-Ray Vision, Animal Influence) defer to a future charges-bearing batch under the existing `UseItem` planner blocker from batch 1.7. The Deferred-wiring subsection gained a parent bullet for items-batch-1.15 with eight per-ring sub-bullets covering: a new fall-event hook primitive plus auto-cast Feather Fall (Feather Falling), composite detection-immunity with a spell-list-scoped immunity predicate (Mind Shielding), composite cold-resistance plus narrative temperature-immunity (Warmth), `ModifySpeed { mode: 'water-walk' }` extension to the existing speed-mode primitive (Water Walking), composite difficult-terrain-suppression plus condition-immunity plus magical-speed-reduction-immunity sub-predicates (Free Action), variant-typed resistance sharing the item-instance variant pattern with Dragon Scale Mail (Resistance), continuous-HP-regen-with-10-minute-tick distinct from the hourly Ioun Stone Regeneration variant (Regeneration), and toggleable Invisible composing the `ToggleItemEffect` Driftglobe deferred from batch 1.7 with the existing Invisibility spell's "ends on attack or spell" trigger (Invisibility). Magic-item count bumped from 93 to 101.

**Content authoring: items batch 1.14**

8 Ioun Stones added to the starter pack, introducing the Ioun Stone family with one passive stone per ability score plus the two simpler-shape stones: Ioun Stone Strength (very rare), Ioun Stone Agility (very rare), Ioun Stone Fortitude (very rare), Ioun Stone Intellect (very rare), Ioun Stone Insight (very rare), Ioun Stone Leadership (very rare), Ioun Stone Awareness (rare), Ioun Stone Protection (rare). All eight ship as `itemKind: 'magic'` with `effects: []` and no charges, all attunement-required; snapshot-clean. The orbiting mechanic (stone hovers around the wearer's head, no body slot consumed) is model-implicit (attuned items without an equipped slot already fit the magic-item shape). The Deferred-wiring subsection gained a parent bullet for items-batch-1.14 covering: the canonical primitive split between `OverrideAbilityScore` (set-to-N, used by the belt / headband / gauntlets) and the new `IncreaseAbilityScore { amount, cap }` shape the Ioun Stones want (additive-with-cap; generalizes to the Tome of Clear Thought / Quickness / Leadership family in a future batch), Awareness's surprise-immunity arm wiring through a `SuppressInitiativeDisadvantage { source: 'surprise' }` predicate distinct from Cloak of Elvenkind's wearer-perspective conditional-disadvantage shape, Protection wiring for free off the existing `AddModifier { target: 'ac' }` Ring of Protection uses, and a deferred-Ioun-Stone bullet covering the charges-style variants (Absorption, Greater Absorption, Reserve) under a future spell-level-pool charges shape plus the legendaries (Mastery for proficiency-bonus override, Regeneration for hourly out-of-combat HP regen, Greater Absorption). Magic-item count bumped from 85 to 93.

**Content authoring: items batch 1.13**

8 spell scrolls added to the starter pack, filling the previously-empty scroll category with reference entries spanning cantrip through 9th level across all five non-artifact rarities: Spell Scroll of Fire Bolt (common, cantrip), Spell Scroll of Magic Missile (common, 1st), Spell Scroll of Misty Step (uncommon, 2nd), Spell Scroll of Fireball (uncommon, 3rd), Spell Scroll of Greater Invisibility (rare, 4th), Spell Scroll of Cone of Cold (rare, 5th), Spell Scroll of Disintegrate (very rare, 6th), Spell Scroll of Wish (legendary, 9th). All eight ship as `itemKind: 'consumable'` with `onConsume: []` and narrative descriptions that name the underlying spell, the scroll-fixed save DC / spell attack bonus, and the non-class-list read-check DC; snapshot-clean. The scroll category is currently a sub-pattern of `consumable` rather than a first-class `itemKind: 'scroll'`; the Deferred-wiring subsection gained a parent bullet for items-batch-1.13 covering both that schema-extension question and five wiring arms: cross-cutting `ConsumeItem`+`ItemSpellGrant` composition (the staff-of-charming variant batch 1.11 named, where DC and attack bonus come from the item not the wielder), class-list gating with a "consumer can natively cast `spellId`" predicate the engine doesn't currently expose, per-level scaling (DC 13/+5 → DC 19/+11), concentration handling (scrolls preserve the spell's concentration unlike Potion of Speed's suppression), and risk-clause inheritance (Wish's 33% stress arm and similar live in the spell wiring, not the scroll). Consumable count bumped from 17 to 25.

**Content authoring: items batch 1.12**

8 potions and oils added to the starter pack, extending the consumables family with the second-most-common DMG consumable cohort: Potion of Water Breathing (uncommon), Potion of Fire Breath (uncommon), Potion of Hill Giant Strength (uncommon), Oil of Slipperiness (uncommon), Potion of Invisibility (very rare), Potion of Flying (very rare), Potion of Speed (very rare), Oil of Sharpness (very rare). All eight ship as `itemKind: 'consumable'` with `onConsume: []` and a narrative `description`, matching the existing batch-1.3 potion shape; snapshot-clean (the features snapshot enumerates magic items only, not consumables). The Deferred-wiring subsection gained a parent bullet for items-batch-1.12 with per-potion sub-bullets, six of which compose with primitives already named in earlier batches (`ApplyCondition` + breathlessness marker from batch 1.2's Necklace of Adaptation, the Invisible condition vocabulary already wired, `ModifySpeed { mode: 'fly' }` shipped at slice 77, the existing Haste spell wiring with concentration suppressed for the potion variant, the `OverrideAbilityScore` primitive batch 1.2 named for Belt of Hill Giant Strength, and slice-76's `ItemInstance.temporaryBuff` weapon-buff shape for Oil of Sharpness), and three of which introduce new shapes: condition-with-internal-charges plus single-target-breath-attack for Potion of Fire Breath, target-selection branch (creature vs surface) plus difficult-terrain-zone primitive for Oil of Slipperiness, and post-effect chained-follow-up-condition for Potion of Speed's lethargic afterclap. The remaining giant-strength tier (Stone, Frost, Fire, Cloud, Storm) ships in a future batch under the same `OverrideAbilityScore` primitive at different value targets. Consumable count bumped from 9 to 17.

**Content authoring: items batch 1.11**

8 rod / staff / wand entries added to the starter pack, the second charges-bearing wave after batch 1.7: Rod of the Pact Keeper +1 (uncommon, Warlock-attunement), Staff of the Adder (uncommon, Cleric / Druid / Warlock), Staff of Charming (rare, Spellcaster), Staff of Healing (rare, Bard / Cleric / Druid), Wand of Web (uncommon, Spellcaster), Wand of Magic Detection (uncommon, no attunement), Wand of Fireballs (rare, Spellcaster), Wand of Wonder (rare, Spellcaster). All eight carry the canonical RAW charges shape (1 / dawn for the rod and Staff of the Adder, 10 + 1d8+2 / dawn for Staff of Charming, 10 + 1d6+4 / dawn for Staff of Healing, 7 + 1d6+1 / dawn for the rare wands matching Wand of Magic Missiles, 3 + 1d3 / dawn for Wand of Magic Detection). Snapshot refresh is part of this commit; the diff is exactly eight new IDs added to `withChargesIds`, bringing the charges-exercising item count from 7 (Wand of Magic Missiles + the batch-1.7 wave) to 15. Per-item action wiring (spell grants with item-fixed save DCs, summon-from-staff, charge-absorption-on-incoming-spell, variable-charge spell scaling, random-effects table) remains deferred behind the `UseItem` planner already named for batch 1.7's wave; gaps doc gained a parent bullet with eight per-item shapes covering the new primitives (Staff of the Adder's `SummonCreature`-with-damage-revert, Staff of Charming's save-DC override and absorption arm, Staff of Healing's item-level-progression override, Wand of Fireballs' variable-charges-per-cast, Wand of Wonder's `RollOnRandomTable` shape shared with Bag of Beans and Deck of Many Things). Magic-item count bumped from 77 to 85.

**Content authoring: items batch 1.10**

8 specific named armors and shields added to the starter pack, the defensive parallel to batch 1.9: Adamantine Armor (uncommon), Mithral Armor (uncommon), Elven Chain (rare), Glamoured Studded Leather (rare), Dragon Scale Mail (very rare), Demon Armor (very rare), Sentinel Shield (uncommon), Spellguard Shield (very rare). All eight ship as `itemKind: 'magic'` with `effects: []` and no charges, matching the existing Cloak of Protection / Armor +N template shape (consumer-side overlays template onto base armor or shield); snapshot-clean. Animated Shield and Plate Armor of Etherealness deferred to a future charges-bearing batch. The Deferred-wiring subsection gained a parent bullet for items-batch-1.10 with per-item sub-bullets cataloguing five new mechanical shapes: inbound-attack-crit-rewrite (Adamantine), armor-property-suppression (Mithral), armor-category-override (Elven Chain), resistance-by-dragon-color with item-instance variant (Dragon Scale Mail), item-curse plus natural-weapon-grant (Demon Armor, the natural-attack shape generalizes to monk unarmed strikes and other content-yet-to-ship), and composite save-and-attack defense with two save / attack sub-predicates (Spellguard Shield). Sentinel Shield wires entirely through existing `SetAdvantage` shapes once projection lands. Magic-item count bumped from 69 to 77.

**Content authoring: items batch 1.9**

8 specific named magic weapons added to the starter pack, extending beyond batch 1.5's generic +N templates with the iconic DMG roster: Vicious Weapon (rare), Sun Blade (rare), Frost Brand (very rare), Holy Avenger (legendary, paladin-attunement), Vorpal Sword (legendary), Dwarven Thrower (very rare, dwarf-attunement), Hammer of Thunderbolts (legendary), Berserker Axe (rare). All eight ship as `itemKind: 'magic'` with `effects: []` and no charges, matching the existing Flametongue Longsword shape (consumer-side overlays the template onto a base weapon); snapshot-clean. Second batch to exercise the schema's `attunementCondition` field after Pearl of Power's "Spellcaster" entry, this time for class lock (Holy Avenger / Paladin) and species lock (Dwarven Thrower / Dwarf); enforcement is data-only today. The Deferred-wiring subsection gained a parent bullet for items-batch-1.9 with per-item sub-bullets cataloguing six new mechanical shapes: on-crit special effect (Vicious extra dice, Vorpal sever-on-20), type-conditional damage rider (Sun Blade / Holy Avenger vs Undead-and-Fiend), composite passive with damage rider plus held-resistance plus narrative arm (Frost Brand), returns-on-throw plus type-conditional rider (Dwarven Thrower), multi-item-attunement-gated ability-score override composed with on-crit-save-or-die (Hammer of Thunderbolts), level-scaled max-HP projection plus item-curse-triggered compulsion (Berserker Axe), and the data-only-today attunement-precondition gate for class / species restrictions. Magic-item count bumped from 61 to 69.

**Content authoring: items batch 1.8**

8 common-rarity wondrous items added to the starter pack, filling the previously-empty common tier of the rarity ladder: Cloak of Many Fashions, Clothes of Mending, Pole of Angling, Hat of Vermin, Ear Horn of Hearing, Mystery Key, Shield of Expression, Tankard of Sobriety. All eight ship as pure stub (`itemKind: 'magic'`, `effects: []`, no charges, no attunement); snapshot-clean under slice 126. The Deferred-wiring subsection in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) gained a parent bullet for items-batch-1.8 with per-item sub-bullets categorizing each one as cosmetic-narrative (Cloak of Many Fashions, Clothes of Mending, Pole of Angling, Shield of Expression), table-discretion narrative (Mystery Key, Tankard of Sobriety, Hat of Vermin), or mechanical-via-existing-deferral (Ear Horn of Hearing — composes a `SuppressCondition` shape with the existing magic-item-effect-projection blocker). Charlatan's Die (1/day charges) was deferred to a future charges-bearing common batch to keep this batch snapshot-clean. Magic-item count bumped from 53 to 61; the rarity distribution now spans common through legendary for the first time.

**Content authoring: items batch 1.7**

6 charges-bearing magic items added to the starter pack, the long-queued followup the engine session called out after slice 126 narrowed the features snapshot to wired-only entries: Pearl of Power (uncommon, 1 charge / dawn, attuned by a spellcaster), Pipes of Haunting (uncommon, 3 charges, regain 1d4+1 at dawn), Wind Fan (uncommon, 1 charge / dawn), Circlet of Blasting (uncommon, 1 charge / dawn), Gem of Brightness (uncommon, 50 charges, no recharge), Driftglobe (uncommon, 1 charge / dawn). All six ship with the canonical RAW charges shape; per-item action wiring (spell grants, damage rolls, light-radius, item-disenchant-on-empty) remains deferred behind a `UseItem` planner that walks the activated item's effects and routes charge consumption, documented in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md). First batch to grow `withChargesIds` in the features snapshot since slice 126 (Wand of Magic Missiles was the only prior entry); the snapshot refresh is part of this commit and the diff is exactly six new IDs. Magic-item count bumped from 47 to 53; charges-exercising item count from 1 to 7.

**Content authoring: monsters batch 1.7**

5 low-CR Fiend introduction entries added (opens a wholly-unstarted creature type): Manes (CR 1/8), Lemure (CR 0), Dretch (CR 1/4), Imp (CR 1), Quasit (CR 1). Includes the canonical familiar pair. Three of the five (Imp, Lemure, Quasit) carry slice-112 `GrantResistance` B/P/S nonmagical trait entries; the RAW "not made with silvered weapons" further qualifier on Imp and Lemure is deferred. First batch to exercise the `subtype` field (`"devil"`, `"demon"`, `"devil, shapechanger"`, `"demon, shapechanger"`). Seven new mechanic-level deferrals introduced, with **Magic Resistance** as the headliner: missing both an "all saves" variant of `SetAdvantage.on` (today's save target requires an ability key) and an `event.isMagical` fact in the dispatcher. Affects Imp and Quasit in this batch but is canonical across most CR 5+ Fiends, Fey, Dragons, and higher-tier spellcasters across the MM. Other new deferrals: Shapechanger (composes with existing polymorph planner via monster-action wiring), Devil's Sight, at-will Invisibility (monster-action pathway to the existing invisible condition), Scare (recurring-save monster bonus action — shape exists via Hold Person's `recurringSave.onSuccess: 'removeCondition'`), Fetid Cloud (overlaps the existing area-effect spell mechanic), Hellish Rejuvenation (narrative). Monster count grows from 44 to 49.

**Content authoring: monsters batch 1.6**

5 low-CR Undead variety entries added: Shadow (CR 1/2), Specter (CR 1), Wight (CR 3), Ghost (CR 4), Wraith (CR 5). Fills the Undead pile out to a usable haunted-location roster. First batch to populate `traits` on monster statblocks: all five carry `GrantResistance` entries with `qualifier: 'nonmagical'` for B/P/S (slice 112 effect shape). The data is currently inert at runtime because `buildEffectStack` doesn't fold monster traits and `mitigateDamage` doesn't consult `monster.damageResistances` / `damageImmunities` / `damageVulnerabilities` — this is a pre-existing cross-cutting gap (Skeleton's bludgeoning vulnerability and Young Red Dragon's fire immunity have been inert since alpha.5), now documented as the third structural-gap bullet in the gaps doc. Authoring this batch RAW-accurately means the data wires for free when the engine consumes monster effects. Seven new mechanic-level deferrals introduced: Max-HP-reduction-on-hit (Life Drain across Specter/Wraith/Wight), Ability-score-reduction-on-hit (Shadow's Strength Drain), Incorporeal Movement, Environment-conditional advantage (Sunlight Sensitivity / Weakness, Shadow Stealth), On-kill-spawn (Wraith Create Specter, Shadow STR-0 spawn), Possession (Ghost), Etherealness (Ghost). Monster count grows from 39 to 44.

**Content authoring: monsters batch 1.5**

6 low-CR Beast tier-up entries added: Mastiff (CR 1/8), Boar (CR 1/4), Constrictor Snake (CR 1/4), Giant Frog (CR 1/4), Giant Centipede (CR 1/4), Black Bear (CR 1/2). Brings the Beast pile to encounter-table density for wilderness / dungeon ambient threats. All six ship as bare statblocks (`traits: []`); RAW Multiattack / Keen senses / on-hit save-or-condition rider fold into existing deferrals. Four new deferrals introduced and documented in the gaps doc: on-hit auto-grapple (Constrictor Snake, Giant Frog — bite that applies grappled / restrained without a save), Charge (Boar — movement-conditional +1d6 + STR save vs prone, needs a movement-since-turn-start fact), Relentless (Boar — flat-damage-cap variant of Undead Fortitude), Swallow (Giant Frog — compound multi-step action with recurring acid + blinded + restrained while consumed), and degree-of-failure save tier (Giant Centipede — fail-by-5 compounds to 0 HP + no heal for 1 hour). Monster count grows from 33 to 39.

**Content authoring: monsters batch 1.4**

4 caster NPCs added to close the spell-using humanoid gap: Apprentice Wizard (CR 1/4), Priest (CR 2), Druid (CR 2), Mage (CR 6). All four ship as bare statblocks (`traits: []`); their cantrip and prepared-spell tables fold into the existing monster-spellcasting deferral note alongside Acolyte and Cult Fanatic (six casters now waiting on the same primitive — when monster spellcasting lands, all six wire as pure data). One new deferral introduced: Divine Eminence (Priest's bonus-action slot-spend that adds radiant damage to the next weapon attack — needs a slot-spend-without-cast primitive composed with the existing on-hit-add-damage rider shape). Monster count grows from 29 to 33.

**Content authoring: items batch 1.6**

8 more wondrous / utility items added to the starter pack: Immovable Rod (uncommon), Ring of Jumping (uncommon), Folding Boat (rare), Eversmoking Bottle (uncommon), Marvelous Pigments (very rare), Apparatus of the Crab (legendary), Bag of Beans (rare), Quaal's Feather Token Anchor (rare). Three uncommon, three rare, one very rare, one legendary; only Ring of Jumping requires attunement. Pure stub shape (`effects: []`, no charges), snapshot-clean under slice 126. The Deferred-wiring subsection gained a parent bullet for items-batch-1.6 with five sub-bullets covering the RAW-shape blockers (positional anchoring, jump-distance derive surface, object transformation, vehicle + pilot-controlled actions, and a roll-up for the pure-DM-discretion items in this batch). Originally proposed Driftglobe in slot 2 but swapped for Ring of Jumping because Driftglobe carries 1/day Daylight charges, which would re-trip `withChargesIds`. Magic-item count bumped from 39 to 47.

**Content authoring: items batch 1.5**

6 numeric-bonus magic gear templates added to the starter pack: Weapon +1 (uncommon), Weapon +2 (rare), Weapon +3 (very rare), Armor +1 (rare), Armor +2 (very rare), Armor +3 (legendary). Shipped as generic templates rather than specific weapons / armors (the existing Flametongue Longsword is the only concrete +N weapon entry); consumers instantiate by overlaying a template onto a base weapon or armor. All `itemKind: 'magic'` with `effects: []` and no charges, snapshot-clean. The Deferred-wiring subsection gained a sixth bullet covering both blockers: the shared magic-item-effect-projection pass, plus an extension of the slice-76 `ItemInstance.temporaryBuff` shape into a permanent / inherent counterpart for the weapon templates. Magic-item count bumped from 33 to 39, broadening the rarity mix into very-rare and legendary for the first time outside Deck of Many Things.

**Content authoring: items batch 1.4**

8 more wondrous items added to the starter pack: Gloves of Missile Snaring, Lantern of Revealing, Cap of Water Breathing, Robe of Useful Items, Bracers of Archery, Winged Boots, Gloves of Swimming and Climbing, Stone of Good Luck. All uncommon, six attuned + two unattuned. Pure stub (`itemKind: 'magic'`, `effects: []`, no charges), snapshot-clean under slice 126. The Deferred-wiring subsection in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) gained four new bullets (reactive missile interception, activated breathlessness, per-day time-budget resource, items-waiting-on-projection) plus a pure-narrative bullet for Lantern of Revealing + Robe of Useful Items. Magic-item count bumped from 25 to 33.

**Content authoring: items batch 1.3**

6 consumable potions added to the starter pack: Potion of Climbing, Potion of Animal Friendship, Potion of Growth, Potion of Heroism, Potion of Mind Reading, Potion of Resistance. All ship as `itemKind: 'consumable'` with `onConsume: []` and a narrative `description` field, matching the existing three healing potions; the consume-side effect resolver that would walk `onConsume` and emit events is documented as a deferred primitive in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md). The Deferred-wiring subsection gained a single new bullet covering all six potions plus the variant shapes each one needs (durational-condition-from-consume, item-spell-grant routed through consume, durational enlarge buff, temp-HP composed with a Bless-shaped condition, and caster-choice-at-consume-time for the damage-type pick). Consumable count bumped from 3 to 9.

**Content authoring: items batch 1.2**

8 more wondrous items added to the starter pack: Headband of Intellect, Boots of Striding and Springing, Cloak of the Bat, Helm of Telepathy, Necklace of Adaptation, Decanter of Endless Water, Belt of Hill Giant Strength, Sending Stones. Six uncommon + two rare (Cloak of the Bat, Belt of Hill Giant Strength). All ship as `itemKind: 'magic'` with `effects: []`, snapshot-clean under the slice-126 narrowing. The "Deferred mechanical wiring" subsection in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) gained five new primitive entries (ability-score override, speed-floor + jump-multiplier, environment-conditional flight, environmental immunity, paired-item linkage) covering the batch's RAW shapes. Magic-item count bumped from 17 to 25.

**Content authoring: items batch 1.1**

8 uncommon wondrous items added to the starter pack: Goggles of Night, Cloak of Elvenkind, Slippers of Spider Climbing, Brooch of Shielding, Periapt of Wound Closure, Ring of Swimming, Eyes of the Eagle, Hat of Disguise. All ship as `itemKind: 'magic'` with `effects: []` (schema-only, matching the existing wondrous-item pattern); the engine primitives each would eventually need are catalogued in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) under "Deferred mechanical wiring." Magic-item count in the gaps doc bumped from 9 to 17.

The post-alpha.5 stretch: 75 slices. Slices 48–122 mix vocabulary expansion with infrastructure hygiene. The architectural skeleton is untouched. `SCHEMA_VERSION` unchanged; every new event shape and effect kind is additive. Test count grew from 1009 to 1371 across 192 files (+362 / +53); all replay-equivalence and RNG-capture invariants still hold.

The session cadence settled into a "primitive + canonical content user" shape: each slice adds a focused engine primitive (~50–200 LoC) and the first one or two RAW spells / features that exercise it, walking those entries from schema-only to wired in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md).

### Added

**Spell catalog completion** (10 sub-slices): every PHB 2024 spell now ships in the starter pack across L0–L9 (399 total, up from ~33). Per-level: 34 / 60 / 63 / 54 / 40 / 46 / 38 / 24 / 19 / 21. Mechanically-wired count grew from ~26 to ~152.

**Class-feature fill-out** (14 sub-slices): all 12 classes ship full L1–L20 feature tables. The class-feature matrix is fully wired through L7 across all classes; later tiers ship features at every grant level with effects wired where the primitive vocabulary covers them. Slices also wired specific subclass features: Cleric Blessed Strikes (Divine Strike at L7+), Paladin Aura Improvements (L18).

**Engine primitives** (~35 sub-slices). Grouped by category:

*Summon system* (slice 48). New `CompanionSummoned` / `CompanionDismissed` events plus a `summon` `SpellMechanic` kind carrying inline statblock (name, AC, hpBase, hpPerSlotAbove, baseSlotLevel, speedFeet). Companions land as `kind: 'creature'` Characters with a `summonSource` pointer to controller / spell / slot / effect. Concentration auto-dismiss via `clearConcentrationEffect`. 11 spells wired across the summon family (find-familiar, unseen-servant, find-steed, summon-beast, animate-dead, conjure-animals, phantom-steed, summon-fey / lesser-demons / shadowspawn / undead, and others).

*On-hit trigger system* (slice 61). New `consumeOnTrigger: boolean` field on `OnEvent` effects (one-shot riders lift their parent condition after firing). Smite cohort wired: divine-favor, searing-smite, wrathful-smite, thunderous-smite, branding-smite.

*Aura primitive* (slice 63) plus *source-relative formulas* (slice 64). New `GrantAura { auraId, rangeFeet, allyConditionId }` effect: discoverable metadata for consumers (dndbnb / VTTs) to project a condition onto in-range allies; the engine doesn't auto-project (position is consumer territory). The `sourceAbilityMod` Formula kind plus the threading needed to evaluate Formula values inside `AddModifier` effects: `EffectAccumulator` now folds Formula values into numeric modifiers when a `FormulaContext` is provided. Paladin Aura of Protection (L6, +CHA-mod-of-source to ally saves), Aura of Courage (L10, Frightened immunity), Crusader's Mantle (L3 spell) all wire.

*Condition-immunity gate* (slice 66). New `isImmuneToCondition(state, content, targetId, conditionId)` derive helper consulted by the spell planner's save (`conditionOnFail`) and buff branches before staging `ConditionApplied` events. Aura of Courage now actually blocks Frightened.

*Area-effect via aura-damage* (slices 68 / 70 / 71 / 72). The `aura-damage` SpellMechanic was extended four times: optional damage + conditionOnFail (slice 68); optional saveAbility (slice 70 — Cloud of Daggers no-save zone); `trigger` tag for multi-component zones (slice 71 — Hunger of Hadar's 2d6 cold on enter + 2d6 acid on turn end). A sibling `movement-damage` mechanic (slice 72) for per-foot-moved damage (Spike Growth) with a dedicated `engine.plan.tickMovementDamage` planner. 12 zone spells wired.

*Composite-buff confirmation* (slice 73, content). Conditions can carry multiple effects in their `effects` array. Haste, blur, pass-without-trace ship as composite-buff conditions with no engine change.

*AC-buff condition* (slice 74). New `SetACFloor { value }` effect kind, `acFloors` collector on EffectAccumulator, floor check in `computeAC` that bumps the natural total. Shield of Faith and Barkskin wire.

*Temp-HP grant primitive* (slice 75). New `temp-hp` SpellMechanic kind plus a planner that rolls dice + flat + slot-scaling and emits `TempHPGranted`. False Life wires.

*Item-buff primitive* (slices 76 / 90). New `ItemInstance.temporaryBuff` (attackBonus, damageBonus, sourceEffectInstanceId, source) plus `ItemBuffApplied` / `ItemBuffRemoved` events and reducers. `engine.plan.magicWeapon` drives Magic Weapon. Slice 90 extended the shape with `extraDamageDice` + `extraDamageType` for Elemental Weapon's per-hit elemental rider; the attack planner rolls the extra dice on hit (doubled on crit) and emits a second damage component. `clearConcentrationEffect` walks item instances and strips any buff tagged with the dropped effect's id.

*Movement-mode retrofit* (slice 77). `getEffectiveSpeed` retrofitted so `ModifySpeed` entries actually affect the engine's move planner. Fly + Spider Climb wire as buff conditions. Retrofits also Fast Movement, Unarmored Movement, Roving, Haste, and the Grappled/Restrained/Paralyzed/Petrified/Unconscious zero-speed conditions.

*Push primitive* (slice 78). New `CreaturePushed` informational event (engine doesn't model positions; consumer applies the position change). Save mechanic gains optional `pushedFeetOnFail`. Gust of Wind + Earthbind wire.

*Recurring-rider primitive* (slice 79). New `recurring` SpellMechanic kind (effect: temp-hp / heal / damage, optional dice / flat / addCasterAbilityMod / damageType). New `engine.plan.tickRecurring({casterId, targetId})` planner. Heroism wires the two-part shape (cast-time buff + per-tick TempHPGranted).

*Falling-protection primitive* (slice 81). New `GrantFallingProtection` Effect kind. `engine.plan.falling` short-circuits to no events when present. Feather Fall wires as a buff that lasts 1 minute.

*Caster-chosen options at cast time* (slices 82–87). New `CasterChoice` discriminated union on `CastSpellIntent` (`{kind: 'damageType', value}` and `{kind: 'variant', value}`). Three primitive variants:
  - `casterChoosesDamageType: { allowed: DamageType[] }` on attack mechanics (Chromatic Orb).
  - `casterChoosesVariant: { variants: [{ key, conditionId }] }` on buff mechanics (Enlarge/Reduce) and save mechanics (Calm Emotions).
  - Command (5-variant save), Enhance Ability (6-variant buff), Bestow Curse (4-variant save) all wire as pure data on top of the primitives. All four of Bestow Curse's variants are subsequently wired by later slices (88 extra-damage, 92 inactive-turn, 96 attack-disadvantage); the ability-disadvantage variant remains narrative-only pending a nested-sub-choice primitive.

*Target-side source-filtered on-hit rider* (slice 88). `AppliedCondition` provenance threaded through `dispatchTriggers`; new `event.attackerIsSource` fact lets a target-side rider filter on "the creature attacking me is this condition's source." Hex's recurring on-hit rider wires; Bestow Curse's extra-damage variant retrofits.

*Spirit Shroud variants* (slice 89). Caster-chosen 3-variant buff (cold / necrotic / radiant). Each variant carries an OnEvent rider that adds +1d8 of the chosen damage type on the caster's weapon hits.

*Dedicated reaction planner pattern* (slice 91). New `engine.plan.absorbElements({casterId, slotLevel, damageType, triggeringDamageEventId, halvedAmount})`. Compensating Healed event for the absorbed half (event-sourcing-friendly: doesn't mutate the triggering DamageApplied). Per-element charged condition (`absorb-elements-charged-acid-active` and four siblings) with `consumeOnTrigger` OnEvent rider for the on-next-hit elemental damage rider.

*Recurring-save primitive* (slices 92 / 93). New `recurringSave` field on `ConditionSchema` (ability + trigger + onFail + onSuccess). `engine.plan.tickRecurringSave({targetId, conditionId})` rolls a save against the source caster's spell DC. `onFail: 'consumeAction'` wires Bestow Curse's inactive-turn variant. `onSuccess: 'removeCondition'` wires Hold Person, Hold Monster, Tasha's Hideous Laughter, Confusion (the RAW "spell ends on the target" branch).

*Trap mechanic* (slice 94). New `TrapSchema` runtime collection on `CampaignState` (id, label, sourceCharacterId, sourceSpellId, payload, chargesRemaining). Three events: `TrapArmed` (stamps into state.traps), `TrapTriggered` (decrements charges), `TrapExpired` (removes from state, with chargesExhausted / dispelled / duration reasons). New `trap` SpellMechanic; new `engine.plan.triggerTrap({trapId, triggeringCharacterId})`. Glyph of Warding (Explosive Runes variant) + Cordon of Arrows wire.

*Resistance-buff confirmation* (slice 95, content). The engine already supported per-element `GrantResistance`; Protection from Energy ships as a 5-variant buff. No engine change.

*Directional attack disadvantage* (slice 96) plus *generic attacker-side advantage wiring* (slice 97). New `SetAdvantageVsSource` Effect kind: filters on the roll's target equaling the bearing applied condition's `sourceCharacterId`. BuilderContext gains an optional `sourceCharacterId`. EffectAccumulator gains `setAdvantageVsSource` / `advantageVsSource`. Attack planner consults `attackerEffects.advantageVsSource('attack', targetId)`. Bestow Curse's attack-disadvantage variant wires. Slice 97 closes a latent bug: the existing six conditions (blinded, frightened, poisoned, prone, restrained giving disadvantage, invisible giving advantage) already carried `SetAdvantage on:'attack'` effects but the attack planner never consulted `attackerEffects.advantageFor('attack')`. Now it does.

*Heal-blocking primitive* + *ApplyCondition TriggerAction dispatch* (slice 98). New `BlockHealing` Effect kind, accumulator query, and `isHealingBlocked` derive helper. `planHealMechanic` consults the helper; when blocked, emits Healed with `amount: 0` and `(blocked)` annotation in `source`. ApplyCondition TriggerAction handler added to `fireTrigger` (previously only AddDamage was wired). Spirit Shroud's three variants gain a second TriggerAction applying `healing-blocked-active` to hit targets.

*AddDamageToAttacker TriggerAction* (slice 100). Retaliation variant of AddDamage: dispatcher routes the resulting DamageApplied to `event.attackerId`. Crits don't double the retaliation dice per RAW. Fire Shield wires as a 2-variant buff (warm: cold resistance + fire retaliation; chill: fire resistance + cold retaliation).

*Multi-effect buff content sweep* (slice 101). Three high-level buff spells wired through existing primitives, no engine change. Foresight (L9) ships as a 14-effect condition: per-ability SetAdvantage on each save / check plus SetAdvantage on attack plus ImposeDisadvantageOnAttackers; the RAW "can't be surprised" clause stays narrative since surprise is initiative-order state, not a modeled condition. Mind Blank (L8) ships as a 2-effect condition: GrantConditionImmunity charmed plus GrantImmunity psychic; the thought-reading / scrying-detection clauses stay narrative. Invulnerability (L9) ships as a 1-effect condition: GrantImmunity damageType `'all'`. Wired spell count goes from ~144 to ~147 (~252 schema-only remain).

*Auto-expiry for trigger-applied conditions* (slice 102). Closes the loose end slice 98 left: `durationRounds` on an `ApplyCondition` TriggerAction was declarative metadata the engine never consumed. Now `fireApplyCondition` (inside an active encounter) stamps `expiresOnRound = currentRound + durationRounds` on the emitted ConditionApplied, and `planAdvanceTurn` sweeps every character's `appliedConditions` on each turn-start, emitting `ConditionRemoved` for any whose `sourceCharacterId` matches the new active combatant and whose `expiresOnRound` is in the past. Spirit Shroud's heal-block now auto-expires at the start of the caster's next turn per RAW. Outside an encounter the expiry stays consumer-managed (currentRound is undefined). Pays forward for every future trigger-applied condition that ships with a `durationRounds`.

*Type-conditional buff / ward primitive* (slice 103). Three pieces. (1) The existing `condition?: Predicate` field on `ImposeDisadvantageOnAttackers` was previously dropped at builder time; the accumulator now stores per-entry predicates and `imposesDisadvantageOnAttackers(attackerFacts)` filters them against caller-supplied facts. (2) New `getCreatureType(character, content)` derive helper resolves creature type from `statblockId` (monsters) → `speciesId` (PCs / NPCs) → default `Humanoid`. (3) New `ApplyConditionToAttacker` TriggerAction variant: retargets the emitted ConditionApplied to the attacker of the triggering AttackRolled (the conditions analog of `AddDamageToAttacker`). The attack planner threads the attacker's creature type into the disadvantage query, and `dispatchTriggers.buildEventFacts` now stamps `event.attackerCreatureType` / `event.targetCreatureType` on AttackRolled events so OnEvent filters can gate on type. Canonical content users: Protection from Evil and Good (L1) and Magic Circle (L3) ship the type-gated ImposeDisadvantageOnAttackers wired against the six warded types (aberration, celestial, elemental, fey, fiend, undead); Holy Aura (L8) ships a multi-effect concentration buff combining per-ability save advantage, universal ImposeDisadvantageOnAttackers, and an OnEvent rider with `event.attackerCreatureType in ['Fiend', 'Undead']` emitting `ApplyConditionToAttacker { blinded }`. PfEoG's RAW source-gated condition-immunity arm (charmed / frightened only from the six types) stays narrative-only pending a source-predicate on GrantConditionImmunity. Magic Circle's RAW caster-choice-of-type isn't modeled (broader than RAW: applies against all six).

*Source-predicate on GrantConditionImmunity* (slice 104). Closes slice 103's loose end: PfEoG's RAW charmed / frightened immunity arms now actually gate. `GrantConditionImmunity` gains an optional `condition?: Predicate` field; the EffectAccumulator stores per-conditionId lists of `{predicate?}` entries (rather than the prior plain Set), and `hasConditionImmunity(id, sourceFacts?)` filters them against caller-supplied source facts. `isImmuneToCondition` gains an optional `sourceCharacterId`; when supplied, the helper builds a `sourceCreatureType` fact from the source's record via `getCreatureType` and threads it down. All three call sites pass it: the cast-spell save and buff branches use `intent.characterId`; the concentration aura tick uses `intent.casterId`. Protection from Evil and Good and Magic Circle ship the matching source-gated GrantConditionImmunity entries for charmed and frightened. PfEoG's RAW possessed clause stays narrative (the engine has no `possessed` condition). Unfiltered immunity entries (Aura of Courage's Frightened immunity, Freedom of Movement's Paralyzed immunity, etc.) resolve identically to the old behavior — predicate-free entries match unconditionally.

*Source-relative formula threading across derive surfaces* (slice 105). Floor-raising. Slice 64 introduced the `sourceAbilityMod` Formula kind and threaded `characters` through `computeSavingThrow`; the other derive surfaces ignored the field, so a hypothetical source-relative `AddModifier` on an ability check / AC / attack / spell DC / action economy / damage mitigation entry silently resolved to 0. This slice threads `characters` through every remaining derive input shape (`ComputeAbilityCheckInput`, `ComputeACInput`, `ComputeAttackInput`, `ComputeSpellDCInput`, `ComputeActionEconomyInput`, `MitigateDamageInput`, `ComputeDerivedCharacterInput`), updates the `engine.derive.*` consumer surface to pass `state.characters`, and updates ~25 planner-side call sites (`attack`, `cast-spell`, `concentration`, `offhand-attack`, `falling`, `recurring-save`, `reactive-spells`, `transformations`, `stunning-strike`, `checks`, `travel`) to pass through. No PHB content uses source-relative formulas outside saves today, so verification rides on a hand-built test condition in [tests/unit/derive/source-relative-formulas.test.ts](tests/unit/derive/source-relative-formulas.test.ts); future content that uses the shape (e.g., a hypothetical "+CHA-mod-of-source to one ability check" buff) wires for free across every derive surface.

*Utility-buff content sweep* (slice 106). Four utility spells walked from schema-only to wired via existing primitives, no engine change. Blade Ward (L0) ships as a 3-effect condition (GrantResistance for bludgeoning / piercing / slashing). Resistance (L0) ships as a Guidance-shaped narrative buff (empty-effect `resisted` condition; consumer rolls the 1d4 and removes). Longstrider (L1) ships as a `ModifySpeed walk add 10` buff. Darkvision (L2) ships as a `GrantSense darkvision 60` buff. Wired spell count ~150 → ~154; conditions 31 → 35.

*Sanctuary (dedicated reaction-style planner)* (slice 107). Pre-attack target-selection rider — different shape from the bearer-side reactions (Shield / Counterspell / Absorb Elements). New `SanctuaryProtected` event + `engine.plan.sanctuaryWardSave({ attackerId, wardedCharacterId })` planner: looks up the warded creature's `sanctuary-active` applied condition + its source caster, rolls the attacker's WIS save against the caster's spell DC via `computeSavingThrow` + a fresh d20 (with advantage / disadvantage threaded from the attacker's effect stack), emits `SaveRolled`, and on failure emits `SanctuaryProtected` (record-only). The outcome exposes `prevented: boolean` for consumer branching; on a failed save the consumer drops the attack. The `sanctuary-active` condition carries an OnEvent rider with `consumeOnTrigger: true` and filter `event.attackerIsSelf === true`, so the spell ends when the warded creature makes any attack roll per RAW (the "casts a harmful spell / deals damage" RAW vectors aren't modeled — no `castDeclared-with-hostile-target` or `dealtDamage` facts today). Sanctuary becomes the 5th wired reaction spell (after counterspell / shield / absorb-elements / guidance via consume-d4).

*On-miss trigger primitive* (slice 108). Two small pieces. (1) New `event.targetIsSource` fact in `dispatchTriggers.buildEventFacts` — mirror of the slice-88 `event.attackerIsSource` fact, true when the bearing AppliedCondition's `sourceCharacterId` equals the event's target. (2) New `sourceFromEventTarget?: boolean` flag on `ApplyConditionToAttacker`: when true, the emitted ConditionApplied stamps `sourceCharacterId = event.targetId` (the missed creature) instead of the rider's bearer. Fighter L13 Studied Attacks ships as the canonical user: an OnEvent rider on the class feature (`event.attackerIsSelf === true && event.hit === false`) emits the new action with `studied-target-active`. The condition pairs `SetAdvantageVsSource(on: 'attack', advantage)` with a consume-on-next-attack OnEvent rider (`attackerIsSelf && targetIsSource`, consumeOnTrigger) that lifts after the fighter's next attack against the studied creature. RAW "before end of your next turn" time-window expiry isn't modeled (consumer manages cleanup at end of turn); consecutive misses against the same target don't refresh because ConditionApplied dedupes by conditionId while the consume rider still fires.

*Turn-end auto-expiry primitive* (slice 109). Extends slice 102's `expiresOnRound` model with a turn-end branch for "until end of your next turn" / "1 round" durations. New declarative `autoExpiry?: { afterRounds, trigger }` field on ConditionSchema; new `expiryTrigger?: 'turnStart' | 'turnEnd'` on ConditionApplied event + AppliedCondition runtime (default 'turnStart' for backwards compat with slice 102). The cast-spell buff branch reads the condition def's autoExpiry and stamps `expiresOnRound + expiryTrigger` on the emitted event when cast inside an active encounter. `planAdvanceTurn` gains a turn-end sweep that fires after `TurnEnded` for entries with `expiryTrigger: 'turnEnd'` and `sourceCharacterId == endingCombatant.id` (self-cast buffs match since source == bearer). The existing turn-start sweep skips turn-end entries so they don't double-fire. Blade Ward is the canonical user: `autoExpiry: { afterRounds: 1, trigger: 'turnEnd' }` on the `blade-warded-active` condition, so the 3-effect B/P/S resistance buff lifts at the end of the bearer's turn in the round after the cast per RAW.

*Concentration cleanup for rider-applied conditions* (slice 110). Closes a leak where OnEvent riders living inside a parent concentration condition emit ConditionApplied targeting *other* creatures (Spirit Shroud's heal-block on hit targets, Holy Aura's blinded on attacking fiends / undead) but `EffectInstance.conditionsApplied` only tracked the direct cast-time apply on the bearer. New `sourceEffectInstanceId?: ULID` on `ConditionAppliedEvent` and `AppliedCondition`. The dispatcher walks `state.effectInstances` to find the parent EffectInstance for whichever AppliedCondition fired the rider and stamps the id on emitted events. `clearConcentrationEffect` gains a second sweep across every character's appliedConditions that removes matching entries (reversing any `hpMaxBonusDelta` first, mirroring the direct-conditions loop). Non-concentration riders (Fighter Studied Attacks, etc.) leave the field undefined and behave unchanged.

*On-fatal-damage intercept primitive* (slice 111). New `PreventFatalDamage` marker Effect kind. When a condition carrying it lives on a creature and incoming damage would drop HP to 0 or below, the damage components are scaled proportionally so HP lands at 1 and the bearing condition is consumed via a paired ConditionRemoved event. Reducers are pure (cannot emit new events), so the intercept lives planner-side as `interceptFatalDamage` in [src/derive/fatal-damage-intercept.ts](src/derive/fatal-damage-intercept.ts). Every primary damage emitter — planAttack, planCastSpell (3 paths), planFalling, planOffhandAttack, planWeaponMastery Graze, planTrap, concentration aura damage, recurring damage — calls it after `mitigateDamage` and before constructing the DamageApplied event. Multi-event emitters feed an `applyAll(state, eventsSoFar)` snapshot to the helper so per-event intercepts see prior events' damage applied. Death Ward ships as the canonical user (`death-ward-active` condition + buff wiring on the spell). Initial limitation closed by slice 114: rider-emitted damage from the trigger dispatcher initially bypassed the intercept.

*Nonmagical-attack resistance qualifier* (slice 112). `GrantResistance` gains an optional `qualifier: 'nonmagical' | 'magical'` field. When set, the resistance check fires only if the incoming damage source matches. Plain (unqualified) resistances behave unchanged. EffectAccumulator's `resistances` Set becomes a `Map<DamageType, Array<{qualifier?}>>`, mirroring the slice-104 shape on `conditionImmunities`. `hasResistance(type, sourceIsMagical?)` applies any-match semantics across stacked entries. `mitigateDamage` gains a `sourceIsMagical?: boolean` input field; every emitter that goes through the mitigation pipeline now passes it. New [src/derive/magicality.ts](src/derive/magicality.ts) helper `isMagicWeaponAttack` reads `ItemInstance.temporaryBuff` and falls back to `ItemDefinition.itemKind === 'magic'`. Stoneskin ships in SRD form (`stoneskin-active` condition with B/P/S `GrantResistance` entries each qualified `nonmagical`). Same primitive unblocks the common MM "resistance to B/P/S from nonmagical attacks" monster trait for future statblock authoring.

*Route rider damage through mitigateDamage* (slice 113). Closes the limitation documented on slices 111 + 112: the trigger dispatcher's `fireAddDamage` and `fireAddDamageToAttacker` plus a couple of one-off emitters (weapon-mastery Graze, trap base damage) emitted raw DamageApplied events that bypassed the mitigation pipeline entirely. Resistance / immunity / vulnerability / flat reduction / the slice-112 magicality qualifier all silently failed for rider damage. Every damage emitter now runs mitigateDamage. For dispatcher riders the magicality is inferred per-rider via a new `isRiderMagical` helper: spell-sourced rider (bearing condition tracked by an EffectInstance with a spellId) counts as magical (Spirit Shroud, Hex, Crusader's Mantle, smite cohort, Fire Shield retaliation); AttackRolled rider with weaponInstanceId inherits the weapon's magicality (Sneak Attack on a magic longsword counts as magical); otherwise non-magical (conservative default).

*Rider-aware on-fatal-damage intercept* (slice 114). Closes slice 111's documented limitation: Death Ward did not fire when a dispatcher rider (Sneak Attack, smites, Spirit Shroud rider, Fire Shield retaliation) alone would drop the warded target. Two changes plug the gap. (1) `fireAddDamage` / `fireAddDamageToAttacker` now consult `interceptFatalDamage` on every rider DamageApplied; when the rider would drop the bearer, components scale so HP lands at 1 and a paired ConditionRemoved consumes the bearing condition. (2) `dispatchTriggers` advances a `runningState` via `applyAll` between fired triggers — cadence checks, appliedConditions, the intercept's HP guard, and the `consumeOnTrigger` parent-lookup all see post-prior-rider state. Closes a latent gap where a rider-consumed condition was still visible to later riders on the same character.

*Predicate-gated AddModifier* (slice 115). `AddModifier.condition?: Predicate` was being silently ignored by the effect builder. This slice honors the condition at modifier-sum time. Each `ModifierContribution` now optionally carries the predicate alongside its source + value; `EffectAccumulator.modifierSum` (and `modifierBreakdown`) accept a `facts?: ReadonlyMap<string, unknown>` and skip contributions whose predicate fails. Predicate-less modifiers continue to apply unconditionally, so all existing unconditional modifiers behave unchanged. Canonical user: Archery Fighting Style wired with `{ kind: 'eq', path: 'event.attackKind', value: 'ranged' }`. `computeAttackBonus` populates the `event.attackKind` fact from the weapon definition's `attackKind` property (already in content), so ranged-weapon attacks see the +2 and melee attacks don't. Same wiring on the Ranger L2 Fighting Style "archery" option. Same plumbing unblocks the rest of the Fighting Style gates as facts get populated.

*bearer.wearingArmor fact + Defense Fighting Style* (slice 116). Extends slice 115's predicate-gated `AddModifier` pattern to the AC derive path. `computeAC` builds a `bearer.wearingArmor` fact (true iff the character has an item equipped in the armor slot) and feeds it to both `modifierSum('ac', facts)` sites — the natural-armor branch and the regular armor branch. Defense's +1 AC ships across all four pack locations (standalone feat + Fighter / Paladin / Ranger L2 options) with `condition: { kind: 'eq', path: 'bearer.wearingArmor', value: true }`. RAW-correct. Same plumbing accommodates future `bearer.*` facts: `bearer.tempHpAbove` for Armor of Agathys, `bearer.notIncapacitated` for Mirror Image, `bearer.offHandKind` for Dueling.

*Dueling Fighting Style + Frenzy melee gate* (slice 117). Closes the third of the four Fighting Style gates and surfaces a latent miss: the `target: 'damage'` modifier sum was never being consumed by any planner. Five effects relied on it (4 Dueling entries + 1 Frenzy condition) and all were dormant. `planAttack` now consumes `attackerEffects.modifierSum('damage', facts)` into `damageRollPayload.modifier`. The facts map carries `event.attackKind` (from the weapon definition) and `bearer.offHandHasWeapon` (true iff the off-hand slot holds an item whose def is `itemKind: 'weapon'`; shields live in a separate slot). Dueling's +2 ships across all four pack locations with `condition: { kind: 'all', terms: [eq(event.attackKind, 'melee'), eq(bearer.offHandHasWeapon, false)] }`. The Frenzy condition (`frenzied`) gets the same melee predicate. Both effects now correctly apply.

*CI hygiene: Node 24 migration* (slice 118). GitHub flagged `actions/checkout@v4` and `actions/setup-node@v4` for Node 20 EOL on 2026-06-02. Both actions bumped to `@v5` (Node 24 runtime) across all three workflows: ci.yml, deploy-demo.yml, deploy-dndbnb.yml. CI test matrix updated from `[18, 20, 22]` to `[20, 22, 24]`: Node 18 reached EOL on 2025-04-30 (drop), Node 24 is the current line (add). package.json `engines.node` bumped from `>=18` to `>=20` to match. Deploy workflows stay on Node 22 (the LTS we target for production-bundle output); only the test matrix exercises the wider range.

*Two-Weapon Fighting Fighting Style* (slice 119). Closes the 4th and final Fighting Style gate (the FS track is fully wired). New `GrantTwoWeaponFighting` marker Effect kind — same shape as `GrantEvasion` / `GrantFallingProtection`: no fields, accumulator flag, planner queries via `hasTwoWeaponFighting()`. `planOffHandAttack` now builds an attacker effect stack and flips the damage-modifier rule when the flag is set: instead of stripping positive ability mods (RAW default for off-hand attacks), the full ability modifier lands on the damage roll. Wired across all three pack locations (standalone feat + Fighter L1 option + Ranger L2 option). Features-coverage snapshot refreshed for the new effect kind.

*Protection Fighting Style reaction planner* (slice 120). RAW 2024: "When a creature you can see attacks a target other than you that is within 5 feet of you, you can use your reaction to impose Disadvantage on the attack roll. You must be wielding a Shield." Closes 5 of 6 PHB 2024 Fighting Styles — only Great Weapon Fighting remains. New `GrantProtectionFightingStyle` marker Effect kind (same pattern as GrantTwoWeaponFighting). New `ProtectionUsed` record-only event (record protector / attacker / triggering attack id / fresh d20). New `engine.plan.protection({ protectorId, attackerId, triggeringAttackEventId })` reaction planner: validates the protector has the FS marker + has a shield equipped + hasn't used their reaction, rolls one fresh d20, and emits `ActionEconomyConsumed { kind: 'reaction' }` + `ProtectionUsed`. Position / vision gates ("ally within 5 ft", "attacker visible") stay consumer-side. The consumer pairs the new d20 with the original AttackRolled.d20 to compute the disadvantaged outcome (lower of the two). Wired across all three pack locations (standalone feat + Fighter L1 option + Paladin L2 option). Transcript formatter, apply.ts no-op reducer, features-coverage snapshot all refreshed.

*Great Weapon Fighting Fighting Style* (slice 121). Closes the 6th and final 2024-PHB Fighting Style — the entire Fighting Style track is now wired. RAW 2024: "When you roll damage for an attack you make with a Melee weapon that you are holding with two hands, you can treat any 1 or 2 on a damage die as a 3." New `GrantGreatWeaponFighting` marker Effect kind (same shape as the other FS markers). `planAttack` applies the substitution in place on the rolled damage dice when three gates pass: the attacker's effect stack carries the flag, `weaponDef.attackKind === 'melee'`, and the wield is two-handed (`weaponDef.properties` includes `two-handed`, or includes `versatile` AND both `attacker.equipped.offHand` and `attacker.equipped.shield` are undefined — stricter than the existing Flex `wieldedTwoHanded` because a shield occupies a hand even though the engine tracks it in a separate slot). RAW's "the weapon's damage dice" wording is honored: the slice-90 `extraDamageDice` rider (Elemental Weapon etc.) is NOT touched, only the weapon's native dice array. Wired across all three pack locations (standalone feat + Fighter L1 + Paladin L2). Features-coverage snapshot refreshed.

*Numeric-comparison predicates + Armor of Agathys* (slice 122). Two new predicate kinds (`gt` / `gte`) take a `path` and a numeric `value`; the evaluator looks up the fact at `path`, returns false when missing or non-numeric, otherwise compares. New `bearer.tempHp` fact populated by the trigger dispatcher's `buildEventFacts` (true for any character carrying an OnEvent rider — exposes their current temp HP for predicate use). New `armor-of-agathys` spell on the warlock list: cast emits a TempHPGranted (5 base + 5 per slot above 1) plus a ConditionApplied for `armor-of-agathys-active`. The condition's OnEvent rider fires on AttackRolled with filter `all(targetIsSelf, hit, bearer.tempHp gt 0)` and emits `AddDamageToAttacker { dice: '0d6+5', damageType: 'cold' }`. The dice parser's `count <= 0` reject was relaxed to `count < 0` so "0d6+5" parses as flat 5 — lets dice-shaped slots carry flat damage without a separate primitive. RAW gate "melee attack" subsequently closed by slice 123 (see below); temp-HP provenance still unmodeled (engine `hp.temp` is a single number, so the rider fires for any temp HP source, not just AoA's).

*`event.attackKind` fact, retaliation tightened to melee-only* (slice 123). New required `attackKind: 'melee' | 'ranged'` field on `AttackRolledEvent`, populated by `planAttack` and `planOffHandAttack` from `weaponDef.attackKind` and by `castSpell`'s attack mechanic as `'ranged'` (every currently-wired PHB 2024 spell attack is a ranged spell attack). Surfaced as the `event.attackKind` fact in `dispatchTriggers.buildEventFacts`. Armor of Agathys + Fire Shield (warm / chill) gain a fourth `eq event.attackKind melee` clause on their retaliation OnEvent rider filters — ranged attackers no longer trigger retaliation, matching RAW. Thrown weapons (dagger, handaxe) classify as melee since the engine doesn't model thrown-vs-melee-strike intent. Future ranged-only riders (Bracers of Archery etc.) read the same fact.

*Mirror Image attack-deflection pool* (slice 124). New `MirrorImageDeflectedEvent` + reducer that decrements the bearer's `mirror-image-active` condition `level` (3 → 2 → 1 → condition removed). New derive helper `findMirrorImage` (src/derive/mirror-image.ts) + thresholds (6 / 8 / 11 by duplicate count). `planAttack` and `planOffHandAttack` intercept incoming weapon attacks: roll the deflection d20 first, and on redirect-success the attack rolls against duplicate AC = 10 + bearer DEX mod. Deflected attacks emit `AttackRolled` with `hit: false` (so bearer-side retaliation riders don't double-fire) + the new event; duplicate-destroying hits at duplicates=0 also emit a follow-up `ConditionRemoved`. New optional `appliedConditionLevel?: number` on `SpellBuffMechanicSchema` lets a buff stamp an initial pool count at cast time (Mirror Image: 3). Vision-gate exemptions (blind / blindsight / truesight attackers) and the cast-spell attack-mechanic path (rare in practice — most attacks against the bearer are weapon attacks) are documented deferrals.

*Trigger dispatch on deflected Mirror Image attacks* (slice 125). Closes the slice-124 loose end: `tryBuildDeflectedAttack` now routes the deflected `AttackRolled` through `dispatchTriggers` so attacker-side on-miss / on-attack riders (Studied Attacks' consume-on-attack-vs-source, future similar) fire as RAW expects. Bearer-side hit-gated riders (AoA, Fire Shield) still don't fire because the event carries `hit: false`. Helper signature gains `state` and `content`; planAttack and planOffHandAttack thread them through. New test case in `plan-mirror-image.test.ts` seeds a fighter with `studied-target-active` sourced to a Mirror-Image-warded bearer and asserts the consume rider fires on the deflected attack.

*Narrow features-test snapshots to wired-only entries* (slice 126). Test-infrastructure refactor. The per-id catalog snapshots in `tests/coverage/features.test.ts` (class features, subclasses, conditions, feats, magic items) tripped on every stub addition, contradicting the parallel-authoring contract's "pure content additions shouldn't move this snapshot" claim. All five per-id snapshots are now narrowed to track only wired entries (effects-bearing). Pure stub additions no longer trip; wire-status flips, wired additions, and wired-entry removals still trip. The aggregate "wired vs stub" counts test for class features is dropped (its signal is implicit in the wired list's length). `docs/parallel-authoring.md` lines 50, 70, 102 updated to the truthful new contract. Monsters were never in any features.test.ts snapshot — the monster lane was always clean and stays clean.

*Vision senses on the effect stack + Mirror Image RAW bypass* (slice 127). The `GrantSense` effect kind was schema-only: the accumulator no-op'd it and downstream consumers had no way to ask "does this attacker see by blindsight / truesight / etc.?". This slice wires the primitive: `EffectAccumulator.grantSense(sense, range)` records grants, `hasSense(sense)` is a boolean, `senseRange(sense)` returns max range (RAW: overlapping grants take the larger range, not the sum). `applyEffectToBuilder` dispatches `GrantSense` to `grantSense`. Canonical user: Mirror Image's RAW vision-gate. `planAttack` and `planOffHandAttack` now compute `attackerBypassesMirrorImage = attackerEffects.hasSense('blindsight') || attackerEffects.hasSense('truesight') || attacker carries 'blinded'` and skip the deflection branch when true (PHB 2024: "A creature is unaffected by this spell if it can't see, if it relies on senses other than sight, or if it can perceive illusions as false."). Secondary canonical user: Boon of Truesight (epic-boon feat) flipped from `effects: []` to `[{ kind: 'GrantSense', sense: 'truesight', range: 60 }]`, exercising the primitive via `featsTaken`. Tests: two new `EffectAccumulator` units (sense propagation + larger-range-wins), two new Mirror Image planner cases (truesight attacker bypasses; blinded attacker bypasses). The features snapshot moved as expected (`boon-of-truesight` flipped from stub to wired in the per-id feats catalog), validating the slice-126 design intent. The Goggles of Night deferral note in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) updated to reflect that the GrantSense primitive ships now and the remaining blocker is the magic-item-effects-projection gap, not GrantSense itself.

*Dedicated Thunder Step planner* (slice 128). `thunder-step` was schema-only because it doesn't fit any single SpellMechanic kind: it's a teleport (like Misty Step) plus an area damage on the origin square plus an optional second teleportee. New `engine.plan.thunderStep` adds the planner, modeled on Misty Step's skeleton (action-economy + slot consume + range / occupancy / known-spell validation) and extended with: (a) optional `ally: { combatantId, to }` argument that enforces the RAW "willing creature within 5 ft" proximity gate before the teleport and emits a tail-end `CombatantMoved` for the ally; (b) a 10-ft Chebyshev AoE around the caster's origin that excludes the caster and the ally; (c) one shared 3d10 thunder damage pool (`+1d10` per slot above 3rd) rolled once and applied to every affected creature (halved on a successful CON save against the caster's spell save DC, full on failure). Caster's destination and the ally's destination must each be unoccupied and within 90 ft of the origin; the two must not collide. Tests cover happy path (declared / slot / action / per-target save+damage / two moves), AoE exclusions (caster + ally untouched, out-of-range bystander untouched), no-ally short form, range / proximity / coincident-destination / slot-level / scaling rejections, and a seed-walked save half / fail full pair. `spell-coverage.test.ts` retagged the skip from "not yet implemented" to "has dedicated planThunderStep."

*Monster statblock data folds into the effect stack* (slice 129). Closes the longest-standing latent gap surfaced by the monsters-batch-2 merge handoff: `MonsterStatblock.traits`, `damageResistances`, `damageImmunities`, `damageVulnerabilities`, and `conditionImmunities` were populated by content but unread anywhere in `src/derive/` or `src/engine/`. Skeleton's bludgeoning vulnerability and Young Red Dragon's fire immunity had been inert since alpha.5. The fix is a single new `collectMonsterEffects` helper in [src/derive/effect-stack.ts](src/derive/effect-stack.ts) that fires when `character.statblockId` resolves to a content-pack monster: it synthesizes `GrantResistance` / `GrantImmunity` / `GrantVulnerability` / `GrantConditionImmunity` effects from the four scalar arrays and appends `traits[]` (already `EffectSchema[]`) verbatim, then `buildEffectStack` walks them through `applyEffectToBuilder` with `source: 'monster'`. `mitigateDamage` and `isImmuneToCondition` both already consume the accumulator, so the fold lights up automatically across every existing consumer with zero downstream changes. Eight unit tests on the stack itself plus three end-to-end tests through `mitigateDamage` verify the actual damage outputs: Skeleton takes 20 from 10 bludgeoning (doubled), Young Red Dragon takes 0 from 30 fire (immune), Imp takes 5 from 10 nonmagical slashing but a full 10 from magical slashing (qualifier-aware). The remaining monster-data gaps documented for follow-up: `savingThrows` proficiencies in `computeSavingThrow`, and the Magic Resistance primitive (needs an "all saves" `SetAdvantage.on` variant plus an `event.isMagical` dispatcher fact).

*Monster `savingThrows` wires into `computeSavingThrow`* (slice 130). Direct follow-on to slice 129: monsters declare a `savingThrows: Record<string, number>` map of listed save bonuses (e.g. Young Red Dragon DEX +4 / CON +9 / WIS +4 / CHA +8), which per RAW MM presentation is the baked total (ability mod + proficiency already included). `computeSavingThrow` now consults `content.monsters.get(character.statblockId)?.savingThrows?.[ability]` first when the character resolves to a content-pack monster; when present, the listed bonus becomes a single `monster:<id>:save` breakdown entry replacing the ability-mod + class-proficiency reconstruction. Effects, exhaustion, and advantage still layer on top. PCs (no statblockId) and monsters without a listed save for the rolled ability fall through to the existing path unchanged. Six new tests cover Young Red Dragon's four listed saves, an unlisted STR save falling through, Mage's INT / WIS bonuses, the breakdown shape (single monster entry, no ability-mod or proficiency lines), Skeleton's pass-through (no savingThrows declared), and the PC pass-through. The fast-path is bounded to listed entries by design: the slice doesn't address the pre-existing fixture quirk where creature characters attach a fighter-L1 enrollment that incorrectly grants STR + CON proficiency for unlisted saves (that's a fixture / character-modeling gap, not a derivation gap).

*Magic Resistance primitive* (slice 131). RAW "the creature has advantage on saving throws against spells and other magical effects" is canonical across most CR 5+ Fiends / Fey / Dragons / spellcaster monsters; the Imp and Quasit shipped in monsters batch 1.7 were waiting on it. New marker `Effect` kind `GrantMagicResistance` (no params), `EffectAccumulator.markMagicResistance()` / `hasMagicResistance()`, and a `sourceIsMagical?: boolean` parameter on `ComputeSaveInput`. `computeSavingThrow` contributes advantage to the save when both flags are set; advantage / disadvantage cancellation against other sources still applies. `planCastSpell`'s save mechanic passes `sourceIsMagical: true` and now honors `hasAdvantage` / `hasDisadvantage` by rolling 2d20 take-max / take-min (pre-slice 131 it always rolled one d20, silently ignoring effect-stack save-advantage signals). Imp and Quasit traits gained `{ kind: 'GrantMagicResistance' }`. Tests: one accumulator unit, five new `computeSavingThrow` cases (Imp + sourceIsMagical → advantage, Imp + non-magical → no advantage, Imp + undefined → no advantage, PC + magical without trait → no advantage, Quasit + magical → advantage), and three cast-spell end-to-end cases (Fireball at Imp emits `used: 'advantage'` with two d20 rolls, Fireball at plain fighter stays single-d20 / `used: 'none'`, Fireball at mixed targets produces correct per-target shape). The other save planners (trap, recurring-save, reactive-spells, stunning-strike) don't yet pass `sourceIsMagical`; cast-spell is the canonical magical-save path and one-line follow-ups can extend the others as the content needs.

*Magic-item passive effects projection pass* (slice 132). The items lane named this gap as the single biggest blocker across ~20 wondrous items they authored in items batches 1.1 through 1.6. Pre-slice 132, `collectItemEffects` walked only `character.equipped.attuned`, so items that don't require attunement (Bag of Holding, Goggles of Night, Ring of Swimming, Bracers of Archery, Gloves of Swimming and Climbing, Stone of Good Luck, etc.) never reached the wearer's effect stack from inventory. The fix: extend the walk to iterate `character.inventory` too, gated on `def.requiresAttunement === false`, with a `Set` for dedup so an attuned item present in both lists doesn't double-fold. Items requiring attunement still only project via the existing attuned path. Canonical users wired in the same slice as the proof: Goggles of Night gains `{ kind: 'GrantSense', sense: 'darkvision', range: 60 }` and Ring of Swimming gains `{ kind: 'ModifySpeed', mode: 'swim', op: 'set', value: 40 }`. Both project from inventory without attunement, matching their RAW "you have darkvision while wearing these" and "your swim speed becomes 40 ft" shapes. Six new unit tests cover non-attunement projection, attunement-required gating, the existing attuned path (regression), dedup across inventory + attuned, non-magic items pass through, and the negative no-darkvision case for unattuned attunement-required items. The features-test snapshot moved (slice-126 design intent: 0 → 2 entries in `wiredIds`), validating the wire as a positive-signal event. The remaining ~18 wondrous items in the pack are now content-authoring follow-ups (the engine projection path is in place; what's left is wiring each item's `effects` array). Gaps doc updated to reclassify those items from "blocked on projection" to "content-authoring follow-up."

*sourceIsMagical rollout to non-cast-spell magical save planners* (slice 133). Slice 131 wired `sourceIsMagical` into `computeSavingThrow` plus the cast-spell save mechanic, but left three other magical-save planners untouched: `planTriggerTrap` (spell-armed traps: Glyph of Warding's Explosive Runes, Cordon of Arrows), `planTickRecurringSave` (recurring saves against spell-applied conditions: Hold Person's `held-paralyzed-active`, Hold Monster's `held-monster-active`, Hideous Laughter, Confusion, Bestow Curse's `cursed-inactive-active`), and `planSanctuaryWardSave` (Sanctuary's pre-attack WIS save). All three now pass `sourceIsMagical: true`. `planTriggerTrap` and `planTickRecurringSave` also gained 2d20-take-max / take-min support (they previously hard-coded single-d20 rolls and ignored `hasAdvantage` flags); `planSanctuaryWardSave` already honored advantage. Stunning Strike stays non-magical (Monk Discipline / ki is not a spell per RAW). Three end-to-end tests assert that an Imp triggering a glyph, an Imp saving against `held-paralyzed-active`, and an Imp attacking through Sanctuary all emit `SaveRolled` with `used: 'advantage'` and two d20 rolls.

*Cursed condition + Remove Curse planner* (slice 134). The Remove Curse spell shipped schema-only at alpha.5 because the engine had no way to identify which conditions are curses. Slice 134 adds a small fixed `category: 'curse' \| 'disease' \| 'poison'` enum to ConditionSchema (matching the 2024 PHB's three named removal interactions; future Lesser Restoration / Greater Restoration plug into the same taxonomy without new schema work). Bestow Curse's four `cursed-*-active` variants (ability-disadvantage, attack-disadvantage, inactive-turn, extra-damage) are all tagged `category: 'curse'`. New dedicated `planRemoveCurse` planner (3rd-level slot + action, touch range, no save / no roll): walks the touched target's appliedConditions and emits a `ConditionRemoved` per match, mirroring the RAW "any curse affecting that creature ends" semantics. `engine.plan.removeCurse` exposed on the public surface; spell-coverage retags the skip from "cursed condition itself not yet modeled" to "has dedicated planRemoveCurse." Seven tests cover single-curse strip, multi-curse strip (all four variants at once), non-curse condition pass-through (Bless + Prone untouched), no-curse target (slot still consumed, no removals), slotLevel < 3 rejection, unknown-spell rejection, and higher-level slot honor.

*Remote sensor primitive + Clairvoyance planner* (slice 135). Clairvoyance shipped schema-only because the engine had no entity for "an invisible sensor placed at a remote location that the caster perceives through." Slice 135 adds a `Sensor` runtime entity modeled on the slice-94 Trap shape: `state.sensors: Record<SensorId, Sensor>` where each sensor carries id + label + free-text location + casterId + sourceSpellId + sourceEffectInstanceId + sight/hearing mode. Three new event types (`RemoteSensorPlaced`, `RemoteSensorModeChanged`, `RemoteSensorRemoved`) drive the lifecycle; reducers in [src/engine/reducers/sensor.ts](src/engine/reducers/sensor.ts). New `planClairvoyance` (3rd-level slot + action + concentration + sensor place), `planSwitchSensorMode` (action + RemoteSensorModeChanged for the seeing/hearing toggle), and `planRemoveSensor` (voluntary spell end) all live in [src/engine/plan/sensor.ts](src/engine/plan/sensor.ts). `clearConcentrationEffect` extended to sweep sensors linked to a dropped concentration via `sourceEffectInstanceId`, mirroring the item-buff sweep added in slice 76. The engine doesn't model perception or remote position; `Sensor.location` is free-text consumer territory (the DM or VTT decides what's seen / heard through it). Eleven new tests cover the place / switch / remove lifecycle plus three structural cases: linked-EffectInstanceId tag on the sensor matches the ConcentrationStarted event, a second cast breaks prior concentration and the cleanup sweeps the old sensor, and switching to the same mode the sensor already has is rejected. Scrying composes the same primitive with the existing save mechanic; the target-WIS-save arm is a follow-up slice.

*Scrying planner composes the slice-135 sensor primitive with a target WIS save* (slice 136). RAW 2024 Scrying: 5th-level divination, 10-minute cast, Self / Concentration up to 10 minutes; the caster names a target and that creature makes a Wisdom save against the caster's spell save DC (adjusted by RAW's familiarity tier table). On failure a sensor appears near the target and follows it; on success the spell fails and the slot is still spent. New `planScrying` planner emits ActionEconomyConsumed (in encounter) → SpellCastDeclared → SpellSlotConsumed → SaveRolled (WIS, `sourceIsMagical: true` so Magic Resistance is honored). When the save fails, the events continue with ConcentrationBroken (if prior) → ConcentrationStarted → RemoteSensorPlaced; when the save succeeds the planner stops after the save event with `resisted: true` on the outcome. The familiarity-tier DC adjustment is an explicit `dcAdjustment?: number` field on the intent (RAW: -10 to +5 across five tiers); consumers / DMs supply the tier per their narrative. The engine doesn't track the "sensor follows target" position or the RAW 24-hour cooldown-per-target on a successful save; both are consumer territory. Seven tests cover the seed-walked fail-path full chain, the success-path fizzle (slot still spent, no concentration, no sensor), Imp Magic Resistance contributing advantage to the save, slot-level / unknown-spell rejection, the `dcAdjustment` actually shifting the SaveRolled.dc, and the consumer-display location on the placed sensor including the target name.

*Illusion-interaction primitive: Silent Image + Major Image + Investigate action* (slice 137). New `Illusion` runtime entity (`state.illusions: Record<IllusionId, Illusion>`) modeled on the slice-94 Trap / slice-135 Sensor pattern: id + label + free-text location + `kind: 'visual' \| 'audiovisual'` + casterId + sourceSpellId + sourceEffectInstanceId + a baked `investigationDC` (the caster's spell save DC at cast time) + `disbelievedBy: CharacterId[]` set. Three new event types (`IllusionCreated`, `IllusionInvestigated`, `IllusionDismissed`) drive the lifecycle; reducers in [src/engine/reducers/illusion.ts](src/engine/reducers/illusion.ts). Four planners in [src/engine/plan/illusion.ts](src/engine/plan/illusion.ts): `planSilentImage` (1st-level visual), `planMajorImage` (3rd-level audiovisual), `planInvestigateIllusion` (a creature spends an action to Study the illusion, rolls INT (Investigation) against the baked DC, success adds them to `disbelievedBy`), `planDismissIllusion` (caster ends spell voluntarily). `clearConcentrationEffect` sweeps illusions linked to a dropped concentration via `sourceEffectInstanceId`, mirroring the slice-135 sensor sweep. The engine surfaces the disbelief set but doesn't itself act on it; consumers / DMs decide what disbelieving creatures see and do. RAW edge cases not modeled this slice: the caster's bonus-action move (Silent Image) and action-recolour (Major Image) of the illusion's shape (a consumer can re-emit IllusionCreated with new label / location until a future schema extension lets the planner mutate the existing entity). Twelve tests cover both cast paths, both Investigate outcomes, the unknown-spell rejection, voluntary dismiss, concentration-drop cleanup, and the multi-creature belief state (two investigators on the same illusion track independently).

*SRD 5.2.1 duration-string normalization across 6 concentration spells* (slice 171). Audit of concentration-spell duration strings vs SRD format:

Six spells shipped with `duration: "Up to N minute"` (missing the `"Concentration, "` prefix that SRD 5.2.1 uses universally): Bless, Bane, Faerie Fire, Polymorph, Sleep, Spirit Guardians. All have `concentration: true` set correctly, so the engine treated them as concentration spells; only the human-readable string drifted from SRD format.

Fixed all 6 to `"Concentration, up to N minute(s)"`.

Engine ripple: the spell-duration parser at `src/internal/spell-duration.ts` only recognized `"up to N units"` and `"N units"` shapes. After the duration-string update, the parser saw `"concentration, up to N units"` and returned `undefined`, making the spells never expire. Updated the regex to optionally strip a leading `"concentration, "` prefix; the engine's concentration tracking still keys off the `concentration: true` flag, so the prefix is purely a display detail. Suite green.

17 spell drift fixes total across slices 167-171.

*SRD 5.2.1 content drift: 3 more spells (Chill Touch, Poison Spray, Black Tentacles)* (slice 170). Continued spell mining:

- **Chill Touch**: range `"120 feet"` → `"Touch"`; mechanicalEffects gained `attackKind: "melee"`; damage `1d8 necrotic` → `1d10 necrotic`; cantripScalingDice `1d8` → `1d10`. The 2024 PHB rebuilt Chill Touch from a ranged necrotic ray to a melee spell attack with a heavier damage die.
- **Poison Spray**: `kind: save` (CON) → `kind: attack` (ranged spell attack). 2024 PHB switched it from a save-or-take to a ranged spell attack. Damage 1d12 poison + cantrip scaling unchanged.
- **Black Tentacles**: `saveAbility: DEX` → `STR`. 2024 SRD: Strength save (was Dexterity in 2014). Damage / restrained-on-fail / area unchanged.

Test ripple: `spell-coverage.test.ts` `'poison-spray': { kind: 'save' }` → `{ kind: 'attack' }`. Suite green (1452 passed, 197 skipped).

14 spell drift fixes shipped across slices 167-170.

*SRD 5.2.1 content drift: 3 more spells (Hypnotic Pattern, Inflict Wounds, False Life)* (slice 169). Continued spell mechanicalEffects audit:

- **Hypnotic Pattern**: `conditionOnFail: incapacitated` → `charmed`. Per SRD 5.2.1, the primary condition is Charmed (with Incapacitated + Speed 0 as riders that the pack's single-condition schema doesn't model). The pack was applying just Incapacitated, missing the Charmed primary.
- **Inflict Wounds**: `kind: attack` → `kind: save` (CON, halfOnSuccess true). 2024 PHB changed Inflict Wounds from a melee spell attack to a Constitution save (still 2d10 necrotic, +1d10 per slot, half on success). Significant mechanical shift — the spell no longer hits/misses on an attack roll.
- **False Life**: `amountDice: "1d4"` → `"2d4"` (base temp HP) + duration `"1 hour"` → `"Instantaneous"`. 2024 PHB doubled the base dice; SRD also makes the duration instantaneous (the temp HP simply doesn't expire by spell duration; the engine drops temp HP normally on long rest).

Test ripple:
- `tests/unit/engine/spell-coverage.test.ts`: `'inflict-wounds': { kind: 'attack' }` → `{ kind: 'save' }`.
- `tests/unit/engine/plan-death-ward.test.ts`: the seed-search loop's "any damage" threshold was changed from "any positive damage" to "≥ 7 damage" because the 2024 save-based Inflict Wounds can produce smaller (half-damage) rolls that don't trigger the Death Ward intercept; the test now finds a failed-save roll specifically.

Continuing the mining: at this point ~11 spell drift fixes shipped across slices 167-169. Diminishing returns ahead; the remaining drift surface is mostly mechanic-shape rewrites (Sleep, Color Spray, Power Word Stun) that need new mechanicalEffect kinds rather than simple field swaps. Suite green (1452 passed, 197 skipped).

*SRD 5.2.1 content drift: 3 more spells refreshed* (slice 168). Continued spell mechanicalEffects audit:

- **Phantasmal Killer**: `halfOnSuccess: false` → `true`. Per SRD 5.2.1: "On a successful save, the target takes half as much damage, and the spell ends." Pack was using the 2014 shape where save = no damage.
- **Blade Barrier**: damageType `slashing` → `force`. Per SRD 5.2.1 Blade Barrier deals Force damage (not slashing as the 2014 version specified).
- **Web**: duration string `"Up to 1 hour"` → `"Concentration, up to 1 hour"`. The `concentration: true` flag was already correct; the display string was missing the prefix.

No test ripple (these fields aren't asserted in tests). Suite green (1452 passed, 197 skipped).

*SRD 5.2.1 content drift: 6 spells refreshed to 2024 mechanics* (slice 167). Audit pass on spell `mechanicalEffects` arrays surfaced 2014→2024 drift on several heavily-used spells:

- **Healing Word**: base 1d4 → 2d4, per-slot scaling 1d4 → 2d4 (2024 PHB doubled the base healing).
- **Mass Healing Word**: base 1d4 → 2d4 (per SRD 5.2.1).
- **Mass Cure Wounds**: base 3d8 → 5d8.
- **Ice Storm**: bludgeoning 2d8 → 2d10, per-slot scaling +1d8 → +1d10. The SRD 5.2.1 also adds 4d6 cold (not modeled by the single-damage-type spell encoding; documented gap).
- **Wall of Fire**: added missing `extraDicePerSlotLevel: 1` (SRD 5.2.1 scales +1d8 per slot above 4).
- **Spiritual Weapon**: duration `1 minute` → `Concentration, up to 1 minute`; concentration `false` → `true`; added missing `extraDicePerSlotLevel: 1`. The 2024 PHB made this a concentration spell (a significant playstyle change from 2014 where it stacked with other concentration buffs).

Test ripple:
- `plan-cast-spell-disciple-of-life.test.ts`: heal-range assertions updated to reflect the new Healing Word base (2d4 not 1d4); test ranges shifted from 4-7 / 7-10 to 5-11 / 8-14.
- Showcase transcript auto-refreshed: Mass Healing Word healing output changes downstream.

What else the audit surfaced but didn't fix in this slice:
- **Sleep** (2014 PHB `hp-pool-knockout 5d8` → 2024 SRD `WIS save vs Incapacitated, recurring save, Unconscious on second failure`). Substantial mechanical rewrite requiring a new `mechanicalEffect` kind or composite encoding; not a simple field swap. Tracked for a future content slice.
- **Ice Storm's secondary 4d6 cold damage** (not modeled by single-type spell encoding).

Suite green (1452 passed, 197 skipped).

*SRD 5.2.1 content drift: Young Red Dragon Rend attack* (slice 166). Replaces the 2014-style separate Bite (2d10 piercing) + Claw (2d6 slashing) weapon items with a single Rend (2d6 slashing) per SRD 5.2.1's Multiattack pattern (`The dragon makes three Rend attacks` instead of `bite + claw + claw`).

Pack changes:
- Removed `dragon-bite` and `dragon-claw` weapon items (2014 carryovers).
- Added `young-red-dragon-rend` weapon item (2d6 slashing, matching the Young Red Dragon SRD 5.2.1 entry).

Test changes:
- `tests/golden/showcase.test.ts` Stoneheart dragon's multiattack switched from `{ bite x 1, claws x 2 }` to `{ rend x 3 }`.
- Showcase transcript auto-refreshed: the Item Acquired line consolidates, attack rolls and damage formulas update to slashing 2d6, and the downstream HP echoes shift accordingly.

What's still deferred:
- The Rend attack's fire damage rider (1d6 fire per SRD: "Hit: 13 (2d6 + 6) Slashing damage plus 3 (1d6) Fire damage"). Pack weapon schema models a single damage type per weapon; the elemental rider would need an `extraDamage` field or per-monster action shape. Same gap as the old bite (2014 PHB Young Red Dragon bite also had a 1d6 fire rider that the pack never modeled).
- The other 9 chromatic dragons (4 Young + 5 Adult variants) also use Rend in SRD 5.2.1 with their own damage dice (Young White 2d4+4, Young Black 2d6+4, Adult Red 1d10+8, etc.). Each would need its own Rend weapon variant if consumers want a wired multiattack. Pack-level dragon multiattack stays `null` (consumers configure attacks locally, same as in showcase).

Suite green (1452 passed, 197 skipped).

*Test fixture audit: refresh stale fixture data to SRD 5.2.1* (slice 165). Audit pass over `tests/fixtures/` and `tests/golden/showcase.test.ts` looking for hardcoded SRD-content values that drifted from the post-slice-141-163 pack data. Findings + fixes:

- `tests/fixtures/index.ts` `buildOgre` HP default: 59 → 68 (2014 → SRD 5.2.1).
- `tests/golden/showcase.test.ts` Ogre fixture HP: 59 → 68 (line 1012).
- `tests/golden/showcase.test.ts` Giant Ape polymorph form: HP 157 → 168, INT 7 → 5 (matches SRD 5.2.1 animals.md).
- `tests/golden/showcase.test.ts` Goblin Scout DEX: 14 → 15 (matches SRD 5.2.1 Goblin Warrior).
- `tests/unit/rules-truth.test.ts` Paladin spellcasting comment: corrected to reflect that SRD 5.2.1 grants Paladin Spellcasting at L1 (engine math via the half-caster `Math.ceil(level/2)` formula already produces the correct L1 + L2 slot counts; the old comment claimed L2-onset which was incorrect). Added a positive assertion for Paladin L1 having 2 first-level slots.

Two golden transcripts auto-refreshed (s13-creature, showcase): the only changes are the Ogre HP transition (59 → 68) and downstream HP-remaining echoes in the showcase narrative.

What the wider audit also surfaced but didn't fix in this slice (content-authoring follow-ups, not test bugs):
- The pack ships `dragon-bite` (2d10 piercing) and `dragon-claw` (2d6 slashing) as separate weapon items for Young Red Dragon. SRD 5.2.1 simplified this to a single `Rend` attack (2d6 + STR slashing + 1d6 fire) repeated by Multiattack. Updating the pack to match would require a new weapon item + the dragon's multiattack pattern. Tracked for a future content slice.

Most "test drift" was caught and fixed during the slice 141-163 data-refresh campaign (Young Red Dragon save profile in slice 157, Specter / Imp resistance in slices 162-163). This slice is the cleanup pass on fixtures + showcase that didn't get touched by the data refreshes (their assertions are HP / AC / ability values, not save / resistance, so they didn't break when the data shifted). Suite green (1452 passed, 197 skipped).

*SRD 5.2.1 compliance: secondary monster fields initiative complete (doc refresh)* (slice 164). Closing slice of the secondary-fields fix campaign (slices 154 to 163). Updates [docs/srd-5.2.1-audit-monsters-secondary.md](docs/srd-5.2.1-audit-monsters-secondary.md) to mark every fix slice done and document the final state (110 / 111 monsters match SRD 5.2.1 on all audited fields). Updates [docs/content-attribution.md](docs/content-attribution.md) to note the secondary audit is complete (replacing the slice-154 "audit only" caveat). Documents the audit's scope and what it didn't cover (action / trait text bodies; non-`GrantResistance` trait shapes).

Cumulative monster audit work in this initiative (slices 141 to 164): 111 entries, primary + secondary fields all verified against SRD 5.2.1. The only outstanding diff is Young White Dragon's INT save SRD source ambiguity. Doc-only slice; suite green.

*SRD 5.2.1 compliance: remove obsolete nonmagical B/P/S traits from 23 monsters* (slice 163). Cleanup follow-up to slice 162. The pack carried 2014-era `traits: [{kind: 'GrantResistance', damageType: B/P/S, qualifier: 'nonmagical'}]` entries on 23 monsters. SRD 5.2.1 reorganized these creatures' damage profiles:

- For 8 monsters (Specter, Ghost, Wraith, Air Elemental, Fire Elemental, Invisible Stalker, Couatl, Chain Devil) the 2024 SRD lists B/P/S as **unqualified** resistance. Slice 162 added the unqualified entries to `damageResistances`; the obsolete nonmagical traits were functionally redundant.
- For the other 15 monsters (Shadow, Wight, Lemure, Imp, Quasit, Earth Elemental, Water Elemental, Stone Golem, Iron Golem, Deva, Planetar, Solar, Bearded Devil, Barbed Devil, Bone Devil), SRD 5.2.1 **dropped B/P/S resistance entirely**. The pack's nonmagical traits were over-resisting per the 2024 update.

Slice 163 surgically removes the 69 nonmagical-trait lines (3 per monster). Empty `traits` arrays remain as `[]`. The slice-112 nonmagical-qualifier primitive itself is preserved for content that still uses it (notably the Stoneskin spell's `stoneskin-active` condition, which the 2024 PHB technically simplifies but the pack keeps SRD-5.2-shape to exercise the engine primitive).

Two engine tests updated: `tests/unit/derive/monster-effect-stack.test.ts` had `Imp: traits[] from batch 1.7 fold` and `Imp halves nonmagical slashing damage` assertions; both rewritten to reflect the SRD 5.2.1 Imp profile (no B/P/S resistance, Cold resistance only). Suite green.

This is a real behavior change: pre-slice-163, the engine applied nonmagical B/P/S resistance to all 23 monsters. Post-slice-163, only the 8 with unqualified entries in damageResistances do (and they resist all sources). Consumers running pre-2024-style campaigns can vendor a pack that restores the 2014 traits.

Cumulative secondary-fields audit progress: 110/111 match (1 outstanding diff is the Young White Dragon INT-save SRD-source-typo).

*SRD 5.2.1 compliance: damage resistance / immunity / vulnerability + condition immunity refresh on 23 monsters* (slice 162). Seventh and last fix-slice from the slice 154 secondary audit. Refreshes the four defensive arrays on 23 monsters to match SRD 5.2.1.

Notable patterns:

- **Incorporeal undead gain unqualified B/P/S resistance.** SRD 5.2.1 simplified Specter / Ghost / Wraith: 2014 had B/P/S resistance "from nonmagical attacks"; 2024 SRD drops the qualifier so the resistance covers all B/P/S sources. Pack now lists B/P/S in damageResistances directly (the slice-112 GrantResistance nonmagical traits remain as a redundant secondary fold that produces the same observed behavior; trait cleanup is a follow-up).
- **Elementals gain unqualified B/P/S resistance.** Air / Fire / Water Elemental + Invisible Stalker.
- **Couatl gains unqualified B/P/S** resistance + radiant immunity (slice-112 nonmagical traits kept).
- **Solar's damage profile flips.** Drops radiant resistance + necrotic immunity (per 2024 SRD); adds radiant immunity.
- **Condition immunity catalog adjustments:** Knight gains frightened immunity. Zombie gains exhaustion immunity. Shadow gains unconscious. Specter / Ghost / Wraith got their condition immunity sets reshuffled. Several Animated Objects + Oozes gained `grappled / restrained` + dropped `blinded`. Shield Guardian gains `petrified`. Animated Rug of Smothering loses slashing vulnerability (SRD doesn't list it).
- **Adds where pack had empty arrays:** Fire Elemental gets B/P/S resistance, Invisible Stalker gets B/P/S resistance, Gelatinous Cube gets acid immunity.

One test (`Specter traits[] GrantResistance` qualified-nonmagical assertion in `monster-effect-stack.test.ts`) updated: the test was written for the 2014 nonmagical-only behavior; SRD 5.2.1's unqualified resistance means `hasResistance('bludgeoning', true)` is now true. Comment in the test explains the SRD change.

Audit re-run: 0 damage/cond diffs remaining (was 28). Drift drops from 24 to 1; match rises from 87 to 110. The single remaining diff is the Young White Dragon INT-save SRD source typo (pack intentionally omits a save proficiency whose SRD column shows ambiguous "2" without sign). Suite green.

Known follow-up: 23 monsters still ship redundant `traits: [GrantResistance qualifier: nonmagical]` entries for B/P/S that the SRD 5.2.1 update made unqualified. Functionally inert (the array-level entry covers all cases); cleanup is a separate slice. The 2014-style nonmagical-only resistance on creatures whose SRD 5.2.1 simplified to unqualified is gone from observable behavior because the array-level entry now grants the broader resistance.

*SRD 5.2.1 compliance: speed refresh on 17 monsters* (slice 161). Sixth fix-slice from the slice 154 secondary audit. Refreshes the speed map on 17 monsters where pack values drifted from SRD 5.2.1.

Notable patterns: incorporeal undead got walk speeds in 2024 SRD where 2014 had walk 0 (Specter walk 30, Ghost walk 5, Wraith walk 5; all retain fly + hover). Air Elemental walk 0 to 10. Animated Flying Sword walk 0 to 5. Big monsters got bigger movement: Mammoth walk 40 to 50, Shambling Mound walk 20 to 30, Ochre Jelly walk 10 to 20 + climb 10 to 20, Gibbering Mouther walk 10 to 20 + swim 10 to 20. Several monsters gained climb speeds (Spy, Giant Rat, Owlbear 40, Barbed Devil 30). Cloud Giant gained fly 20 (hover); Storm Giant gained fly 25 (hover). Lemure walk 15 to 20. Black Bear walk 40 to 30 + new swim 30 + climb 30.

Audit re-run: 0 speed diffs remaining (was 20). Drift drops from 34 to 24; match rises from 77 to 87. Suite green. One Young White Dragon INT-save diff reappears in the audit summary: SRD's INT save column shows "2" without sign while mod is "-2"; treating as SRD source typo, pack correctly omits INT proficiency.

*Test perf: reduce property-test NUM_RUNS default from 1000 to 50* (slice 160). Property tests under `tests/property/` were running 1000 fast-check iterations per property by default (content-pack-validator was 200). At 1000, the combat-sequence file alone took ~108 seconds; the full suite took ~115 seconds wall.

Per [CLAUDE.md](../CLAUDE.md) testing guidance, property tests at fixed-iteration CI gates "aren't required" and should be cut as ceremony; the safety value comes from the invariant assertions themselves, which 50 iterations exercise just as faithfully (each iteration sequences 25+ random actions through the engine, so 50 iterations × 25 actions = 1250 distinct action-property checks per property).

Updated NUM_RUNS default to 50 in all 7 property test files (combat-sequence, content-pack-validator, derivations, multiclass, plan, reducers, spell-cast-sequence). The `FAST_CHECK_NUM_RUNS` env var still works for deep local fuzz runs (e.g. `FAST_CHECK_NUM_RUNS=1000 npx vitest run tests/property/`).

Measured: full suite wall time drops from ~115s to ~45s. Combat-sequence file from 108s to 2s. Content-pack-validator from 96s to 15s. Suite still green (1451 passed, 197 skipped).

*SRD 5.2.1 compliance: senses refresh on 81 monsters* (slice 159). Fifth fix-slice from the slice 154 secondary audit. Refreshes the senses map on 81 monsters where pack omitted or drifted from SRD 5.2.1.

Pattern: many low-CR monsters were missing the `passivePerceptionOverride` entirely; this slice adds the SRD-listed value for every monster that lists one. Several Beast and Humanoid entries gained the SRD-listed `darkvision: 60` they were missing (Goblin Warrior, Skeleton, Ogre, Wolf, Brown Bear, Dire Wolf, Mastiff, Black Bear, etc.). Higher-CR entries get richer sense profiles: Storm Giant adds darkvision 120 + truesight 30 + corrects PP from 19 to 20; Giant Spider's incorrect blindsight 10 dropped to match SRD's darkvision-only profile.

Target maps extracted from SRD markdown via script (same pattern as slices 157 and 158). The senses field order in JSON normalized to blindsight / darkvision / tremorsense / truesight / passivePerceptionOverride for consistency.

Audit re-run: 0 senses diffs remaining (was 93). Drift drops from 85 to 34; match rises from 26 to 77. Suite green.

*SRD 5.2.1 compliance: skill proficiency refresh on 28 monsters* (slice 158). Fourth fix-slice from the slice 154 secondary audit. Refreshes the skills map on 28 monsters where pack values drifted from SRD 5.2.1. Target maps extracted from the SRD markdown via script (same pattern as slice 157).

Pattern: most diffs are individual skill-value bumps where the 2024 SRD raised the bonus (e.g. Wolf perception +3 to +5, Owlbear perception +3 to +5, Black Bear perception +3 to +5, Cloud Giant perception +7 to +11). A few monsters got skill additions (Goblin Warrior gains Stealth +6; Giant Spider gains Perception +4; Stone Giant gains Athletics +12 and Stealth +5). Tough lost its sole skill entry (intimidation +2; SRD 5.2.1's Tough has no listed skill proficiencies).

Audit re-run: 0 skill diffs remaining (was 39). Drift count drops from 89 to 85; match count rises from 22 to 26. Full suite green.

*SRD 5.2.1 compliance: saving throw proficiency refresh on 28 monsters* (slice 157). Refreshes the savingThrows map on 28 monsters where the pack carried 2014 MM proficiency profiles instead of SRD 5.2.1. Audit at [docs/srd-5.2.1-audit-monsters-secondary.md](docs/srd-5.2.1-audit-monsters-secondary.md).

Pattern: 2024 SRD significantly narrowed dragon save proficiencies (every chromatic dragon dropped CON + CHA proficiency, keeping only DEX + WIS) and adjusted save profiles on several other CR 8+ monsters. Target maps were script-extracted directly from the SRD markdown tables (only saves where save bonus differs from ability modifier, indicating proficiency added).

Notable changes:
- All 10 chromatic dragons (Young White / Black / Green / Blue / Red + Adult White / Black / Green / Blue / Red): drop CON and CHA save proficiency.
- Solar: drop all save proficiencies (SRD lists none; pack had INT / WIS / CHA).
- Aboleth: add DEX +3.
- Cloud Giant: drop CHA (kept CON + WIS).
- Bone Devil: add INT, WIS, CHA proficiencies (pack had only no entries; SRD has STR + INT + WIS + CHA).
- Bearded Devil: drop WIS, add CHA.
- Mammoth, Tyrannosaurus Rex, Gladiator, Pegasus, Planetar: add multi-save proficiency profiles.
- Smaller swaps on Couatl, Chain Devil, Cultist, Cultist Fanatic, Giant Rat, Mastiff, Storm Giant, Assassin.

Two save derivation tests in tests/unit/derive/save.test.ts updated to reflect the new SRD-compliant Young Red Dragon save profile (was DEX +4 / CON +9 / WIS +4 / CHA +8, now DEX +4 / WIS +4). Audit re-run: 0 save diffs remaining (was 45); drift count drops from 101 to 89 monsters with any secondary-field diff. Full suite green.

*SRD 5.2.1 compliance: type / size / alignment fixes for 3 monsters* (slice 156). Second small-fix slice from the slice 154 secondary audit. Three monsters had type/size/alignment drift between 2014 MM and SRD 5.2.1:

- **Goblin Warrior**: type Humanoid → Fey + new subtype "goblinoid" + alignment {neutral, evil} → {chaotic, neutral}. The 2024 SRD reclassified goblins as Fey (Goblinoid) with a Chaotic Neutral default alignment, a significant lore shift from 2014.
- **Stirge**: type Beast → Monstrosity. 2024 SRD reclassification.
- **Invisible Stalker**: size Medium → Large. 2024 SRD upsized it.

Three small JSON edits; no engine / test changes needed. Suite green. Slice 154 audit's type drift (2 entries) and size drift (1 entry) cleared. About 270 remaining secondary-field diffs queued for slices 157 to 164 (saves / skills / senses / speed / damage R/I/V / condition immunities).

*SRD 5.2.1 compliance: fix passivePerception schema-rename bugs* (slice 155). Trivial fix from the slice 154 audit. Two pack monsters used `senses.passivePerception` instead of the schema-declared `senses.passivePerceptionOverride`, so Zod silently dropped the override value and the engine fell back to derivation. Renamed both: Wolf (PP 13) and Young Red Dragon (PP 18). Now Young Red Dragon matches the SRD's PP 18 exactly; Wolf still has value-drift (pack 13 vs SRD 15) because the underlying perception skill drifts from +3 to +5, a separate fix in the skill-refresh slice. Full suite green.

*SRD 5.2.1 compliance: secondary monster fields audit* (slice 154). Follow-up to the slice 141 to 149 primary monster initiative. Audit at [docs/srd-5.2.1-audit-monsters-secondary.md](docs/srd-5.2.1-audit-monsters-secondary.md).

The primary audit script verified AC + HP + abilities + CR; this slice extends to size, type, alignment, speed (walk / swim / fly / climb / burrow), saving throw proficiencies, skill proficiencies, senses (darkvision / blindsight / tremorsense / truesight / passive perception override), damage resistances + immunities + vulnerabilities, condition immunities. Action / trait text bodies remain not covered (would need narrative-text parsing).

Results: 10 of 111 monsters clean on secondary fields (Allosaurus, Deva, Fire Giant, Frost Giant, Griffon, Hill Giant, Plesiosaurus, Pteranodon, Satyr, Sprite); 101 have at least one diff. About 270 individual field diffs split as 94 senses (mostly passive perception derivation), 45 saving throw proficiency, 39 skill proficiency, 20 speed (climb / fly / swim / walk), 12 condition immunity, 10 damage resistance, 5 damage immunity, 2 type (Goblin Warrior 2024 reclassified to Fey + Stirge to Monstrosity), 1 size (Invisible Stalker Medium to Large), 1 damage vulnerability.

Two field-name bugs surfaced: Wolf and Young Red Dragon use `senses.passivePerception` instead of the schema-declared `senses.passivePerceptionOverride`; Zod silently drops the unknown key, so those overrides currently don't reach the engine.

Fix surface (~270 field-level edits across 101 monsters) is too large for a single reviewable slice; remediation is queued in slices 155 through 164: field-name bugs (155), type / size fixes (156), saving throws (157 to 159), skills + senses (160 to 161), speed (162), damage R / I / V + condition immunities (163), doc refresh (164). Suite green; no JSON / test / engine changes this slice (audit-only).

*SRD 5.2.1 compliance: classes + subclasses + class features audit* (slice 153). Fifth audit in the SRD 5.2.1 standardization series. Documentation-only slice; no pack content changes. Audit at [docs/srd-5.2.1-audit-classes.md](docs/srd-5.2.1-audit-classes.md).

Layer-by-layer findings:

- **Classes (12 / 12 clean):** all 12 pack class names match SRD 5.2.1 exactly.
- **Subclasses (12 / 12 clean):** all 12 pack subclass names match the single SRD-listed subclass per class (Path of the Berserker, College of Lore, Life Domain, Circle of the Land, Champion, Warrior of the Open Hand, Oath of Devotion, Hunter, Thief, Draconic Sorcery, Fiend Patron, Evoker).
- **Main-class features:** 13 level-placement drift entries (e.g. Fighter Tactical Mind/Shift/Master each off by one tier; Rogue Reliable Talent at pack L11 vs SRD L7; Cleric Channel Divinity at pack L1 vs SRD L2; Wizard subclass Evoker's Sculpt Spells at pack L3 vs SRD L6). ~17 SRD-listed features genuinely missing from pack (Bard Words of Creation L20, Cleric Divine Order L1 + Greater Divine Intervention L20, Druid Primal Order L1, Monk Heightened Focus / Self-Restoration / Disciplined Survivor / Superior Defense, Paladin's Smite L2 + Aura Expansion L18, Ranger Expertise L9 + Precise Hunter L17, Rogue Uncanny Dodge L5 + Elusive L18, Sorcerer Sorcery Incarnate L7, Warlock Contact Patron L9).
- **Subclass features (biggest gap):** 41 SRD-listed subclass features missing from pack. Most subclasses ship only L3 features; SRD's L6 / L10 / L14 / L17 / L18 / L20 grants are not yet authored (e.g. Champion's L7 Additional Fighting Style / L10 Heroic Warrior / L15 Superior Critical / L18 Survivor; Life Domain's L3 Preserve Life / L6 Blessed Healer / L17 Supreme Healing; Oath of Devotion's L7 Aura of Devotion / L15 Smite of Protection / L20 Holy Nimbus).
- **Pack-only PHB 2014-flavored entries (5):** Cleric Improved Divine Intervention (L20), Monk Empty Body (L18) + Step of the Wind: Heightened Mobility (L7), Sorcerer Sorcery Points at L20, Warlock Pact Boon (L3). These are 2014 PHB feature names the 2024 PHB restructured. Kept under the slice-152 hybrid policy.

Several SRD "features" listed at every class (Spellcasting L1, `<Class>` Subclass L3, Ability Score Improvement L4, Epic Boon L19, Weapon Mastery L1 for martial classes) are handled by the pack via schema fields (spellcastingAbility / spellSlotTable, the subclasses[] array, the level-up planner, featsTaken, per-weapon mastery properties) rather than `levelTable.features` entries; the audit script doesn't flag these as missing.

Fixes are content-authoring follow-ups: a per-class slice fixing the 13 level-placement bugs + a multi-slice campaign authoring the 41 missing subclass features. README + content-attribution.md updated. No JSON / test / engine changes; suite green (1451 passed, 197 skipped).

*SRD 5.2.1 compliance: character-creation audit (species + backgrounds + feats), documented hybrid* (slice 152). Fourth audit in the SRD 5.2.1 standardization series. Documentation-only slice; no pack content changes. Audit at [docs/srd-5.2.1-audit-character-creation.md](docs/srd-5.2.1-audit-character-creation.md).

Findings: SRD 5.2.1 ships much narrower character-creation content than PHB 2024 (4 backgrounds, 17 feats, 9 species). Pack ships 19 backgrounds + 33 feats + 7 species. All 7 pack species are SRD-derived (Goliath + Orc are SRD-listed but not yet in pack). Backgrounds split 4 SRD (Acolyte, Criminal, Sage, Soldier) + 15 PHB 2024-only (Artisan, Charlatan, Entertainer, Farmer, Folk Hero, Guard, Guide, Guild Artisan, Hermit, Merchant, Noble, Outlander, Sailor, Scribe, Wayfarer). Feats split 16 SRD-derived (Alert, Magic Initiate x 3 variants, Savage Attacker, Skilled, the 4 SRD-listed Fighting Styles, 6 of 9 Epic Boons) + 17 PHB 2024-only.

Policy: keep all PHB 2024-only entries in the pack because character creation breadth needs them; document the hybrid in content-attribution.md and the dedicated audit doc rather than deleting. Downstream consumers building strict SRD-only forks can vendor a subset using the audit doc's named lists. Same shape as the slice-151 "wired non-SRD spells" decision, but applied at the per-category level: backgrounds and feats categorize as foundational, so the PHB 2024 extras stay rather than drop. README + content-attribution.md updated to reflect the new SRD subset / hybrid framing; no test or pack JSON changes; suite green.

*SRD 5.2.1 compliance: spell audit + drop 64 schema-only non-SRD + rename 15 named-mage variants* (slice 151). Audit at [docs/srd-5.2.1-audit-spells.md](docs/srd-5.2.1-audit-spells.md). Of the 400 pack spells: 309 exact-matched SRD 5.2.1 by name, 15 needed renames to drop the 2014 wizard-name prefix (Bigby's Hand to Arcane Hand, Drawmij's Instant Summons to Instant Summons, Evard's Black Tentacles to Black Tentacles, Leomund's Tiny Hut to Tiny Hut, Melf's Acid Arrow to Acid Arrow, Mordenkainen's Faithful Hound / Magnificent Mansion / Private Sanctum / Sword to Faithful Hound / Magnificent Mansion / Private Sanctum / Arcane Sword, Nystul's Magic Aura to Arcanist's Magic Aura, Otiluke's Resilient Sphere to Resilient Sphere, Otto's Irresistible Dance to Irresistible Dance, Rary's Telepathic Bond to Telepathic Bond, Tasha's Hideous Laughter to Hideous Laughter), and 76 were not in SRD 5.2.1.

Per the policy chosen for this initiative (option 3: keep wired, drop schema-only), 12 of those 76 stay because the engine has substantial investment (dedicated planner files or test files outside spell-coverage.test.ts): Absorb Elements (reactive-spells.ts planner + tests), Armor of Agathys, Blade Ward, Cause Fear, Cloud of Daggers, Cordon of Arrows, Crusader's Mantle, Elemental Weapon (dedicated planner), Hunger of Hadar, Spirit Shroud, Summon Beast, Thunder Step (movement.ts planner). The other 64 (schema-only XGE/TCE additions consumed only by the generic planCastSpell + spell-coverage iteration) get dropped: arcane-gate, aura-of-purity, aura-of-vitality, blinding-smite, branding-smite, circle-of-power, compelled-duel, crown-of-madness, crown-of-stars, destructive-wave, dream-of-the-blue-veil, dust-devil, earth-tremor, earthbind, elemental-bane, far-step, feeblemind, feign-death, friends, frostbite, hail-of-thorns, holy-weapon, investiture-of-X (4 variants), invulnerability, lightning-arrow, maddening-darkness, mass-polymorph, mind-sliver, mold-earth, power-word-pain, psychic-scream, ravenous-void, skywrite, soul-cage, steel-wind-strike, summon-X (12 variants), swift-quiver, synaptic-static, tashas-otherworldly-guise, telepathy, tensers-transformation, thorn-whip, thunderclap, thunderous-smite, time-ravage, toll-the-dead, wall-of-light, watery-sphere, whirlwind, word-of-radiance, wrathful-smite.

Spell count drops from 400 to 336 (324 SRD 5.2.1-derived + 12 wired-non-SRD extras documented in content-attribution.md). spell-coverage.test.ts SPELL_EXPECTATIONS table updated: 64 dropped entries + 10 id renames (5 of the 15 renames are name-only, no id change). README + starter-pack-gaps.md + content-attribution.md count and description updates also land. SRD spell coverage: 324 of 340 SRD 5.2.1 spells in pack (95%); the missing 16 (Animated Object, Antilife Shell, Befuddlement, Blink, Divine Smite, Elementalism, Floating Disk, Freezing Sphere, Ice Knife, Illusory Script, Mind Spike, Shining Smite, Sorcerous Burst, Summon Dragon, Transport via Plants, Vitriolic Sphere) are content-authoring follow-ups. Full suite green (1451 tests, 197 skipped; -64 vs pre-slice from removed coverage iterations).

*SRD 5.2.1 compliance: magic-item audit + drop 16 non-SRD entries* (slice 150). Follows the slice 141 to 149 monster audit with the same scripted pattern applied to magic items. Audit at [docs/srd-5.2.1-audit-items.md](docs/srd-5.2.1-audit-items.md). Of the 138 pack magic items: 98 are exact name matches against SRD 5.2.1, 21 are granular variants of grouped SRD entries (Armor / Weapon +1/+2/+3, Belt of X Giant Strength × 6, Ioun Stone variants × 8, Quaal's Feather Token Anchor), 3 are slight SRD name variants (Stone of Good Luck, Flametongue Longsword, Amulet of Proof Against Detection and Location with capitalization diff), and 16 are not in SRD 5.2.1 and get dropped this slice: Cap of Water Breathing, Cloak of Billowing, Cloak of Many Fashions, Clothes of Mending, Deck of Many Things (legendary alpha.5 seed; only ever shipped as a name with no card text), Driftglobe, Ear Horn of Hearing, Hat of Vermin, Mystery Key, Pole of Angling, Rod of the Pact Keeper +1, Shield of Expression, Smoldering Armor, Staff of the Adder, Tankard of Sobriety, Wand of Pyrotechnics. Magic-item count drops from 138 to 122; charges-bearing count drops from 31 to 27 (Driftglobe, Rod of the Pact Keeper +1, Staff of the Adder, Wand of Pyrotechnics all carried charges). features.test.ts snapshot refreshed accordingly (only those four item ids leave `withChargesIds`; everything else is unchanged). README + starter-pack-gaps.md count + description updates also land. Full suite green (1480 tests). Rarity / attunement / effect-array drift checks across the surviving 122 entries are a separate follow-up audit.

*SRD 5.2.1 compliance: doc refresh (initiative complete)* (slice 149). Closing slice of the SRD 5.2.1 standardization initiative (slices 141 to 149). Three docs refreshed to reflect the post-audit reality:

- [docs/content-attribution.md](docs/content-attribution.md) rewritten. The pre-audit doc audited only the original 5-monster seed; the rewrite reflects the 111-monster cohort. New SRD 5.2.1 standardization section documents the slice 141 drop + slice 142 rename + slices 143 to 148 stat-drift refresh. Categories are now grouped as "confidently SRD-derived" (monsters: fully audited; conditions; equipment; backgrounds; species), "not yet individually audited" (spells, magic items, classes, feats; same-category audit pattern is available but not yet applied), and "not in SRD 5.2.1" (Bastions, Epic Boons, Deck of Many Things card text).

- [README.md](README.md) updated. The SRD-compliance headline bumped from ~60% to ~65% (monster cohort now confidently SRD 5.2.1 instead of mixed 2014 / 2024 / non-SRD). Monster row gets a 🟢 status indicator (was 🟡) reflecting the audit verification. Slice-43 historical bullet annotated to note Orc dropped + Goblin renamed.

- [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) scrubbed. The seven dropped monster names removed from current-state listings: Multiattack, RAW spellcasting, Poison damage rider, Magic Resistance, Devil's Sight, Innate Spellcasting per-spell envelope, Tail Spike ammunition, Telepathy as a sense, Legendary Actions, Legendary Resistance, Couatl Constrict cross-reference, Frightful Presence cross-reference. Six deferred-mechanic bullets were entirely about dropped-monster mechanics and got removed wholesale (Antimagic Cone, Eye Rays, Mind Blast, Tentacle stun on grapple, Extract Brain, Permanent magical Invisibility). Five batch-description sections updated to flag that their effective batch counts dropped due to slice 141 (caster NPCs from 4 to 3, Low-CR Fiends from 5 to 4, Aberration opener from 5 to 3, Fey cohort from 5 to 4, Devil ladder from 5 to 4). The nine renamed monsters get an inline rename note in their original batch descriptions plus updated names in the current-state listings (Multiattack, RAW spellcasting, Pack Tactics, Dark Devotion). The seed batch (MM beasts) annotated to flag Orc dropped + Goblin Warrior renamed.

**Initiative complete.** All 111 starter-pack monsters match SRD 5.2.1 exactly on AC, HP, abilities, and CR. Action / trait / save / resistance / immunity / speed / size / type drift checks remain as a follow-up audit (these don't drift often between 2014 and 2024 stat blocks, but the slice-149 audit script didn't cover them). Magic items (138) and spells (399) are the obvious next audits to apply the same script-driven pattern.

*SRD 5.2.1 compliance: drift refresh batch 6 of 6 (drift cleared)* (slice 148). Final drift-refresh slice. Refreshes the last 6 stat-drifted monsters to SRD 5.2.1: Triceratops (AC 13→14, HP 95→114, 10d12+30→12d12+36), Unicorn (HP 67→97, 9d10+18→13d10+26), Wight (HP 45→82, 6d8+18→11d8+33), Wolf (AC 13→12, STR 12→14), Young White Dragon (HP 133→123, 14d10+56→13d10+52), Zombie (HP 22→15, 3d8+9→2d8+6). **Audit: 111 exact-match / 0 drift.** All 111 starter-pack monsters now match SRD 5.2.1 exactly on AC, HP (average + formula), all six abilities, and CR. The action / trait / save / resistance / immunity / speed / size / alignment / type drift checks remain as a separate follow-up audit (these don't drift often but the script doesn't catch them). Slice 149 closes the initiative with a doc-refresh pass: README + starter-pack-gaps.md cleanup of references to dropped monsters from the deferred-mechanic listings, and content-attribution.md refresh to reflect the post-audit reality. Full suite green.

*SRD 5.2.1 compliance: drift refresh batch 5 of 6* (slice 147). Refreshes 10 stat-drifted monsters (alphabetical 41-50 of 56) to SRD 5.2.1: Quasit (HP 7→25, 3d4→10d4), Satyr (AC 14→13), Shadow (HP 16→27, 3d8+3→5d8+5), Shambling Mound (HP 136→110, 16d10+48→13d10+39), Skeleton (AC 13→14, DEX 14→16), Solar (HP 243→297, 18d10+144→22d10+176), Sprite (HP 2→10, 1d4→4d4), Stirge (AC 14→13, HP 2→5, 1d4→2d4), Stone Golem (AC 17→18, HP 178→220, 17d10+85→21d10+105), Storm Giant (WIS 18→20). Audit: 105 exact-match (+10) / 6 drift (-10). Only 6 drift entries remain (Triceratops, Unicorn, Wight, Wolf, Young White Dragon, Zombie), the slice 148 batch. Full suite green.

*SRD 5.2.1 compliance: drift refresh batch 4 of 6* (slice 146). Refreshes 10 stat-drifted monsters (alphabetical 31-40 of 56) to SRD 5.2.1: Imp (HP 10→21, 3d4+3→6d4+6), Invisible Stalker (HP 104→97, 16d8+32→13d10+26), Iron Golem (HP 210→252, 20d10+100→24d10+120), Lemure (AC 7→9, HP 13→9, 2d8+4→2d8, CON 14→11), Mage (AC 12→15, HP 40→81, 9d8→18d8), Ochre Jelly (HP 45→52, 6d10+12→7d10+14), Ogre (HP 59→68, 7d10+21→8d10+24), Otyugh (HP 114→104, 12d10+48→11d10+44), Planetar (HP 200→262, 16d10+112→21d10+147, CHA 24→25), Priest (HP 27→38, 5d8+5→7d8+7, STR 10→16). Audit: 95 exact-match (+10) / 16 drift (-10). Full suite green.

*SRD 5.2.1 compliance: drift refresh batch 3 of 6* (slice 145). Refreshes 10 stat-drifted monsters (alphabetical 21-30 of 56) to SRD 5.2.1: Druid (AC 11→13, HP 27→44, 5d8+5→8d8+8, WIS 15→16), Dryad (AC 11→16), Earth Elemental (HP 126→147, 12d10+60→14d10+70), Fire Elemental (HP 102→93, 12d10+36→11d10+33), Frost Giant (HP 138→149, 12d12+60→13d12+65), Gelatinous Cube (HP 84→63, 8d10+40→6d10+30), Giant Centipede (AC 13→14, HP 4→9, 1d6+1→2d6+2), Giant Rat (AC 12→13, DEX 15→16), Gibbering Mouther (HP 67→52, 9d8+27→7d8+21), Gray Ooze (AC 8→9). Audit: 85 exact-match (+10) / 26 drift (-10). The Druid monster id collides with the Druid class id (both at 6-space indent), so the refresh script's first-match strategy initially missed the monster entry; the Druid stats were applied via a direct Edit instead. Future batches don't hit any other id collisions (verified via id-overlap check between classes and monsters). Full suite green.

*SRD 5.2.1 compliance: drift refresh batch 2 of 6* (slice 144). Refreshes 10 stat-drifted monsters (alphabetical 11-20 of 56) to SRD 5.2.1: Black Pudding (HP 85→68, 10d10+30→8d10+24), Boar (HP 11→13, 2d8+2→2d8+4, CON 12→14), Bone Devil (AC 19→16, HP 142→161, 15d10+60→17d10+68), Brown Bear (HP 34→22, 4d10+12→3d10+6, STR 19→17, DEX 10→12, CON 16→15), Chain Devil (AC 16→15), Constrictor Snake (AC 12→13), Couatl (HP 97→60, 13d8+39→8d8+24), Deva (HP 136→229, 16d8+64→27d8+108), Dire Wolf (HP 37→22, 5d10+10→3d10+6), Dretch (STR 11→12). Audit script: 75 exact-match (+10) / 36 drift (-10). Full suite green.

*SRD 5.2.1 compliance: drift refresh batch 1 of 6* (slice 143). Third slice of the SRD 5.2.1 standardization initiative. Refreshes 10 stat-drifted monsters (alphabetical 1-10 of 56) to SRD 5.2.1: Aboleth (HP 135→150, formula 18d10+36→20d10+40), Adult Black Dragon (CHA 17→19), Adult Blue Dragon (HP 225→212, formula 18d12+108→17d12+102, CHA 19→20), Adult Green Dragon (CHA 17→18), Adult Red Dragon (CHA 21→23), Ankheg (HP 39→45, formula 6d10+6→6d10+12, CON 13→14), Assassin (AC 15→16, HP 78→97, formula 12d8+24→15d8+30, DEX 16→18, INT 13→16), Bandit Captain (HP 65→52, formula 10d8+20→8d8+16), Bearded Devil (HP 52→58, formula 8d8+16→9d8+18, CHA 11→14), Black Bear (DEX 10→12). Audit script confirms: 65 exact-match (+10) / 46 drift (-10). Full suite still green at 1480 tests; no test referenced any of these old stat values. The remaining 46 drift entries get refreshed in slices 144 to 148 (~10 per batch).

*SRD 5.2.1 compliance: rename + refresh 9 monster entries* (slice 142). Second slice of the SRD 5.2.1 standardization initiative. Nine starter-pack monsters had matching SRD 5.2.1 stat blocks under a different name with stat drift; this slice renames the id + name and refreshes the drifted fields:

- `acolyte` (Acolyte) → `priest-acolyte` (Priest Acolyte): AC 10→13, HP 9→11, formula 2d8→2d8+2, STR 10→14, CON 10→12
- `cult-fanatic` (Cult Fanatic) → `cultist-fanatic` (Cultist Fanatic): HP 33→44, formula 6d8+6→8d8+8, WIS 13→14, CHA 14→13
- `flying-sword` (Flying Sword) → `animated-flying-sword` (Animated Flying Sword): HP 17→14, formula 5d6→4d6
- `goblin` (Goblin) → `goblin-warrior` (Goblin Warrior): HP 7→10, formula 2d6→3d6, DEX 14→15
- `minotaur` (Minotaur) → `minotaur-of-baphomet` (Minotaur of Baphomet): HP 76→85, formula 9d10+27→10d10+30
- `rug-of-smothering` (Rug of Smothering) → `animated-rug-of-smothering` (Animated Rug of Smothering): HP 33→27, formula 6d10→5d10
- `thug` (Thug) → `tough` (Tough): AC 11→12, DEX 11→12
- `tribal-warrior` (Tribal Warrior) → `warrior-infantry` (Warrior Infantry): AC 12→13, HP 11→9, formula 2d8+2→2d8, CON 12→11
- `veteran` (Veteran) → `warrior-veteran` (Warrior Veteran): HP 58→65, formula 9d8+18→10d8+20

One test file required a single id update (`starter-pack.test.ts` asserted `monsters.get('goblin')?.cr === 0.25`; the goblin sanity-check now uses `goblin-warrior`). All other "Veteran" / "Goblin" / "Acolyte" string occurrences across the test suite turn out to be unrelated character-fixture display names, not monster-id references. Full suite still green at 1480 tests. Re-running the audit script after the renames: 55 exact-match + 56 drift (the 56 drift entries are the next slices' work, no new drift introduced).

*SRD 5.2.1 compliance: drop 7 non-SRD monster entries* (slice 141). First slice of the SRD 5.2.1 standardization initiative (audit at [docs/srd-5.2.1-audit.md](docs/srd-5.2.1-audit.md)). Seven starter-pack monsters had no SRD 5.2.1 counterpart: Beholder + Mind Flayer (WotC trademarks, never in any SRD), Orc (was in SRD 5.1 but removed from 5.2.1 since Orcs are now a playable species), Apprentice Wizard / Manes / Pixie / Spined Devil (not in SRD 5.2.1). All seven deleted from [src/content/packs/starter-pack.json](src/content/packs/starter-pack.json); monster count drops from 118 to 111. Zero references to the deleted ids existed anywhere outside starter-pack.json (verified via grep across src/ and tests/), so no test refresh required and no `-u` needed. The audit doc tracks the remaining work: slice 142 renames 9 entries that match SRD 5.2.1 under different names + refreshes their stats; slices 143 to 148 batch-refresh the 56 entries with stat drift (2014 MM values that the SRD 5.2.1 updated); slice 149 closes out the doc refresh (content-attribution, README coverage headline, gaps doc).

*Monster Recharge area-save Breath Weapons* (slice 140). The monsters-batch-2 / 3 handoffs named this as the highest-leverage remaining engine primitive: 11+ monsters share one parameterized RAW shape (Recharge X-6 die roll at turn-start + area save + halve-on-success damage). Slice 140 adds the primitive and wires 12 statblocks on it. New `MonsterStatblock.breathWeapon?: BreathWeaponSpec` field carrying `{ id, name, rechargeMin, area: { shape: 'cone' | 'line', sizeFeet }, save: { ability, dc, halfOnSuccess }, damageDice, damageType }`. New `Character.breathWeaponExpended: boolean` runtime flag (default false) persists across encounters per RAW. Two new event types: `BreathWeaponFired` (sets the bearer's expended flag) and `BreathWeaponRecharged { roll }` (clears it). New planner `planBreathWeapon({ monsterId, targetIds })` rolls one shared damage pool per RAW Areas of Effect, then per-target save (DEX or CON per the statblock) with `sourceIsMagical: true` (Magic Resistance honored), per-target DamageApplied via `mitigateDamage` + `interceptFatalDamage`. Recharge runs automatically: `planAdvanceTurn` and `planBeginFirstTurn` now call `planBreathWeaponRechargeAtTurnStart` for the active combatant, which rolls a d6 and emits BreathWeaponRecharged on a roll meeting the statblock's `rechargeMin`. The engine doesn't model cone / line area-of-effect target inclusion (no LOS or positional area resolution); consumers / DMs supply the affected target list directly, same as Thunder Step's ally argument and Clairvoyance's location. 12 monsters wired: 5 Young Chromatic Dragons (cone or line, 10d8-12d6 damage at rechargeMin 5), 5 Adult Chromatic Dragons (60-90 ft cones / lines, 14d8-22d6 damage at rechargeMin 5), Iron Golem (15 ft cone, 10d8 poison at rechargeMin 6), Ankheg (30 ft line, 3d6 acid at rechargeMin 6). Nine tests cover fire chain, per-target damage halving on save vs failure (seed-walked), expended gate rejects re-fire, no-breath-weapon rejection, Imp Magic Resistance advantage, Imp fire immunity zeroing damage, recharge success at turn-start clears expended, recharge failure leaves expended set, and non-monster PCs receive no recharge event.

*Content sweep: wire 12 schema-only magic items via the slice-132 projection pass* (slice 139). The items lane named this as the lookback sweep blocked by the parallel-authoring append-only protocol: items they shipped with `effects: []` in items batches 1.1 through 1.20 needed retroactive wiring from the engine lane. Slice 139 walks 12 items from stub to wired with the primitives already in-engine, JSON edits only. Wires: Cloak of Protection (AC +1 + 6 per-ability save +1; matches the Paladin Aura of Protection precedent for "+1 to all saves" since `ModifierTarget` requires discriminated per-ability entries rather than a generic 'save' literal), Ring of Protection (identical mechanical shape to Cloak of Protection), Stone of Good Luck (6 per-ability save +1 + 6 per-ability check +1; first item to exercise the `{ kind: 'check', ability: X }` ModifierTarget shape), Ring of Warmth (`GrantResistance cold`), Boots of the Winterlands (`GrantResistance cold`; the snow / ice difficult-terrain-ignore arm stays deferred), Slippers of Spider Climbing (`ModifySpeed climb set 30`; uses 30 as the canonical walking-speed approximation), Cloak of the Manta Ray (`ModifySpeed swim set 60`; underwater-breathing arm stays deferred behind the breathlessness-marker primitive), Ioun Stone Protection (`AddModifier ac +1`), Brooch of Shielding (`GrantResistance force`; the magic-missile-specific spell-id immunity arm stays deferred), Ring of Free Action (`GrantConditionImmunity paralyzed` + `GrantConditionImmunity restrained`; the difficult-terrain-movement-cost-suppression and magical-speed-reduction-immunity arms stay deferred), Gloves of Swimming and Climbing (`ModifySpeed climb set 30` + `ModifySpeed swim set 30` + `SetAdvantage on skill:athletics advantage`), Sentinel Shield (`SetAdvantage on:'initiative' advantage` + `SetAdvantage on skill:perception advantage`). Spellguard Shield deferred entirely because its mechanical arms (save advantage vs spells, attack disadvantage vs spell attacks) both need predicates the schema doesn't carry. The features snapshot moved as expected: 12 new IDs alphabetized into `wiredIds` (slice-126 design intent; wired-status flip catches engineering wiring a new mechanic, which is exactly what happened across these items). No engine code changes, no new primitives. Pure content edit riding on the slice-132 projection infrastructure.

*Arcane Eye: mobile sensor extension* (slice 138). Extends the slice-135 Sensor primitive with two optional fields: `mobile: boolean` (default false) and `darkvisionRange?: number`. Pre-slice 138 every sensor was implicitly immobile; slice 138 makes that explicit and adds the Arcane Eye-flavored mobile path. New `RemoteSensorMoved { sensorId, fromLocation, toLocation }` event + reducer + apply.ts wiring. New `planArcaneEye` planner (4th-level slot + action + concentration up to 1 hour; places a sensor with `mobile: true`, `darkvisionRange: 30`, `mode: 'sight'`). New `planMoveSensor` planner (caster's bonus action; updates the sensor's free-text `location`; rejects non-mobile sensors, ownership mismatches, and same-location no-ops). Existing `planClairvoyance` and `planScrying` now emit `mobile: false` explicitly on RemoteSensorPlaced; their sensors remain immobile per RAW. Eight tests: cast emits the full chain with the mobile + darkvision fields, slot-level rejection, unknown-spell rejection, successful move with state update, immobile-Clairvoyance-sensor rejection, ownership mismatch rejection, same-location rejection, and a regression check that Clairvoyance's sensor stays `mobile: false`. Locate Creature still ships schema-only as a content-only buff candidate (one-way directional ping with no engine state to track).

**Content authoring: monsters batch 1.1.** 10 low-CR NPC humanoids: Bandit (CR 1/8), Bandit Captain (CR 2), Cultist (CR 1/8), Cult Fanatic (CR 2), Acolyte (CR 1/4), Commoner (CR 0), Guard (CR 1/8), Noble (CR 1/8), Scout (CR 1/2), Spy (CR 1). Monster count grows from 6 to 16. All ten ship as bare statblocks (`traits: []`); RAW Multiattack / Parry / Cunning Action / Sneak Attack / monster spellcasting / Keen Hearing and Sight / Dark Devotion are documented as deferred riders in [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) (each is a known engine-vocabulary gap rather than a content authoring one).

**Content authoring: monsters batch 1.2.** 5 humanoid roster completers: Tribal Warrior (CR 1/8), Thug (CR 1/2), Berserker (CR 2), Veteran (CR 3), Knight (CR 3). Closes the humanoid roster through CR 3; monster count grows from 16 to 21. New deferral notes added to the gaps doc for the additional RAW riders this batch hits: Reckless (same gap shape as Spy's class-feature borrow), Brave (same per-condition-save-advantage gap as Dark Devotion), Pack Tactics (positional-condition-with-advantage), and Leadership (aura-style action grant + temporary-modifier-with-duration combo).

**Content authoring: monsters batch 1.3.** 8 low-CR bestiary picks (first broad fill outside humanoids): Zombie (CR 1/4), Ghoul (CR 1), Giant Rat (CR 1/8), Giant Spider (CR 1), Giant Wolf Spider (CR 1/4), Brown Bear (CR 1), Dire Wolf (CR 1), Stirge (CR 1/8). 6 Beasts + 2 Undead. Monster count grows from 21 to 29. New deferrals documented for Undead Fortitude (save-gated fatal-damage clamp variant of slice-111's PreventFatalDamage), on-hit save-or-condition rider (Ghoul Paralyzing Touch + Dire Wolf knockdown), Recharge-N action (Giant Spider Web), poison rider on attack (Giant Spider + Giant Wolf Spider), and Blood Drain attach (Stirge). Two cross-cutting structural gaps also surfaced once: no `actions` field on `MonsterStatblock` (the existing `dragon-bite` / `dragon-claw` / `ogre-greatclub` weapons in the items array are orphaned), and `creatureSubtype` would benefit from a constrained enum for tag-style subtypes.

### Fixed

- **CI Typecheck unbroken** (slice 99). Three latent test-file type errors had been gating the CI Typecheck step for ~10 commits. `tests/transcript.ts`'s `formatEvent` switch was missing cases for `AbsorbElementsCast` (slice 91) and `TrapArmed` / `TrapTriggered` / `TrapExpired` (slice 94); two test files imported `DamageRolledEvent` from `combat.js` when it lives in `attack.js`. Vitest's esbuild let these slide; tsc didn't. No engine impact.

### Changed

- **gaps doc as the canonical content-coverage surface**. [docs/starter-pack-gaps.md](docs/starter-pack-gaps.md) became the per-spell catalog of "wired vs schema-only" and the per-primitive future-slice queue. Every slice walks the relevant rows from `future` to `shipped` and from `schema-only` to `wired`.

## 0.1.0-alpha.5 - 2026-05-14

The fifth pre-alpha. Headlined by a 48-probe RAW-compliance audit at [tests/audit/raw-compliance.test.ts](tests/audit/raw-compliance.test.ts) and the engine fixes that close every probe (0 failing, 0 skipped), plus a Tier 3 content-stub sweep that took the class-features matrix from 36 wired / 12 stub to **48 wired / 0 stub** at L1–7. The pre-release closes with a Claude-optimization documentation pass + a bug-hunting test sweep (boundary sweeps, multi-class property generator, random spell-cast sequences, content-pack validator fuzzing). Test count grew from 698 to 1009 (311 new tests across 32 new files), all on top of the unchanged architectural invariants. `SCHEMA_VERSION` unchanged: new event shapes (`OpportunityAvailable`, `WeaponLoaded`, `RecklessAttackActivated`, `StunningStrikeAttempted`) and new optional fields (`sourceCharacterId` on conditions, `attackerHasAllyAdjacentToTarget` on `AttackRolled`, `loadedWeaponsFiredThisTurn` + `recklessAttackActive` + `stunningStrikeUsedThisTurn` on combatant turn usage) are all additive; `DerivedCharacter.knownLanguages` and the new `ExpandCritRange` / `GrantHalfProficiencyBonusFloor` / `BoostHealing` / `GrantEvasion` effect primitives are additive.

### Tier 3 content stubs

- **Druidic language wired.** Druid L1 Druidic now emits `GrantProficiency { target: 'language', id: 'druidic' }` instead of an empty `effects: []`. New `computeKnownLanguages` derivation combines species + background `languages[]` with any `GrantProficiency target: 'language'` effects from the active effect stack, returning a sorted deduplicated list. Surfaced on `DerivedCharacter.knownLanguages` and exported from the public barrel.
- **Improved Critical wired (Champion L3).** Champion Fighter L3 Improved Critical now emits a new `ExpandCritRange { threshold: 19 }` effect primitive. The accumulator tracks the lowest threshold (default 20; multiple sources stack down), and `resolveAttack` uses it for crit detection. RAW behavior preserved: a nat 20 still auto-hits, and a crit only counts on a hit (a 19 that misses AC is just a miss).
- **Slow Fall wired (Monk L4).** A new `useSlowFall?: boolean` flag on `FallingIntent` opts the actor into spending their reaction. `planFalling` reduces the raw falling damage by `5 × monk level` before mitigation; in an active encounter it also emits `ActionEconomyConsumed { kind: 'reaction' }`. Rejects when the actor isn't a Monk L4+ or has already used their reaction this round.
- **Martial Arts die scaling wired (Monk L1/5/11/17).** Monk L1 Martial Arts now scales the unarmed strike damage die (1d6 at L1, 1d8 at L5, 1d10 at L11, 1d12 at L17). A new `unarmed-strike` simple-melee weapon ships in the starter pack (1d4 bludgeoning, `light`); when a Monk wields it, both `resolveAttack` and `planOffHandAttack` substitute the Martial Arts die when larger than the weapon's natural die. Dexterous Attacks ability swap and the bonus-action unarmed-strike mechanic remain consumer-driven.
- **Jack of All Trades wired (Bard L2).** New `GrantHalfProficiencyBonusFloor` effect primitive. `computeAbilityCheck` now tracks whether any explicit proficiency contribution applied; if not, adds `floor(profBonus / 2)` (source: `jack-of-all-trades`). Does not stack on proficient / expertise checks. Applies to ability checks, not saves.
- **Sacred Weapon wired (Oath of Devotion L3).** New `engine.plan.sacredWeapon({ paladinId })` planner spends one Channel Divinity charge, emits `ResourceSpent` + `ConditionApplied` (`sacred-weapon-active`, +3 attack bonus) + `ActionEconomyConsumed { kind: 'bonusAction' }` (only on the paladin's own turn). The `+3` is a static placeholder representing a typical Paladin CHA mod — `AddModifier` doesn't yet evaluate Formula values. Paladin L3 also now grants the `channel-divinity` resource (2 uses, short-rest recharge).
- **Disciple of Life wired (Life Cleric L3).** New `BoostHealing { flat, perSpellLevel }` primitive. `planHealMechanic` builds the caster's effect stack and adds `flat + perSpellLevel * slotLevel` HP to each target when the spell is level 1+ (cantrips excluded). Wired as `{ flat: 2, perSpellLevel: 1 }`.
- **Reckless Attack wired (Barbarian L2).** New `engine.plan.recklessAttack({ combatantId })` planner enforces "once per turn before first attack" and emits a `RecklessAttackActivated` event whose reducer flips `turnUsage.recklessAttackActive`. `resolveAttack` reads the flag two ways: the attacker's melee STR attacks gain advantage, and any attack against the reckless combatant grants advantage. Flag clears on the actor's next `TurnStarted`.
- **Stunning Strike wired (Monk L5).** New `engine.plan.stunningStrike({ monkId, targetId })` spends 1 ki, rolls a target CON save vs DC `8 + WIS + prof`, and applies the `stunned` condition on failure. Once-per-turn enforced via a new `turnUsage.stunningStrikeUsedThisTurn` flag (cleared at `TurnStarted`). New `StunningStrikeAttempted` event records activation; the save itself is a regular `SaveRolled`.
- **Frenzy wired (Path of the Berserker L3).** Minimal wire: `engine.plan.frenzy({ combatantId })` consumes one Rage charge and applies the new `frenzied` content condition (+2 damage placeholder). The full Rage mechanic (damage resistance, bonus-action attack grant, end-of-rage exhaustion) is consumer-driven until Rage gets its own planner slice.
- **Evasion wired (Rogue L7, Monk L7).** New `GrantEvasion` primitive. `planCastSpell`'s save-mechanic path reads the target's effect stack: when Evasion is present + the save is DEX + the spell halves on success, the formula flips to (success → 0 damage, fail → half). Rogue L7 gains a new `evasion-rogue` content entry alongside Sneak Attack scaling.
- **Cutting Words wired (College of Lore L3).** New `engine.plan.cuttingWords({ bardId, originalRollTotal, threshold })` rolls the bard's Bardic Inspiration die (d6/d8/d10/d12 by tier), spends one BI charge, and returns `{ events, dieRoll, preventedHit }`. Same consumer-adjudicated shape as `planShield`. Enforces reaction-once-per-round in active encounters.
- **Metamagic wired (Sorcerer L2).** New `engine.plan.metamagic({ sorcererId, option })` validates sorcery-points balance and spends the right cost per option (1 for most, 2 for Quickened, 3 for Heightened, per RAW 2024). The actual mechanical effect on the spell being cast is consumer-driven for now — this slice wires the sorcery-point economy so the resource is no longer inert.
- **Fighting Style choice wired.** Fighter L1, Paladin L2, and Ranger L2 now ship an `OfferChoice` effect with their respective Fighting Style options. Starter-pack feats `fighting-style-archery` / `defense` / `dueling` gain real `AddModifier` effects (unconditional approximations, since the predicate DSL can't yet express "while wielding a ranged weapon" or "while wearing armor"). Great Weapon Fighting / Protection / Two-Weapon Fighting remain effects-empty placeholders.
- **Feral Instinct wired (Barbarian L7).** Emits `SetAdvantage { on: 'initiative', mode: 'advantage' }`. `planRollInitiative` now consults the actor's effect stack and rolls 2 d20s for advantage / 1 for disadvantage / 1 otherwise.
- **Deft Explorer wired (Ranger L2).** Emits an `OfferChoice` for skill expertise (5 options: Perception, Stealth, Survival, Nature, Animal Handling). `planLevelUp` emits the matching `ChoiceRequired`; resolving it grants expertise via `GrantProficiency target:'skill' level:'expertise'`.
- **Wild Companion wired (Druid L2).** New `engine.plan.wildCompanion({ druidId })` planner expends one Wild Shape charge in lieu of transformation. The familiar itself isn't a first-class entity yet; this slice wires the resource-spend side.

Class-features matrix: 36 wired / 12 stub → **48 wired / 0 stub** at L1–7. Subclass matrix flips on Improved Critical, Sacred Weapon, Disciple of Life, Cutting Words, Frenzy.

### Fixed (RAW closures)

- **Action-blocking conditions reject actions.** Incapacitated / Stunned / Paralyzed / Petrified / Unconscious actors, and any combatant at HP 0, can no longer attack / cast / dash / dodge / move. Threaded through every planner via a new `assertActorCanAct` helper.
- **Move-into-occupied-space rejection.** `planMove` scans the active encounter and rejects a destination that matches another combatant's position. Previously two combatants could share a square.
- **Opportunity attacks.** `planMove` now emits one `OpportunityAvailable` per eligible reactor (in-reach, has reaction, not the mover). `planOpportunityAttack` consumes the reaction; cap enforced via `reactionUsedThisRound`. The web demo surfaces an OA queue with Take / Pass per offer, auto-pruned when preconditions lapse.
- **Prone stand-up costs half speed.** `planMove` debits half the actor's speed (rounded down) when standing from prone.
- **Ranged-in-melee disadvantage.** Ranged attacks while a hostile combatant is adjacent to the attacker now resolve with disadvantage.
- **Concentration auto-clears at HP 0.** A downed concentrator's effect is cleared in the same commit as the damage; the redundant invariant in `applyConcentrationBroken` was removed and the helper `clearConcentrationEffect` tolerates a dangling pointer.
- **Spell casts consume the action.** `planCastSpell` now emits `ActionEconomyConsumed` based on the spell's parsed `castingTime` (action / bonus action / reaction / long-cast).
- **Frightened / Charmed track their source.** New `sourceCharacterId` on `AppliedCondition` and `ConditionApplied`. Frightened rejects moves *closer* to the source; Charmed rejects attacks *against* the source.
- **Heavy-weapon Small disadvantage.** A Small attacker with a Heavy weapon attacks with disadvantage.
- **Sneak Attack: ally-adjacent qualifier.** `AttackRolled` carries `attackerHasAllyAdjacentToTarget`; the starter-pack Sneak Attack predicate triggers on advantage OR (used !== disadvantage AND ally-adjacent).
- **Loading property on ranged weapons.** A loaded weapon (light crossbow, heavy crossbow, hand crossbow, longbow) can fire once per turn; `WeaponLoaded` records the consumption and resets at turn start.
- **Difficult terrain doubles movement cost.** `planMove` walks the path via Bresenham cells, summing per-cell `movementCostAt`. Diagonal-through-difficult costs the right amount.
- **`planEquip` rejects illegal equip combinations.** Two-handed weapon in mainHand while a shield is equipped (and vice versa) is now rejected before the `ItemEquipped` event fires.

### Added

- **Rules Lab in the web demo.** 19 showcase probes grouped by category; one-click "Run audit" runs them against a fresh engine + scenario and reports pass / fail per row. Source: [web/audit/probes.ts](web/audit/probes.ts), [web/modes/rules-lab.ts](web/modes/rules-lab.ts).
- **Scenario gallery in the web demo.** Frightened Halfling (source-tracked movement restriction), Misty Step Occupied (occupancy-check rejection), Concentrating Wizard at 1 HP (concentration auto-clear on drop), in addition to the original Goblin Skirmish. URL hash includes `#scenario=<id>&seed=<n>`. Source: [web/scenarios/](web/scenarios/). CI replay test at [tests/integration/web-scenarios.test.ts](tests/integration/web-scenarios.test.ts) covers headline actions per scenario.
- **Map panel in the web demo.** A small grid view between Combat Sandbox and Event Inspector showing each combatant token at their position with initials and color; auto-fits the actor bounds. Source: [web/modes/grid-view.ts](web/modes/grid-view.ts).
- **Trustworthiness roadmap at [docs/trustworthiness-roadmap.md](docs/trustworthiness-roadmap.md)** framing the four-tier path from alpha to "trustworthy for unsupervised tabletop play": Tier 1 close audit, Tier 2 extend audit, Tier 3 content stubs, Tier 4 real SRD pack.

### Changed

- **README cleanup** (1cd20dc): collapsed 70+ lines of in-line "✓ Closed in 2026-05-14 sweep" retrospective in the Engine gaps section into a one-line summary referencing the audit file. Test counts refreshed to 763 / 118.

### Documentation

- **CLAUDE.md source map.** New section that lists where each kind of file lives (events, reducers, planners, derivations, content packs, handlers, test categories). Predicts the answer to "where do new things go" without an `ls`.
- **CLAUDE.md planner shape.** New subsection codifying the skeleton extracted from [src/engine/plan/attack.ts](src/engine/plan/attack.ts) (large, RNG + triggers) and [src/engine/plan/sacred-weapon.ts](src/engine/plan/sacred-weapon.ts) (small, no RNG) so new planners pattern-match.
- **[docs/slice-template.md](docs/slice-template.md).** Breaks the slice workflow into three shape-specific checklists: new planner, new content, new derivation. Each carries a file checklist + order of operations + gotchas.

### Tests

The bug-hunting sweep added 165 tests across 4 files. The headline finding: zero engine bugs surfaced — every invariant the new tests check holds across random samples and exhaustive table walks. The tests are now permanent safety nets.

- **Boundary sweep over canonical PHB 2024 tables** ([tests/boundaries/tabulated-math.test.ts](tests/boundaries/tabulated-math.test.ts)). 153 exhaustive assertions: ability modifier for every score 1-30, proficiency bonus for every level 1-20, full-caster + half-caster + pact spell-slot tables, carrying capacity for every STR 1-30, exhaustion d20 penalty for every level 0-6, plus out-of-range rejection paths. Targets off-by-one bugs that random property-test sampling hits only in expectation.
- **Multi-class character property generator** ([tests/property/generators.ts](tests/property/generators.ts), [tests/property/multiclass.test.ts](tests/property/multiclass.test.ts)). New `multiclassCharacterArb` produces 2-3-class characters meeting 2024 PHB multiclass prerequisites; 10 invariants run at 1000 iterations each cover total-level bounds, distinct classes, proficiency-bonus from total level, slot-count non-negativity + max-vs-available, fighter-leakage protection, pact-warlock isolation, warlock-non-caster mix, and the wizard+paladin ceil-half rule.
- **Random spell-cast sequence invariants** ([tests/property/spell-cast-sequence.test.ts](tests/property/spell-cast-sequence.test.ts)). Single property running 1000 random sequences (5-20 casts each) against a fixed wizard + cleric + two dummies, drawing from a curated pool of 16 fully-wired starter-pack spells. Asserts slot accounting (`SpellSlotConsumed` events match final `spellSlotsUsed`), concentration field consistency, and replay equivalence at sequence end.
- **Content-pack validator path-reporting fuzz** ([tests/property/content-pack-validator.test.ts](tests/property/content-pack-validator.test.ts)). 200 random mutations against the starter-pack JSON (replace-with-wrong-type / replace-with-array / delete-key); asserts that when Zod rejects, the reported issue path overlaps the mutated location so content-pack authors can navigate to the problem.

## 0.1.0-alpha.4

The fourth pre-alpha. Closes two engine bugs that the new browser demo surfaced (Dodge → Attack guard and weapon range enforcement), broadens the public type surface with 15 event-type re-exports, and ships a deployable web demo as a second adoption surface alongside the CLI examples.

Test count grew from 691 to 698 (7 new tests: 2 engine regressions for the new guards, 3 web-demo replay-equivalence assertions, 2 melee-range tests). All Layer 5/6/7/8/9 invariants still hold. `SCHEMA_VERSION` unchanged (no event or state shape changes).

The web demo lives under `/web/` and is not shipped in the npm tarball (excluded by the `files` whitelist); it's a separate adoption surface deployed via GitHub Pages from a CI workflow. See [web/README.md](web/README.md) and [docs/web-demo-plan.md](docs/web-demo-plan.md).

### Fixed

- **`engine.plan.attack` now rejects when the action has already been spent on Dodge / Dash / Disengage / Cast Spell.** Previously the attack planner only checked the per-attack swing budget (`attacksMadeThisTurn < maxAttacksPerAction`); it did not check `turnUsage.actionUsed`. As a result a combatant could Dodge (consuming their action) and then Attack on the same turn. The fix adds an "action already used by a non-attack ability" guard, detected via `actionUsed && attacksMadeThisTurn === 0` so Extra Attack's second swing (which legitimately runs with `actionUsed: true, attacksMadeThisTurn: 1`) continues to work. Regression test in [tests/unit/engine/plan-attack.test.ts](tests/unit/engine/plan-attack.test.ts).
- **`engine.plan.attack` now enforces weapon range against combatant positions.** Previously the attack planner ignored positions entirely, so a longsword could swing at a target 30 ft away as if they were adjacent. The fix consults the active encounter's combatant positions and the weapon's `attackKind` / `properties` / `rangeNormal` / `rangeLong`: melee weapons require Chebyshev distance ≤ 5 ft (10 ft with the `reach` property), ranged weapons require ≤ long range. RAW disadvantage in the (normal, long] band is deferred to a follow-up; this fix only rejects the physically impossible. Unpositioned combatants are exempt (preserves existing un-positioned fixtures). Regression tests in [tests/unit/engine/plan-attack.test.ts](tests/unit/engine/plan-attack.test.ts).

### Added

- Type re-exports for the encounter / inventory / combat event types that were previously only available as schemas: `EncounterCreatedEvent`, `EncounterStartedEvent`, `EncounterEndedEvent`, `InitiativeRolledEvent`, `TurnStartedEvent`, `TurnEndedEvent`, `RoundEndedEvent`, `CombatantMovedEvent`, `ItemAcquiredEvent`, `ItemEquippedEvent`, `ItemUnequippedEvent`, `AttackRolledEvent`, `DamageRolledEvent`, `SaveRolledEvent`, `AbilityCheckRolledEvent`. Source: [src/types/index.ts](src/types/index.ts). Schema exports were already in place; this just closes the type-only gap so consumers can `satisfies XEvent` without reaching into deep paths.
- Web demo at [web/](web/), deployed to GitHub Pages via [.github/workflows/deploy-demo.yml](.github/workflows/deploy-demo.yml). Combat Sandbox (turn-aware action toolbar: Attack / Move / Dash / Dodge / End Turn, with downed-combatant fallback to just End Turn), Event Inspector (virtualized list with category color-coding, auto-scrolls to tail when followed), Export/Import event log with on-page replay verification, generic PendingChoice resolver (shipped dead in v1 by design — the v1 planner set doesn't emit `ChoiceRequired`, but the resolver is in place for future modes), and a `#seed=N` URL hash override for deterministic-reproduction sharing. CI integration test at [tests/integration/web-scenarios.test.ts](tests/integration/web-scenarios.test.ts) asserts replay equivalence against every shipped demo scenario.

## 0.1.0-alpha.3

The third pre-alpha. Closes the engine-gap punch-list down to two ⚪ items (sanity and mass-combat variant rules, each its own slice), ships all three remaining layers of the testing standard (Layer 7 property tests with `fast-check`, Layer 8 feature-coverage matrix, Layer 9 public-API contract test), authors the first subclass content (12 of ~50, one per class at L3), wires five more class features at L1-5 (Barbarian Unarmored Defense + Danger Sense, Sorcerer Sorcerous Restoration, Paladin Lay on Hands, Wizard Arcane Recovery), wires the Bard's L3 Expertise + L5 Font of Inspiration, and lands a new `planDodge` plus an `ImposeDisadvantageOnAttackers` effect primitive so the Dodge action actually defends.

Test count grew from 563 to 691 (128 new tests across 22 new files); all replay-equivalence, RNG-capture, and contract invariants still hold. Build now emits a separate `dist/starter-pack.{js,cjs,d.ts}` chunk so browser consumers can code-split the starter content off the main bundle (`import('ttrpg-engine-dnd/starter-pack')`).

### Added

- **`hp-pool-knockout` spell mechanic** — new `SpellMechanic` kind for Sleep-style spells. Rolls a dice pool (`poolDice`, optional `extraPoolDicePerSlotLevel`), walks targets in ascending current-HP order, and applies the configured condition until the pool runs out. Targets already carrying the condition are skipped. Sleep is now fully wired (5d8 base, +2d8 per slot above 1st, applies `unconscious`).
- **`engine.plan.shield`** — reactive +5 AC planner. Takes the triggering attack's d20 total and the original AC; emits `ActionEconomyConsumed(reaction)`, `SpellSlotConsumed`, `ConditionApplied('shielded')`, and a new `ShieldCast` event with `preventedHit: boolean`. The new `shielded` condition grants +5 AC via `AddModifier`. Magic Missile damage immunity is not yet modeled (would need per-spell immunity primitive).
- **`engine.plan.mistyStep`** — dedicated bonus-action teleport planner. Validates spell knowledge, slot, bonus-action availability, and 30-ft Chebyshev range; emits `SpellCastDeclared`, `SpellSlotConsumed(2+)`, `ActionEconomyConsumed(bonusAction)`, and `CombatantMoved(feetTraveled: 0)`.
- **`engine.plan.expireSpellDurations`** — emits `ConcentrationBroken(reason='durationEnded')` events for every active effect whose listed duration has elapsed by the current in-game clock. New `parseSpellDurationMinutes` parses common 2024 duration phrasings. `EffectInstance` gains `durationMinutes` / `startedAtMinutes`; `ConcentrationStarted` carries `durationMinutes` which the reducer pairs with the in-game clock.
- **`DerivedCharacter.hpMaxBonus` / `effectiveHpMax`** — character-view derivation now surfaces the sum of `AddModifier{target:'hpMax'}` effects. Aid adds a new `aid-buffed` condition that grants +5 hpMax. Reducer-side massive-damage threshold still compares against the stored `hp.max` (full enforcement is a larger refactor).
- **`engine.plan.consumeGuidance`** — rolls the Guidance d4 for the target, removes the `guided` condition, and ends the caster's concentration (`reason='used'`). Pairs with a new `GuidanceUsed` event and a new `'guided'` condition (no static effects; the d4 is rolled at consume time so the player can choose when to apply it).
- **`engine.plan.cleave`** — companion to a successful melee hit with a Cleave-mastery weapon. Rolls a follow-up attack against a second target, strips the attacker's positive ability modifier from damage per RAW, and marks `mastery:cleave` as fired this turn (re-invocation in the same turn throws).
- **Nick mastery in `engine.plan.offHandAttack`** — when the off-hand weapon has Nick, the planner emits `TriggerFired('mastery:nick')` instead of `ActionEconomyConsumed('bonusAction')` once per turn.
- **Flex mastery in `engine.plan.attack`** — versatile weapons with Flex mastery wielded two-handed (off-hand empty) roll the larger `versatileDice` at attack time.
- **`engine.plan.tickAura`** + `aura-damage` `SpellMechanic` — concentration auras like Spirit Guardians register at cast time (only `ConcentrationStarted` fires) and the consumer calls `tickAura({ casterId, targetIds })` per turn for the affected creatures. Each tick rolls a save and applies damage independently per target.
- **`engine.plan.polymorph`** — full RAW validation: caster knowledge, 4th+ slot, target's level caps the form CR, optional WIS save for unwilling targets (the slot is still spent on a successful save), breaks the caster's prior concentration before starting the new one. Returns `{ events, resisted }`.
- **`engine.plan.wildShape`** — druid bonus-action transform. Validates druid level ≥ 2, available `wild-shape` resource pool, form CR cap by level (1/4 / 1/2 / 1 at L2 / L4 / L8), and flying-speed gating until L8. Emits `ActionEconomyConsumed('bonusAction')` (when in an encounter), `ResourceSpent('wild-shape', 1)`, and `PolymorphApplied(kind='wild-shape')`.
- **Events / enums**: `ShieldCastEvent`, `GuidanceUsedEvent`, new `ConcentrationBrokenReason` values `'unconscious'` and `'used'`, new `SpellMechanic` kinds `aura-damage` and `hp-pool-knockout`, new `EffectInstance` fields `slotLevel` / `durationMinutes` / `startedAtMinutes`, new `ConcentrationStartedEvent.slotLevel` (so planners that need the upcast level at tick time can read it off the instance).
- **`engine.plan.forcedMarch`** — RAW 2024 ch.4 forced-march CON-save loop. For each hour past 8, every traveler makes a Constitution save at climbing DC (10 + 1 per hour past 8); failures gain a level of exhaustion. Travelers already at exhaustion max are skipped.
- **`engine.plan.simulacrum`** — RAW 2024 7th-level illusion. Validates caster knowledge, slot, materials (1500 gp ruby dust via `materialsConsumed` flag), and the "one Simulacrum per source creature" constraint. Returns `{ events, simulacrumId }` so consumers can wire UI to the new creature pre-commit.
- **`engine.plan.wish`** — RAW 2024 9th-level conjuration. Eight predefined effects bypass the stress cascade; anything else rolls a d100 and applies exhaustion on a result of 33 or lower. Returns `{ events, stressApplied }`.
- **`hp.maxBonus`** field on Character + `Character.appliedConditions[].hpMaxBonusDelta` stamping. The damage reducer's massive-damage threshold now compares against `hp.max + hp.maxBonus`, so an Aid-buffed character (effective max 17 instead of 12) doesn't die instantly to a 12-damage hit when at -4 HP. The buff planner sets the delta on `ConditionAppliedEvent.hpMaxBonusDelta`; the reducer copies it into the applied-condition entry and bumps `hp.maxBonus`. Removal (ConditionRemoved, ConcentrationBroken, LongRestStarted concentration-clearing) reads the stored delta and reverses it.
- **Property tests with `fast-check`** (Layer 7 of the testing standard in [CLAUDE.md](CLAUDE.md)). 16 properties across three files (`tests/property/derivations.test.ts`, `tests/property/reducers.test.ts`, `tests/property/plan.test.ts`), each run at 1000 iterations by default (override with `FAST_CHECK_NUM_RUNS=10000` for local fuzz). Properties: derivations never throw / AC ≥ 1 / `effectiveHpMax === hp.max + hp.maxBonus` for any character; reducer output is `CampaignStateSchema`-valid after any random event sequence; replay equivalence under fuzzing; `hp.current ≤ effective max` and `exhaustion ∈ [0, 6]` always; same seed → byte-equivalent dice payloads. Shared generators in `tests/property/generators.ts`. Followed by a richer stateful combat-sequence test (6 more properties, 60-turn random fights between fragile fighters).
- **12 subclasses (one per class, L3 entry)** — Path of the Berserker (Barbarian), College of Lore (Bard), Life Domain (Cleric), Circle of the Land (Druid), Champion (Fighter), Warrior of the Open Hand (Monk), Oath of Devotion (Paladin), Hunter (Ranger), Thief (Rogue), Draconic Sorcery (Sorcerer), Fiend Patron (Warlock), Evoker (Wizard). Each ships with its L3 feature entries; the ones that map cleanly to existing effect primitives are wired (College of Lore's bonus skill proficiencies, Champion's Remarkable Athlete walking-speed bump, Thief's Second-Story Work climb speed, Draconic Sorcerer's Resilience AC override, Life Domain's heavy-armor proficiency). The features blocked by engine work not yet done (Improved Critical's crit-on-19, Disciple of Life's heal-rider, Sacred Weapon, Frenzy, Sculpt Spells, Open Hand Technique, Cutting Words, Hunter's Prey, Dark One's Blessing, etc.) ship as `effects: []` stubs and are flagged in the feature-coverage matrix. PCs can now select a subclass at L3 and the engine reads it correctly through `effect-stack.ts`; six new tests in `tests/unit/content/subclasses.test.ts` smoke each subclass and exercise two of the wired features (Draconic AC = 13 + DEX-mod, College of Lore's proficiency count).
- **Class features L1-5 fill-in** — five more class features wired against existing primitives:
  - **Barbarian Unarmored Defense (L1)**: `OverrideACFormula` with DEX + CON. A no-armor Barbarian now derives AC 10 + DEX-mod + CON-mod automatically.
  - **Barbarian Danger Sense (L2)**: `SetAdvantage` on DEX saves.
  - **Sorcerer Sorcerous Restoration (L5)**: `RecoverResource` of `sorcery-points` on short rest (4 points back per RAW).
  - **Paladin Lay on Hands (L1, new entry)**: `GrantResource` of `lay-on-hands` with formula `level × 5` (renewable on long rest).
  - **Wizard Arcane Recovery (L1, new entry)**: `GrantResource` of `arcane-recovery-points` with formula `ceil(wizard-level × 0.5)` (recovered on long rest; consumer spends the budget on slot recovery).
  Five smoke tests in `tests/unit/content/class-features-l1-5.test.ts` build PCs at the relevant levels and assert derivations land. Class-features-matrix split: 29 → 34 wired, 17 → 14 stub, 46 → 48 total at the content layer.
- **Two more Bard class features wired** — Expertise (L3) grants `expertise` proficiency on Insight + Persuasion (fixed picks; OfferChoice-driven selection waits on a richer choice-resolution path). Font of Inspiration (L5) adds a `RecoverResource('bardic-inspiration', 'all', on short rest)` so the Bard's inspiration pool refreshes on short rest at L5+ per RAW 2024. Class-features-matrix split: 34 → 36 wired, 14 → 12 stub. Two smoke tests added: one verifies the effect-stack reports `expertise` on the chosen skills and not on others; one is a derivation completion check for the L5 Bard. Ranger Fighting Style (L2) attempted but reverted to stub — Archery's "+2 to ranged attacks" needs a conditional predicate the derivation evaluator doesn't yet populate facts for.
- **`engine.plan.dodge`** — RAW 2024 Dodge action. Emits `ActionEconomyConsumed('action')` + `ConditionApplied('dodged')`. The new `dodged` condition carries two effects: a new `ImposeDisadvantageOnAttackers` primitive (mirror of `GrantAdvantageToAttackers`) and `SetAdvantage` on DEX saves. The attack planner now consults both the target's `grantsAdvantageToAttackers()` and `imposesDisadvantageOnAttackers()` and applies the 2024 advantage/disadvantage cancellation matrix: if a target both grants advantage and imposes disadvantage (e.g. Restrained + Dodged), the two cancel and the attack rolls with neither — matching RAW. Four smoke tests in `tests/unit/engine/plan-dodge.test.ts` cover the happy path, the disadvantage stack check, the not-active-combatant rejection, and the already-used-action rejection.
- **Subpath export for the starter pack** — `import { loadStarterPack } from 'ttrpg-engine-dnd/starter-pack'` is now a real subpath. The Vite library config builds two entries (`src/index.ts` + `src/starter-pack.ts`) so the starter content JSON lands in its own chunk (~127 KB / 16 KB gzipped) separate from the main engine bundle (~208 KB / 47 KB gzipped). Browser consumers (the planned web demo, app dev servers) can `await import('ttrpg-engine-dnd/starter-pack')` only when they need the content, keeping the initial JS payload smaller.
- **Feature-coverage matrix** (Layer 8 of the testing standard in [CLAUDE.md](CLAUDE.md)). New `tests/coverage/features.test.ts` enumerates every notable 5.5e feature in the starter pack — 46 class features across 12 classes, all 9 weapon masteries, all 22 conditions, 30 feats by category (origin / general / fighting-style / epic-boon), every magic item with effects / charges — and snapshots each entry's wire status. `wired` means effects[] is populated and the engine derivations read them; `stub` means content has an entry but no effects yet (typically because expressing the feature needs engine work not yet done — Stunning Strike trigger, Reckless Attack timing, etc.). Current split: 29 wired class features / 17 stub. Asserts hard floors too (all six 2024 Fighting Styles ship; PHB's 15 canonical conditions all present; Rogue Sneak Attack scales at every odd level; every PHB-2024 mastery has at least one weapon in the pack). Doubles as the data-vs-code audit the standard names.
- **Public API contract test** (Layer 9). `tests/contract/exports.test.ts` snapshots the alphabetized list of runtime exports plus a parsed list of every source-level export from the barrel (catches type-only export changes the runtime view misses). `tests/contract/types.test.ts` uses Vitest's built-in `expectTypeOf` to lock the load-bearing signatures: `createEngine`, `Engine.apply` / `applyAll` / `replay`, `Engine.commit` / `undo` / `redo`, every `Engine.plan.*` return type (PlanResult vs ShieldOutcome / PolymorphOutcome / SimulacrumOutcome / WishOutcome / ConsumeGuidanceOutcome / SpendHeroPointOutcome), plan-intent `type` discriminants, branded-ID nominal typing, the `Event` discriminated union's known members, and `serializeCampaign`/`loadCampaign`'s `Campaign ↔ string` round-trip. Writing the test surfaced a real consistency gap and fixed it: the recent additions (`CleaveIntent`, `ShieldIntent` / `ShieldOutcome`, `PolymorphIntent` / `PolymorphOutcome`, `WildShapeIntent`, `SimulacrumIntent` / `SimulacrumOutcome`, `WishIntent` / `WishOutcome`, `ConsumeGuidanceIntent` / `ConsumeGuidanceOutcome`, `MistyStepIntent`, `ExpireSpellDurationsIntent`, `TickAuraIntent`, `ForcedMarchIntent`, `GrantInitialHeroPointsIntent`, `SpendHeroPointIntent` / `SpendHeroPointOutcome`, `CastSpellIntent`, `CheckConcentrationIntent`, `MoveIntent`, `DashIntent`, `DisengageIntent`) were reachable via `engine.plan.*` but their named types weren't re-exported from `src/index.ts`. Now they are.
- **`grittyRest` flag wired**: rest events carry `expectedDurationMinutes` (60/480 standard, 480/10080 gritty). The planner reads `state.settings.grittyRest` and stamps the right value.
- **`heroPoints` flag wired**: `Character.heroPoints` field, `HeroPointGranted` / `HeroPointSpent` events, `engine.plan.grantInitialHeroPoints` (5 + level − 1 per PC), `engine.plan.spendHeroPoint` (rolls a d6 the consumer adds to the augmented roll, decrements the pool).

### Fixed

- **Concentration ends on drop to 0 HP** (RAW 2024 PHB ch.7). All five damage-emitting planners (attack, falling, weapon-mastery, off-hand attack, cast-spell) now emit `ConcentrationBroken(reason='unconscious')` when damage drops a concentrating target to 0 HP. Magic Missile tracks per-target simulated HP so only the dart that actually drops the target emits the break.
- **Death save auto-roll at start of turn** (RAW 2024 PHB ch.1). `planAdvanceTurn` and `planBeginFirstTurn` emit `DeathSaveRolled` for any combatant whose turn starts while they're at 0 HP and not stable / not already at 3 failures. The reducer invariant for `DeathSaveRolled` was relaxed from `=== 0` to `<= 0` (the engine tracks overflow internally; rules-side anything ≤ 0 is "at 0 HP").

### Documentation

- **README "Known gaps"** updated as each gap closed — engine count 16 → 5, spell-coverage row updated, partial-status bullets reworded, Slice 41 roadmap entry rewritten.
- **docs/authoring-content-packs.md** SpellMechanic table now lists eight kinds (adds `hp-pool-knockout` and `aura-damage`).
- **docs/api-overview.md** planner lists updated with `mistyStep`, `shield`, `expireSpellDurations`, `consumeGuidance`, `cleave`, `tickAura`, `polymorph`, `wildShape`.

## 0.1.0-alpha.2 (2026-05-13)

Alpha refinement. A multi-round D&D-correctness audit surfaced a long list of bugs across the engine, the starter pack content, and the showcase; this release closes them. The README's status and roadmap sections are also rewritten to mark partial slices honestly instead of claiming uniform completeness.

### Added

- **Spell mechanic kinds**: `auto-hit` (Magic Missile-style spells with N darts, no save / no attack roll), `buff` (Bless / Mage Armor / Faerie Fire-style condition application with no save), `remove-condition` (Lesser Restoration-style targeted condition stripping).
- **Effect kind `GrantAdvantageToAttackers`** — when present on a character's effect stack, attacks against them have advantage. Used by Faerie Fire's condition and the Restrained status; the attack planner now consults the *target's* effect stack rather than only the attacker's.
- **`engine.plan.resurrect`** — new planner with proper validation: caster must know or prepare the spell, must have a slot of sufficient level, target must be at 0 HP. Supports `via: 'spell-slot' | 'scroll' | 'special'` so scroll consumption and special revivals can skip caster validation. Replaces consumer-side hand-emitted `CharacterResurrected` events.
- **`SaveRolled` / `AbilityCheckRolled` breakdown** — both events gain an optional `breakdown: Array<{source, value}>` field. The transcript formatter shows the breakdown when the flat bonus has two or more contributing sources, so a +0 save reads as "+2 CON-mod, -2 exhaustion" rather than an apparently missing modifier.
- **`Character.armorClass`** — optional natural-armor AC override. When set, `computeAC` uses it directly (effect modifiers still stack); used by creature statblocks and polymorph forms. Polymorph reducers copy `form.ac` into this field and the snapshot preserves the prior value for revert.
- **Heal mechanic `flatAmount`** — fixed-value heal supplement for spells like Aid (+5 per target).
- **`tests/unit/rules-truth.test.ts`** — ~58 short PHB 2024 assertions covering ability modifiers, proficiency bonus, single-class spell slots, AC under several armor scenarios, attack and save derivations, spell save DC and attack bonus, Sneak Attack dice at every odd Rogue level, and Blessed-condition propagation. Writing this surfaced the half/third caster slot bug.
- **`tests/unit/engine/spell-coverage.test.ts`** — per-shipped-spell smoke test. Each spell either has an explicit event-kind expectation or a `skip` with reason. Catches Magic-Missile-class bugs where a spell ships with no mechanical effect at all.
- **`Character.armorClass` + `CharacterResurrectedEvent.via`** event-shape fields propagated through schemas and reducers.

### Fixed

- **Fireball-class AoE damage rolled once** (PHB 2024 "Areas of Effect"). Previously `planSaveMechanic` rolled damage inside the per-target loop, so each target got an independent roll; now the dice roll once for the whole effect and each target's save determines full vs half.
- **Half / third-caster spell slots use round-up (RAW 2024)**. The previous engine used `floor(level/2)` (the 2014 multiclass rule); 2024 changed it to round-up and gave half-casters slots starting at level 1. Paladin 5 now correctly returns 4 first + 2 second slots (was 3 / 0). Eldritch Knight 4 returns 3 first-level slots (was 2).
- **Sneak Attack scales per Rogue level**. Starter pack previously shipped only the L1 entry (1d6); the class table now declares the feature at every odd level (1d6 -> 10d6 at L19), and the effect-stack dedupes class features by id so only the highest-level instance fires.
- **Creature natural-armor AC** honored via `Character.armorClass`. Ogre 11, Young Red Dragon 18, Goblin Scout 15, etc. Previously every creature defaulted to 10 + DEX.
- **Polymorph AC applies** in combat. Previously a polymorphed character kept their original AC; the form's AC is now copied onto the character.
- **Counterspell consumes the original caster's slot** (RAW 2024: the slot is still spent on a successful counter). `CounterspellIntent.originalSpellLevel` is now required.
- **Long rest ends concentration**. Sleep -> unconscious -> concentration breaks per RAW. The reducer also lifts conditions the broken effect had applied on other targets.
- **Many inert starter-pack spells now wired**: Magic Missile (5 darts at 3rd level), Bless (+2 to attack and all six saves via the new `blessed` condition), Bane (-2 via `baned`), Mage Armor (AC override via `mage-armored`), Faerie Fire (DEX save -> `faerie-fired` -> attackers get advantage), Aid (+5 HP flat heal per target), Web (DEX save -> `restrained`), Lesser Restoration (removes one of: blinded / deafened / paralyzed / poisoned), Polymorph (now in the content pack so slot consumption reads).
- **Dragon weapon dice**. The showcase dragon used a longsword stand-in for both bite and claws (1d8 slashing). Added `dragon-bite` (2d10 piercing) and `dragon-claw` (2d6 slashing) creature-statblock items.
- **Ogre weapon dice**. The "greatclub" in the showcase was secretly a greatsword (2d6 slashing). Added a proper PC `greatclub` (1d8 bludgeoning) and a creature `ogre-greatclub` (2d8 bludgeoning).
- **Restrained condition** gains `GrantAdvantageToAttackers` (the RAW status was missing the "attackers have advantage" leg).
- **Transcript formatter improvements**: damage events show source attribution; sized markdown headings with blank-line paragraph breaks so the rendered preview shows one action per line; creatures and NPCs no longer render as "fighter 1"; spell slots use ordinal labels; polymorph displays the caster.

### Documentation

- **README Status section** rewritten with a three-bucket split (fully wired / partially wired / known engine gaps), an honest 563-tests / 90-files count, and explicit notes on what alpha consumers can rely on versus author.
- **README Roadmap** uses a new ✓/◐/blank legend; every ◐ entry names what's missing. Phase C is "7 fully wired + 3 partial", Phase D is "6 + 1", Phase E is "2 + 7" with class L2+ features, subclasses, the full spell catalog, the DMG / MM catalogs, and variant-rule enforcement all flagged as content-layer gaps.
- **docs/getting-started.md** content-pack description updated to match the recalibrated story; points at the README Status section.

### Internal

- Stale `dnd-engine` references cleaned out of DEVELOPMENT.md, README.md, CLAUDE.md, and the bug-report issue template (the historical CHANGELOG note remains).

## 0.1.0-alpha.1 (2026-05-12)

Patch release. Documentation and attribution only; no API changes.

### Added

- `NOTICE` file with full CC BY 4.0 attribution for SRD 5.2-derived starter-pack content. Shipped in the npm tarball alongside `LICENSE`.
- `docs/content-attribution.md`: item-by-item audit of the starter content pack against SRD 5.2, flagging confidently-covered material, likely-covered items worth verifying for commercial use, and not-confirmed items (Bastions, Epic Boons).
- `ContentPack` schema extended with optional `license`, `attribution`, and `derivedFrom` metadata fields. The starter pack carries `license: "CC-BY-4.0"` and a full attribution string.
- npm package now also ships under name `ttrpg-engine-dnd` (renamed from `dnd-engine` because the original name was an unpublished squat on npm). GitHub repo renamed to match.

### Changed

- `LICENSE` restructured to explain dual licensing: engine code MIT, starter content pack CC BY 4.0.
- README "Intellectual property" section rewritten to point at the new attribution surface; Documentation table gains a row for IP / attribution.

## 0.1.0-alpha.0 (2026-05-12)

First alpha. All six phases of the roadmap are complete except the optional `ttrpg-engine-core` extraction. Forty-six slices shipped across engine mechanics, state schemas, combat fill-in, adoption surface, and 2024 content.

### Engine architecture (Phase A, Slices 1-16)

- Event-sourced state machine. `apply(state, event) -> state` is pure; `replay(events)` reconstructs state byte-for-byte.
- Plan/commit split: all RNG is consumed inside `engine.plan.*` planners; resolution events carry baked rolls. `apply()` never touches RNG. Tested via `ThrowOnCallRNG` on every golden scenario.
- Branded IDs (`CharacterId`, `EncounterId`, etc.) backed by ULIDs. Normalized state. Immer internally, immutable externally.
- Twenty-five effect primitives plus a `CustomEffect` code-handler hook.
- `PendingChoice` protocol for deferred player decisions.
- Combat resolution chain (`AttackDeclared` -> `AttackRolled` -> `DamageRolled` -> `DamageApplied`), full encounter lifecycle, level-up flow with HP rolls, ability checks and saves, spellcasting with cantrip scaling and ritual casting and area shapes, concentration enforcement, OnEvent triggers (Sneak Attack), action economy (Action Surge, Extra Attack, Bonus Action), reactions (opportunity attacks, off-hand strikes, flat damage reduction), movement and positioning, damage mitigation order of operations, inventory equip/attune with 3-slot cap, creature as combatant with multiattack, environmental hazards (falling, cover), all 15 2024 conditions.

### State schemas (Phase B, Slices 17-20)

- Parties + shared inventory + currency + treasure ledger.
- Sessions + journal entries (player / DM / character visibility) + in-game clock formatted as `Day NN HH:MM`.
- Locations + maps (normal / difficult / impassable / water cells) + doors (open / closed / locked) + Bresenham line-of-sight and line-of-effect.
- Quests + objectives + rewards + milestone XP.

### Combat fill-in (Phase C, Slices 21-30)

- Grapple, shove, hide (2024 unarmed save-DC model).
- Counterspell, Dispel Magic, Identify.
- Weapon Mastery effects: Sap, Vex, Slow, Topple, Push, Graze.
- Mounts and vehicles (land / water / air, with HP, AC, capacity, occupants).
- Travel + forage + navigation + forced-march exhaustion.
- NPCs as first-class with attitude (hostile / unfriendly / indifferent / friendly / helpful) and morale.
- Downtime + crafting + training + tool proficiency tracking.
- Magic item charges + recharge cadence + sentient items.
- Resurrection variants: Revivify, Raise Dead, Reincarnate, Resurrection, True Resurrection.
- Wild Shape, Polymorph, Simulacrum, Wish.

### Adoption surface (Phase D, Slices 31-37)

- Starter content pack bundled with the package. `loadStarterPack()` returns a fully-populated `ContentPack`.
- `examples/` directory with three runnable apps: character-sheet printer, encounter + replay demo, save/load round-trip. CI executes them to catch public-API regressions.
- Getting-started doc and API reference under `docs/`.
- Public API conveniences: `engine.do(campaign, intent)`, `serializeCampaign(c)` / `loadCampaign(json)`, `createPC({...})`.
- Per-engine derivation memoization keyed on `CampaignState.version`. Repeated `engine.derive.*` calls return the same object reference until the next commit.
- npm publish prep. Package ships ESM + CJS + `.d.ts`. `prepublishOnly` runs the full CI gate. `publishConfig: public`.
- Content pack validator with path-pointed Zod failures and Levenshtein "Did you mean X?" suggestions on cross-reference errors.

### 2024 content (Phase E, Slices 38-46)

- All 12 PHB classes in the starter pack with 1-20 level tables and signature features at landmark levels.
- ~31 spells across cantrip + level 1-3 tiers covering the common archetypes.
- 7 species, 8 backgrounds, 22 feats (origin + general + all six fighting styles), 25+ items including armor variety, 5 tools, 7 adventuring-gear items.
- 9 magic items across all rarity tiers (common Bag of Holding through legendary Deck of Many Things) including a charged Wand of Magic Missiles.
- 6 monster statblocks (Goblin, Orc, Wolf, Skeleton, Ogre, Young Red Dragon).
- 2024 Bastion stronghold system: facilities, hirelings, turn orders, damage and level-up.
- 9 epic boons under the `epic-boon` feat category.
- Variant rule toggles on `CampaignState.settings`: gritty rest, hero points, sanity, mass combat, custom houserules.

### Testing

- 475 tests across 87 files. Replay-equivalence and RNG-capture invariants asserted on every golden scenario. 80% line coverage gate on `src/engine/`, `src/derive/`, `src/effects/`.
- Showcase scenario "The Stoneheart Saga" exercises 46 distinct mechanical surfaces in one coherent campaign narrative.

### Not yet done

- Phase F: optional `ttrpg-engine-core` extraction for multi-system support.
- Subclass mechanics for individual classes (the level tables ship; specific subclass features are consumer territory).
- The full ~370 spell catalog (representative sample ships; bulk content is for consumers to fill out from the 2024 SRD).
- Specific scenario content (specific NPCs, adventures, named magic items beyond the starter set).
