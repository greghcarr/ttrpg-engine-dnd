# Changelog archive: slices 48-171 bulk-narrative rollup (part A — Spell catalog + Class features + Engine primitives + early audits)

Originally in the `## Unreleased` section of [../../CHANGELOG.md](../../CHANGELOG.md), under `### Added`. Moved here in slice 248 to keep the live CHANGELOG under the single-Read ceiling.

This window covers the first half of the bulk-narrative rollup of slices 48-171 — the post-alpha.5 vocabulary expansion era. It is structured as broad initiative rollups ("Spell catalog completion", "Class-feature fill-out", "Engine primitives") plus specific per-slice notes from the late-100s and early-160s slices (drift fixes for Chill Touch / Poison Spray / Black Tentacles / Hypnotic Pattern / Inflict Wounds / False Life, plus monster-data refresh slices).

For the second half of this rollup (slices 48-150 era, more monster audits + earliest content), see [archive-rollup-narrative-B.md](archive-rollup-narrative-B.md). For the slice-numbered entries above this window, see [archive-slices-172-176.md](archive-slices-172-176.md).

---


8 uncommon wondrous items added to the starter pack: Goggles of Night, Cloak of Elvenkind, Slippers of Spider Climbing, Brooch of Shielding, Periapt of Wound Closure, Ring of Swimming, Eyes of the Eagle, Hat of Disguise. All ship as `itemKind: 'magic'` with `effects: []` (schema-only, matching the existing wondrous-item pattern); the engine primitives each would eventually need are catalogued in [docs/starter-pack-gaps.md](../../docs/starter-pack-gaps.md) under "Deferred mechanical wiring." Magic-item count in the gaps doc bumped from 9 to 17.

The post-alpha.5 stretch: 75 slices. Slices 48–122 mix vocabulary expansion with infrastructure hygiene. The architectural skeleton is untouched. `SCHEMA_VERSION` unchanged; every new event shape and effect kind is additive. Test count grew from 1009 to 1371 across 192 files (+362 / +53); all replay-equivalence and RNG-capture invariants still hold.

The session cadence settled into a "primitive + canonical content user" shape: each slice adds a focused engine primitive (~50–200 LoC) and the first one or two RAW spells / features that exercise it, walking those entries from schema-only to wired in [docs/starter-pack-gaps.md](../../docs/starter-pack-gaps.md).

### Added

**Spell catalog completion** (10 sub-slices): every PHB 2024 spell now ships in the starter pack across L0–L9 (399 total, up from ~33). Per-level: 34 / 60 / 63 / 54 / 40 / 46 / 38 / 24 / 19 / 21. Mechanically-wired count grew from ~26 to ~152.

**Class-feature fill-out** (14 sub-slices): all 12 classes ship full L1–L20 feature tables. The class-feature matrix is fully wired through L7 across all classes; later tiers ship features at every grant level with effects wired where the primitive vocabulary covers them. Slices also wired specific subclass features: Cleric Blessed Strikes (Divine Strike at L7+), Paladin Aura Improvements (L18).

**Engine primitives** (~35 sub-slices). Grouped by category:

*Summon system* (slice 48). New `CompanionSummoned` / `CompanionDismissed` events plus a `summon` `SpellMechanic` kind carrying inline statblock (name, AC, hpBase, hpPerSlotAbove, baseSlotLevel, speedFeet). Companions land as `kind: 'creature'` Characters with a `summonSource` pointer to controller / spell / slot / effect. Concentration auto-dismiss via `clearConcentrationEffect`. 11 spells wired across the summon family (find-familiar, unseen-servant, find-steed, summon-beast, animate-dead, conjure-animals, phantom-steed, summon-fey / lesser-demons / shadowspawn / undead, and others).

*On-hit trigger system* (slice 61). New `consumeOnTrigger: boolean` field on `OnEvent` effects (one-shot riders lift their parent condition after firing). Smite cohort wired: divine-favor, searing-smite, wrathful-smite, thunderous-smite, branding-smite.

*Aura primitive* (slice 63) plus *source-relative formulas* (slice 64). New `GrantAura { auraId, rangeFeet, allyConditionId }` effect: discoverable metadata for consumers (dndbnb / VTTs) to project a condition onto in-range allies; the engine doesn't auto-project (position is consumer territory). The `sourceAbilityMod` Formula kind plus the threading needed to evaluate Formula values inside `AddModifier` effects: `EffectAccumulator` now folds Formula values into numeric modifiers when a `FormulaContext` is provided. Paladin Aura of Protection (L6, +CHA-mod-of-source to ally saves), Aura of Courage (L10, Frightened immunity), Crusader's Mantle (L3 spell) all wire.

*Condition-immunity gate* (slice 66). New `isImmuneToCondition(state, content, targetId, conditionId)` derive helper consulted by the spell planner's save (`conditionOnFail`) and buff branches before staging `ConditionApplied` events. Aura of Courage now actually blocks Frightened.

*Area-effect via aura-damage* (slices 68 / 70 / 71 / 72). The `aura-damage` SpellMechanic was extended four times: optional damage + conditionOnFail (slice 68); optional saveAbility (slice 70 — Cloud of Daggers no-save zone); `trigger` tag for multi-component zones (slice 71 — Hunger of Hadar's 2d6 cold on enter + 2d6 acid on turn end). A sibling `movement-damage` mechanic (slice 72) for per-foot-moved damage (Spike Growth) with a dedicated `engine.plan.tickMovementDamage` planner. 12 zone spells wired.

*Composite-buff confirmation* (slice 73, content). Conditions can carry multiple effects in their `effects` array. Haste, blur, pass-without-trace ship as composite-buff conditions with no engine change.

*AC-buff condition* (slice 74). New `SetACFloor { value }` effect kind, `acFloors` collector on EffectAccumulator, floor check in `computeAC` that bumps the natural total. Shield of Faith and Barkskin wire.

*Temp-HP grant primitive* (slice 75). New `temp-hp` SpellMechanic kind plus a planner that rolls dice + flat + slot-scaling and emits `TempHPGranted`. False Life wires.

*Item-buff primitive* (slices 76 / 90). New `ItemInstance.temporaryBuff` (attackBonus, damageBonus, sourceEffectInstanceId, source) plus `ItemBuffApplied` / `ItemBuffRemoved` events and reducers. `engine.plan.magicWeapon` drives Magic Weapon. Slice 90 extended the shape with `extraDamageDice` + `extraDamageType` for Elemental Weapon's per-hit elemental rider; the attack planner rolls the extra dice on hit (doubled on crit) and emits a second damage component. `clearConcentrationEffect` walks item instances and strips any buff tagged with the dropped effect's id.

*Movement-mode retrofit* (slice 77). `getEffectiveSpeed` retrofitted so `ModifySpeed` entries actually affect the engine's move planner. Fly + Spider Climb wire as buff conditions. Retrofits also Fast Movement, Unarmored Movement, Roving, Haste, and the Grappled/Restrained/Paralyzed/Petrified/Unconscious zero-speed conditions.

*Push primitive* (slice 78). New `CreaturePushed` informational event (engine doesn't model positions; consumer applies the position change). Save mechanic gains optional `pushedFeetOnFail`. Gust of Wind + Earthbind wire.

*Recurring-rider primitive* (slice 79). New `recurring` SpellMechanic kind (effect: temp-hp / heal / damage, optional dice / flat / addCasterAbilityMod / damageType). New `engine.plan.tickRecurring({casterId, targetId})` planner. Heroism wires the two-part shape (cast-time buff + per-tick TempHPGranted).

*Falling-protection primitive* (slice 81). New `GrantFallingProtection` Effect kind. `engine.plan.falling` short-circuits to no events when present. Feather Fall wires as a buff that lasts 1 minute.

*Caster-chosen options at cast time* (slices 82–87). New `CasterChoice` discriminated union on `CastSpellIntent` (`{kind: 'damageType', value}` and `{kind: 'variant', value}`). Three primitive variants:
  - `casterChoosesDamageType: { allowed: DamageType[] }` on attack mechanics (Chromatic Orb).
  - `casterChoosesVariant: { variants: [{ key, conditionId }] }` on buff mechanics (Enlarge/Reduce) and save mechanics (Calm Emotions).
  - Command (5-variant save), Enhance Ability (6-variant buff), Bestow Curse (4-variant save) all wire as pure data on top of the primitives. All four of Bestow Curse's variants are subsequently wired by later slices (88 extra-damage, 92 inactive-turn, 96 attack-disadvantage); the ability-disadvantage variant remains narrative-only pending a nested-sub-choice primitive.

*Target-side source-filtered on-hit rider* (slice 88). `AppliedCondition` provenance threaded through `dispatchTriggers`; new `event.attackerIsSource` fact lets a target-side rider filter on "the creature attacking me is this condition's source." Hex's recurring on-hit rider wires; Bestow Curse's extra-damage variant retrofits.

*Spirit Shroud variants* (slice 89). Caster-chosen 3-variant buff (cold / necrotic / radiant). Each variant carries an OnEvent rider that adds +1d8 of the chosen damage type on the caster's weapon hits.

*Dedicated reaction planner pattern* (slice 91). New `engine.plan.absorbElements({casterId, slotLevel, damageType, triggeringDamageEventId, halvedAmount})`. Compensating Healed event for the absorbed half (event-sourcing-friendly: doesn't mutate the triggering DamageApplied). Per-element charged condition (`absorb-elements-charged-acid-active` and four siblings) with `consumeOnTrigger` OnEvent rider for the on-next-hit elemental damage rider.

*Recurring-save primitive* (slices 92 / 93). New `recurringSave` field on `ConditionSchema` (ability + trigger + onFail + onSuccess). `engine.plan.tickRecurringSave({targetId, conditionId})` rolls a save against the source caster's spell DC. `onFail: 'consumeAction'` wires Bestow Curse's inactive-turn variant. `onSuccess: 'removeCondition'` wires Hold Person, Hold Monster, Tasha's Hideous Laughter, Confusion (the RAW "spell ends on the target" branch).

*Trap mechanic* (slice 94). New `TrapSchema` runtime collection on `CampaignState` (id, label, sourceCharacterId, sourceSpellId, payload, chargesRemaining). Three events: `TrapArmed` (stamps into state.traps), `TrapTriggered` (decrements charges), `TrapExpired` (removes from state, with chargesExhausted / dispelled / duration reasons). New `trap` SpellMechanic; new `engine.plan.triggerTrap({trapId, triggeringCharacterId})`. Glyph of Warding (Explosive Runes variant) + Cordon of Arrows wire.

*Resistance-buff confirmation* (slice 95, content). The engine already supported per-element `GrantResistance`; Protection from Energy ships as a 5-variant buff. No engine change.

*Directional attack disadvantage* (slice 96) plus *generic attacker-side advantage wiring* (slice 97). New `SetAdvantageVsSource` Effect kind: filters on the roll's target equaling the bearing applied condition's `sourceCharacterId`. BuilderContext gains an optional `sourceCharacterId`. EffectAccumulator gains `setAdvantageVsSource` / `advantageVsSource`. Attack planner consults `attackerEffects.advantageVsSource('attack', targetId)`. Bestow Curse's attack-disadvantage variant wires. Slice 97 closes a latent bug: the existing six conditions (blinded, frightened, poisoned, prone, restrained giving disadvantage, invisible giving advantage) already carried `SetAdvantage on:'attack'` effects but the attack planner never consulted `attackerEffects.advantageFor('attack')`. Now it does.

*Heal-blocking primitive* + *ApplyCondition TriggerAction dispatch* (slice 98). New `BlockHealing` Effect kind, accumulator query, and `isHealingBlocked` derive helper. `planHealMechanic` consults the helper; when blocked, emits Healed with `amount: 0` and `(blocked)` annotation in `source`. ApplyCondition TriggerAction handler added to `fireTrigger` (previously only AddDamage was wired). Spirit Shroud's three variants gain a second TriggerAction applying `healing-blocked-active` to hit targets.

*AddDamageToAttacker TriggerAction* (slice 100). Retaliation variant of AddDamage: dispatcher routes the resulting DamageApplied to `event.attackerId`. Crits don't double the retaliation dice per RAW. Fire Shield wires as a 2-variant buff (warm: cold resistance + fire retaliation; chill: fire resistance + cold retaliation).

*Multi-effect buff content sweep* (slice 101). Three high-level buff spells wired through existing primitives, no engine change. Foresight (L9) ships as a 14-effect condition: per-ability SetAdvantage on each save / check plus SetAdvantage on attack plus ImposeDisadvantageOnAttackers; the RAW "can't be surprised" clause stays narrative since surprise is initiative-order state, not a modeled condition. Mind Blank (L8) ships as a 2-effect condition: GrantConditionImmunity charmed plus GrantImmunity psychic; the thought-reading / scrying-detection clauses stay narrative. Invulnerability (L9) ships as a 1-effect condition: GrantImmunity damageType `'all'`. Wired spell count goes from ~144 to ~147 (~252 schema-only remain).

*Auto-expiry for trigger-applied conditions* (slice 102). Closes the loose end slice 98 left: `durationRounds` on an `ApplyCondition` TriggerAction was declarative metadata the engine never consumed. Now `fireApplyCondition` (inside an active encounter) stamps `expiresOnRound = currentRound + durationRounds` on the emitted ConditionApplied, and `planAdvanceTurn` sweeps every character's `appliedConditions` on each turn-start, emitting `ConditionRemoved` for any whose `sourceCharacterId` matches the new active combatant and whose `expiresOnRound` is in the past. Spirit Shroud's heal-block now auto-expires at the start of the caster's next turn per RAW. Outside an encounter the expiry stays consumer-managed (currentRound is undefined). Pays forward for every future trigger-applied condition that ships with a `durationRounds`.

*Type-conditional buff / ward primitive* (slice 103). Three pieces. (1) The existing `condition?: Predicate` field on `ImposeDisadvantageOnAttackers` was previously dropped at builder time; the accumulator now stores per-entry predicates and `imposesDisadvantageOnAttackers(attackerFacts)` filters them against caller-supplied facts. (2) New `getCreatureType(character, content)` derive helper resolves creature type from `statblockId` (monsters) → `speciesId` (PCs / NPCs) → default `Humanoid`. (3) New `ApplyConditionToAttacker` TriggerAction variant: retargets the emitted ConditionApplied to the attacker of the triggering AttackRolled (the conditions analog of `AddDamageToAttacker`). The attack planner threads the attacker's creature type into the disadvantage query, and `dispatchTriggers.buildEventFacts` now stamps `event.attackerCreatureType` / `event.targetCreatureType` on AttackRolled events so OnEvent filters can gate on type. Canonical content users: Protection from Evil and Good (L1) and Magic Circle (L3) ship the type-gated ImposeDisadvantageOnAttackers wired against the six warded types (aberration, celestial, elemental, fey, fiend, undead); Holy Aura (L8) ships a multi-effect concentration buff combining per-ability save advantage, universal ImposeDisadvantageOnAttackers, and an OnEvent rider with `event.attackerCreatureType in ['Fiend', 'Undead']` emitting `ApplyConditionToAttacker { blinded }`. PfEoG's RAW source-gated condition-immunity arm (charmed / frightened only from the six types) stays narrative-only pending a source-predicate on GrantConditionImmunity. Magic Circle's RAW caster-choice-of-type isn't modeled (broader than RAW: applies against all six).

*Source-predicate on GrantConditionImmunity* (slice 104). Closes slice 103's loose end: PfEoG's RAW charmed / frightened immunity arms now actually gate. `GrantConditionImmunity` gains an optional `condition?: Predicate` field; the EffectAccumulator stores per-conditionId lists of `{predicate?}` entries (rather than the prior plain Set), and `hasConditionImmunity(id, sourceFacts?)` filters them against caller-supplied source facts. `isImmuneToCondition` gains an optional `sourceCharacterId`; when supplied, the helper builds a `sourceCreatureType` fact from the source's record via `getCreatureType` and threads it down. All three call sites pass it: the cast-spell save and buff branches use `intent.characterId`; the concentration aura tick uses `intent.casterId`. Protection from Evil and Good and Magic Circle ship the matching source-gated GrantConditionImmunity entries for charmed and frightened. PfEoG's RAW possessed clause stays narrative (the engine has no `possessed` condition). Unfiltered immunity entries (Aura of Courage's Frightened immunity, Freedom of Movement's Paralyzed immunity, etc.) resolve identically to the old behavior — predicate-free entries match unconditionally.

*Source-relative formula threading across derive surfaces* (slice 105). Floor-raising. Slice 64 introduced the `sourceAbilityMod` Formula kind and threaded `characters` through `computeSavingThrow`; the other derive surfaces ignored the field, so a hypothetical source-relative `AddModifier` on an ability check / AC / attack / spell DC / action economy / damage mitigation entry silently resolved to 0. This slice threads `characters` through every remaining derive input shape (`ComputeAbilityCheckInput`, `ComputeACInput`, `ComputeAttackInput`, `ComputeSpellDCInput`, `ComputeActionEconomyInput`, `MitigateDamageInput`, `ComputeDerivedCharacterInput`), updates the `engine.derive.*` consumer surface to pass `state.characters`, and updates ~25 planner-side call sites (`attack`, `cast-spell`, `concentration`, `offhand-attack`, `falling`, `recurring-save`, `reactive-spells`, `transformations`, `stunning-strike`, `checks`, `travel`) to pass through. No PHB content uses source-relative formulas outside saves today, so verification rides on a hand-built test condition in [tests/unit/derive/source-relative-formulas.test.ts](../../tests/unit/derive/source-relative-formulas.test.ts); future content that uses the shape (e.g., a hypothetical "+CHA-mod-of-source to one ability check" buff) wires for free across every derive surface.

*Utility-buff content sweep* (slice 106). Four utility spells walked from schema-only to wired via existing primitives, no engine change. Blade Ward (L0) ships as a 3-effect condition (GrantResistance for bludgeoning / piercing / slashing). Resistance (L0) ships as a Guidance-shaped narrative buff (empty-effect `resisted` condition; consumer rolls the 1d4 and removes). Longstrider (L1) ships as a `ModifySpeed walk add 10` buff. Darkvision (L2) ships as a `GrantSense darkvision 60` buff. Wired spell count ~150 → ~154; conditions 31 → 35.

*Sanctuary (dedicated reaction-style planner)* (slice 107). Pre-attack target-selection rider — different shape from the bearer-side reactions (Shield / Counterspell / Absorb Elements). New `SanctuaryProtected` event + `engine.plan.sanctuaryWardSave({ attackerId, wardedCharacterId })` planner: looks up the warded creature's `sanctuary-active` applied condition + its source caster, rolls the attacker's WIS save against the caster's spell DC via `computeSavingThrow` + a fresh d20 (with advantage / disadvantage threaded from the attacker's effect stack), emits `SaveRolled`, and on failure emits `SanctuaryProtected` (record-only). The outcome exposes `prevented: boolean` for consumer branching; on a failed save the consumer drops the attack. The `sanctuary-active` condition carries an OnEvent rider with `consumeOnTrigger: true` and filter `event.attackerIsSelf === true`, so the spell ends when the warded creature makes any attack roll per RAW (the "casts a harmful spell / deals damage" RAW vectors aren't modeled — no `castDeclared-with-hostile-target` or `dealtDamage` facts today). Sanctuary becomes the 5th wired reaction spell (after counterspell / shield / absorb-elements / guidance via consume-d4).

*On-miss trigger primitive* (slice 108). Two small pieces. (1) New `event.targetIsSource` fact in `dispatchTriggers.buildEventFacts` — mirror of the slice-88 `event.attackerIsSource` fact, true when the bearing AppliedCondition's `sourceCharacterId` equals the event's target. (2) New `sourceFromEventTarget?: boolean` flag on `ApplyConditionToAttacker`: when true, the emitted ConditionApplied stamps `sourceCharacterId = event.targetId` (the missed creature) instead of the rider's bearer. Fighter L13 Studied Attacks ships as the canonical user: an OnEvent rider on the class feature (`event.attackerIsSelf === true && event.hit === false`) emits the new action with `studied-target-active`. The condition pairs `SetAdvantageVsSource(on: 'attack', advantage)` with a consume-on-next-attack OnEvent rider (`attackerIsSelf && targetIsSource`, consumeOnTrigger) that lifts after the fighter's next attack against the studied creature. RAW "before end of your next turn" time-window expiry isn't modeled (consumer manages cleanup at end of turn); consecutive misses against the same target don't refresh because ConditionApplied dedupes by conditionId while the consume rider still fires.

*Turn-end auto-expiry primitive* (slice 109). Extends slice 102's `expiresOnRound` model with a turn-end branch for "until end of your next turn" / "1 round" durations. New declarative `autoExpiry?: { afterRounds, trigger }` field on ConditionSchema; new `expiryTrigger?: 'turnStart' | 'turnEnd'` on ConditionApplied event + AppliedCondition runtime (default 'turnStart' for backwards compat with slice 102). The cast-spell buff branch reads the condition def's autoExpiry and stamps `expiresOnRound + expiryTrigger` on the emitted event when cast inside an active encounter. `planAdvanceTurn` gains a turn-end sweep that fires after `TurnEnded` for entries with `expiryTrigger: 'turnEnd'` and `sourceCharacterId == endingCombatant.id` (self-cast buffs match since source == bearer). The existing turn-start sweep skips turn-end entries so they don't double-fire. Blade Ward is the canonical user: `autoExpiry: { afterRounds: 1, trigger: 'turnEnd' }` on the `blade-warded-active` condition, so the 3-effect B/P/S resistance buff lifts at the end of the bearer's turn in the round after the cast per RAW.

*Concentration cleanup for rider-applied conditions* (slice 110). Closes a leak where OnEvent riders living inside a parent concentration condition emit ConditionApplied targeting *other* creatures (Spirit Shroud's heal-block on hit targets, Holy Aura's blinded on attacking fiends / undead) but `EffectInstance.conditionsApplied` only tracked the direct cast-time apply on the bearer. New `sourceEffectInstanceId?: ULID` on `ConditionAppliedEvent` and `AppliedCondition`. The dispatcher walks `state.effectInstances` to find the parent EffectInstance for whichever AppliedCondition fired the rider and stamps the id on emitted events. `clearConcentrationEffect` gains a second sweep across every character's appliedConditions that removes matching entries (reversing any `hpMaxBonusDelta` first, mirroring the direct-conditions loop). Non-concentration riders (Fighter Studied Attacks, etc.) leave the field undefined and behave unchanged.

*On-fatal-damage intercept primitive* (slice 111). New `PreventFatalDamage` marker Effect kind. When a condition carrying it lives on a creature and incoming damage would drop HP to 0 or below, the damage components are scaled proportionally so HP lands at 1 and the bearing condition is consumed via a paired ConditionRemoved event. Reducers are pure (cannot emit new events), so the intercept lives planner-side as `interceptFatalDamage` in [src/derive/fatal-damage-intercept.ts](../../src/derive/fatal-damage-intercept.ts). Every primary damage emitter — planAttack, planCastSpell (3 paths), planFalling, planOffhandAttack, planWeaponMastery Graze, planTrap, concentration aura damage, recurring damage — calls it after `mitigateDamage` and before constructing the DamageApplied event. Multi-event emitters feed an `applyAll(state, eventsSoFar)` snapshot to the helper so per-event intercepts see prior events' damage applied. Death Ward ships as the canonical user (`death-ward-active` condition + buff wiring on the spell). Initial limitation closed by slice 114: rider-emitted damage from the trigger dispatcher initially bypassed the intercept.

*Nonmagical-attack resistance qualifier* (slice 112). `GrantResistance` gains an optional `qualifier: 'nonmagical' | 'magical'` field. When set, the resistance check fires only if the incoming damage source matches. Plain (unqualified) resistances behave unchanged. EffectAccumulator's `resistances` Set becomes a `Map<DamageType, Array<{qualifier?}>>`, mirroring the slice-104 shape on `conditionImmunities`. `hasResistance(type, sourceIsMagical?)` applies any-match semantics across stacked entries. `mitigateDamage` gains a `sourceIsMagical?: boolean` input field; every emitter that goes through the mitigation pipeline now passes it. New [src/derive/magicality.ts](../../src/derive/magicality.ts) helper `isMagicWeaponAttack` reads `ItemInstance.temporaryBuff` and falls back to `ItemDefinition.itemKind === 'magic'`. Stoneskin ships in SRD form (`stoneskin-active` condition with B/P/S `GrantResistance` entries each qualified `nonmagical`). Same primitive unblocks the common MM "resistance to B/P/S from nonmagical attacks" monster trait for future statblock authoring.

*Route rider damage through mitigateDamage* (slice 113). Closes the limitation documented on slices 111 + 112: the trigger dispatcher's `fireAddDamage` and `fireAddDamageToAttacker` plus a couple of one-off emitters (weapon-mastery Graze, trap base damage) emitted raw DamageApplied events that bypassed the mitigation pipeline entirely. Resistance / immunity / vulnerability / flat reduction / the slice-112 magicality qualifier all silently failed for rider damage. Every damage emitter now runs mitigateDamage. For dispatcher riders the magicality is inferred per-rider via a new `isRiderMagical` helper: spell-sourced rider (bearing condition tracked by an EffectInstance with a spellId) counts as magical (Spirit Shroud, Hex, Crusader's Mantle, smite cohort, Fire Shield retaliation); AttackRolled rider with weaponInstanceId inherits the weapon's magicality (Sneak Attack on a magic longsword counts as magical); otherwise non-magical (conservative default).

*Rider-aware on-fatal-damage intercept* (slice 114). Closes slice 111's documented limitation: Death Ward did not fire when a dispatcher rider (Sneak Attack, smites, Spirit Shroud rider, Fire Shield retaliation) alone would drop the warded target. Two changes plug the gap. (1) `fireAddDamage` / `fireAddDamageToAttacker` now consult `interceptFatalDamage` on every rider DamageApplied; when the rider would drop the bearer, components scale so HP lands at 1 and a paired ConditionRemoved consumes the bearing condition. (2) `dispatchTriggers` advances a `runningState` via `applyAll` between fired triggers — cadence checks, appliedConditions, the intercept's HP guard, and the `consumeOnTrigger` parent-lookup all see post-prior-rider state. Closes a latent gap where a rider-consumed condition was still visible to later riders on the same character.

*Predicate-gated AddModifier* (slice 115). `AddModifier.condition?: Predicate` was being silently ignored by the effect builder. This slice honors the condition at modifier-sum time. Each `ModifierContribution` now optionally carries the predicate alongside its source + value; `EffectAccumulator.modifierSum` (and `modifierBreakdown`) accept a `facts?: ReadonlyMap<string, unknown>` and skip contributions whose predicate fails. Predicate-less modifiers continue to apply unconditionally, so all existing unconditional modifiers behave unchanged. Canonical user: Archery Fighting Style wired with `{ kind: 'eq', path: 'event.attackKind', value: 'ranged' }`. `computeAttackBonus` populates the `event.attackKind` fact from the weapon definition's `attackKind` property (already in content), so ranged-weapon attacks see the +2 and melee attacks don't. Same wiring on the Ranger L2 Fighting Style "archery" option. Same plumbing unblocks the rest of the Fighting Style gates as facts get populated.

*bearer.wearingArmor fact + Defense Fighting Style* (slice 116). Extends slice 115's predicate-gated `AddModifier` pattern to the AC derive path. `computeAC` builds a `bearer.wearingArmor` fact (true iff the character has an item equipped in the armor slot) and feeds it to both `modifierSum('ac', facts)` sites — the natural-armor branch and the regular armor branch. Defense's +1 AC ships across all four pack locations (standalone feat + Fighter / Paladin / Ranger L2 options) with `condition: { kind: 'eq', path: 'bearer.wearingArmor', value: true }`. RAW-correct. Same plumbing accommodates future `bearer.*` facts: `bearer.tempHpAbove` for Armor of Agathys, `bearer.notIncapacitated` for Mirror Image, `bearer.offHandKind` for Dueling.

*Dueling Fighting Style + Frenzy melee gate* (slice 117). Closes the third of the four Fighting Style gates and surfaces a latent miss: the `target: 'damage'` modifier sum was never being consumed by any planner. Five effects relied on it (4 Dueling entries + 1 Frenzy condition) and all were dormant. `planAttack` now consumes `attackerEffects.modifierSum('damage', facts)` into `damageRollPayload.modifier`. The facts map carries `event.attackKind` (from the weapon definition) and `bearer.offHandHasWeapon` (true iff the off-hand slot holds an item whose def is `itemKind: 'weapon'`; shields live in a separate slot). Dueling's +2 ships across all four pack locations with `condition: { kind: 'all', terms: [eq(event.attackKind, 'melee'), eq(bearer.offHandHasWeapon, false)] }`. The Frenzy condition (`frenzied`) gets the same melee predicate. Both effects now correctly apply.

*CI hygiene: Node 24 migration* (slice 118). GitHub flagged `actions/checkout@v4` and `actions/setup-node@v4` for Node 20 EOL on 2026-06-02. Both actions bumped to `@v5` (Node 24 runtime) across all three workflows: ci.yml, deploy-demo.yml, deploy-dndbnb.yml. CI test matrix updated from `[18, 20, 22]` to `[20, 22, 24]`: Node 18 reached EOL on 2025-04-30 (drop), Node 24 is the current line (add). package.json `engines.node` bumped from `>=18` to `>=20` to match. Deploy workflows stay on Node 22 (the LTS we target for production-bundle output); only the test matrix exercises the wider range.

*Two-Weapon Fighting Fighting Style* (slice 119). Closes the 4th and final Fighting Style gate (the FS track is fully wired). New `GrantTwoWeaponFighting` marker Effect kind — same shape as `GrantEvasion` / `GrantFallingProtection`: no fields, accumulator flag, planner queries via `hasTwoWeaponFighting()`. `planOffHandAttack` now builds an attacker effect stack and flips the damage-modifier rule when the flag is set: instead of stripping positive ability mods (RAW default for off-hand attacks), the full ability modifier lands on the damage roll. Wired across all three pack locations (standalone feat + Fighter L1 option + Ranger L2 option). Features-coverage snapshot refreshed for the new effect kind.

*Protection Fighting Style reaction planner* (slice 120). RAW 2024: "When a creature you can see attacks a target other than you that is within 5 feet of you, you can use your reaction to impose Disadvantage on the attack roll. You must be wielding a Shield." Closes 5 of 6 PHB 2024 Fighting Styles — only Great Weapon Fighting remains. New `GrantProtectionFightingStyle` marker Effect kind (same pattern as GrantTwoWeaponFighting). New `ProtectionUsed` record-only event (record protector / attacker / triggering attack id / fresh d20). New `engine.plan.protection({ protectorId, attackerId, triggeringAttackEventId })` reaction planner: validates the protector has the FS marker + has a shield equipped + hasn't used their reaction, rolls one fresh d20, and emits `ActionEconomyConsumed { kind: 'reaction' }` + `ProtectionUsed`. Position / vision gates ("ally within 5 ft", "attacker visible") stay consumer-side. The consumer pairs the new d20 with the original AttackRolled.d20 to compute the disadvantaged outcome (lower of the two). Wired across all three pack locations (standalone feat + Fighter L1 option + Paladin L2 option). Transcript formatter, apply.ts no-op reducer, features-coverage snapshot all refreshed.

*Great Weapon Fighting Fighting Style* (slice 121). Closes the 6th and final 2024-PHB Fighting Style — the entire Fighting Style track is now wired. RAW 2024: "When you roll damage for an attack you make with a Melee weapon that you are holding with two hands, you can treat any 1 or 2 on a damage die as a 3." New `GrantGreatWeaponFighting` marker Effect kind (same shape as the other FS markers). `planAttack` applies the substitution in place on the rolled damage dice when three gates pass: the attacker's effect stack carries the flag, `weaponDef.attackKind === 'melee'`, and the wield is two-handed (`weaponDef.properties` includes `two-handed`, or includes `versatile` AND both `attacker.equipped.offHand` and `attacker.equipped.shield` are undefined — stricter than the existing Flex `wieldedTwoHanded` because a shield occupies a hand even though the engine tracks it in a separate slot). RAW's "the weapon's damage dice" wording is honored: the slice-90 `extraDamageDice` rider (Elemental Weapon etc.) is NOT touched, only the weapon's native dice array. Wired across all three pack locations (standalone feat + Fighter L1 + Paladin L2). Features-coverage snapshot refreshed.

*Numeric-comparison predicates + Armor of Agathys* (slice 122). Two new predicate kinds (`gt` / `gte`) take a `path` and a numeric `value`; the evaluator looks up the fact at `path`, returns false when missing or non-numeric, otherwise compares. New `bearer.tempHp` fact populated by the trigger dispatcher's `buildEventFacts` (true for any character carrying an OnEvent rider — exposes their current temp HP for predicate use). New `armor-of-agathys` spell on the warlock list: cast emits a TempHPGranted (5 base + 5 per slot above 1) plus a ConditionApplied for `armor-of-agathys-active`. The condition's OnEvent rider fires on AttackRolled with filter `all(targetIsSelf, hit, bearer.tempHp gt 0)` and emits `AddDamageToAttacker { dice: '0d6+5', damageType: 'cold' }`. The dice parser's `count <= 0` reject was relaxed to `count < 0` so "0d6+5" parses as flat 5 — lets dice-shaped slots carry flat damage without a separate primitive. RAW gate "melee attack" subsequently closed by slice 123 (see below); temp-HP provenance still unmodeled (engine `hp.temp` is a single number, so the rider fires for any temp HP source, not just AoA's).

*`event.attackKind` fact, retaliation tightened to melee-only* (slice 123). New required `attackKind: 'melee' | 'ranged'` field on `AttackRolledEvent`, populated by `planAttack` and `planOffHandAttack` from `weaponDef.attackKind` and by `castSpell`'s attack mechanic as `'ranged'` (every currently-wired PHB 2024 spell attack is a ranged spell attack). Surfaced as the `event.attackKind` fact in `dispatchTriggers.buildEventFacts`. Armor of Agathys + Fire Shield (warm / chill) gain a fourth `eq event.attackKind melee` clause on their retaliation OnEvent rider filters — ranged attackers no longer trigger retaliation, matching RAW. Thrown weapons (dagger, handaxe) classify as melee since the engine doesn't model thrown-vs-melee-strike intent. Future ranged-only riders (Bracers of Archery etc.) read the same fact.

*Mirror Image attack-deflection pool* (slice 124). New `MirrorImageDeflectedEvent` + reducer that decrements the bearer's `mirror-image-active` condition `level` (3 → 2 → 1 → condition removed). New derive helper `findMirrorImage` (src/derive/mirror-image.ts) + thresholds (6 / 8 / 11 by duplicate count). `planAttack` and `planOffHandAttack` intercept incoming weapon attacks: roll the deflection d20 first, and on redirect-success the attack rolls against duplicate AC = 10 + bearer DEX mod. Deflected attacks emit `AttackRolled` with `hit: false` (so bearer-side retaliation riders don't double-fire) + the new event; duplicate-destroying hits at duplicates=0 also emit a follow-up `ConditionRemoved`. New optional `appliedConditionLevel?: number` on `SpellBuffMechanicSchema` lets a buff stamp an initial pool count at cast time (Mirror Image: 3). Vision-gate exemptions (blind / blindsight / truesight attackers) and the cast-spell attack-mechanic path (rare in practice — most attacks against the bearer are weapon attacks) are documented deferrals.

*Trigger dispatch on deflected Mirror Image attacks* (slice 125). Closes the slice-124 loose end: `tryBuildDeflectedAttack` now routes the deflected `AttackRolled` through `dispatchTriggers` so attacker-side on-miss / on-attack riders (Studied Attacks' consume-on-attack-vs-source, future similar) fire as RAW expects. Bearer-side hit-gated riders (AoA, Fire Shield) still don't fire because the event carries `hit: false`. Helper signature gains `state` and `content`; planAttack and planOffHandAttack thread them through. New test case in `plan-mirror-image.test.ts` seeds a fighter with `studied-target-active` sourced to a Mirror-Image-warded bearer and asserts the consume rider fires on the deflected attack.

*Narrow features-test snapshots to wired-only entries* (slice 126). Test-infrastructure refactor. The per-id catalog snapshots in `tests/coverage/features.test.ts` (class features, subclasses, conditions, feats, magic items) tripped on every stub addition, contradicting the parallel-authoring contract's "pure content additions shouldn't move this snapshot" claim. All five per-id snapshots are now narrowed to track only wired entries (effects-bearing). Pure stub additions no longer trip; wire-status flips, wired additions, and wired-entry removals still trip. The aggregate "wired vs stub" counts test for class features is dropped (its signal is implicit in the wired list's length). `docs/parallel-authoring.md` lines 50, 70, 102 updated to the truthful new contract. Monsters were never in any features.test.ts snapshot — the monster lane was always clean and stays clean.

*Vision senses on the effect stack + Mirror Image RAW bypass* (slice 127). The `GrantSense` effect kind was schema-only: the accumulator no-op'd it and downstream consumers had no way to ask "does this attacker see by blindsight / truesight / etc.?". This slice wires the primitive: `EffectAccumulator.grantSense(sense, range)` records grants, `hasSense(sense)` is a boolean, `senseRange(sense)` returns max range (RAW: overlapping grants take the larger range, not the sum). `applyEffectToBuilder` dispatches `GrantSense` to `grantSense`. Canonical user: Mirror Image's RAW vision-gate. `planAttack` and `planOffHandAttack` now compute `attackerBypassesMirrorImage = attackerEffects.hasSense('blindsight') || attackerEffects.hasSense('truesight') || attacker carries 'blinded'` and skip the deflection branch when true (PHB 2024: "A creature is unaffected by this spell if it can't see, if it relies on senses other than sight, or if it can perceive illusions as false."). Secondary canonical user: Boon of Truesight (epic-boon feat) flipped from `effects: []` to `[{ kind: 'GrantSense', sense: 'truesight', range: 60 }]`, exercising the primitive via `featsTaken`. Tests: two new `EffectAccumulator` units (sense propagation + larger-range-wins), two new Mirror Image planner cases (truesight attacker bypasses; blinded attacker bypasses). The features snapshot moved as expected (`boon-of-truesight` flipped from stub to wired in the per-id feats catalog), validating the slice-126 design intent. The Goggles of Night deferral note in [docs/starter-pack-gaps.md](../../docs/starter-pack-gaps.md) updated to reflect that the GrantSense primitive ships now and the remaining blocker is the magic-item-effects-projection gap, not GrantSense itself.

*Dedicated Thunder Step planner* (slice 128). `thunder-step` was schema-only because it doesn't fit any single SpellMechanic kind: it's a teleport (like Misty Step) plus an area damage on the origin square plus an optional second teleportee. New `engine.plan.thunderStep` adds the planner, modeled on Misty Step's skeleton (action-economy + slot consume + range / occupancy / known-spell validation) and extended with: (a) optional `ally: { combatantId, to }` argument that enforces the RAW "willing creature within 5 ft" proximity gate before the teleport and emits a tail-end `CombatantMoved` for the ally; (b) a 10-ft Chebyshev AoE around the caster's origin that excludes the caster and the ally; (c) one shared 3d10 thunder damage pool (`+1d10` per slot above 3rd) rolled once and applied to every affected creature (halved on a successful CON save against the caster's spell save DC, full on failure). Caster's destination and the ally's destination must each be unoccupied and within 90 ft of the origin; the two must not collide. Tests cover happy path (declared / slot / action / per-target save+damage / two moves), AoE exclusions (caster + ally untouched, out-of-range bystander untouched), no-ally short form, range / proximity / coincident-destination / slot-level / scaling rejections, and a seed-walked save half / fail full pair. `spell-coverage.test.ts` retagged the skip from "not yet implemented" to "has dedicated planThunderStep."

*Monster statblock data folds into the effect stack* (slice 129). Closes the longest-standing latent gap surfaced by the monsters-batch-2 merge handoff: `MonsterStatblock.traits`, `damageResistances`, `damageImmunities`, `damageVulnerabilities`, and `conditionImmunities` were populated by content but unread anywhere in `src/derive/` or `src/engine/`. Skeleton's bludgeoning vulnerability and Young Red Dragon's fire immunity had been inert since alpha.5. The fix is a single new `collectMonsterEffects` helper in [src/derive/effect-stack.ts](../../src/derive/effect-stack.ts) that fires when `character.statblockId` resolves to a content-pack monster: it synthesizes `GrantResistance` / `GrantImmunity` / `GrantVulnerability` / `GrantConditionImmunity` effects from the four scalar arrays and appends `traits[]` (already `EffectSchema[]`) verbatim, then `buildEffectStack` walks them through `applyEffectToBuilder` with `source: 'monster'`. `mitigateDamage` and `isImmuneToCondition` both already consume the accumulator, so the fold lights up automatically across every existing consumer with zero downstream changes. Eight unit tests on the stack itself plus three end-to-end tests through `mitigateDamage` verify the actual damage outputs: Skeleton takes 20 from 10 bludgeoning (doubled), Young Red Dragon takes 0 from 30 fire (immune), Imp takes 5 from 10 nonmagical slashing but a full 10 from magical slashing (qualifier-aware). The remaining monster-data gaps documented for follow-up: `savingThrows` proficiencies in `computeSavingThrow`, and the Magic Resistance primitive (needs an "all saves" `SetAdvantage.on` variant plus an `event.isMagical` dispatcher fact).

*Monster `savingThrows` wires into `computeSavingThrow`* (slice 130). Direct follow-on to slice 129: monsters declare a `savingThrows: Record<string, number>` map of listed save bonuses (e.g. Young Red Dragon DEX +4 / CON +9 / WIS +4 / CHA +8), which per RAW MM presentation is the baked total (ability mod + proficiency already included). `computeSavingThrow` now consults `content.monsters.get(character.statblockId)?.savingThrows?.[ability]` first when the character resolves to a content-pack monster; when present, the listed bonus becomes a single `monster:<id>:save` breakdown entry replacing the ability-mod + class-proficiency reconstruction. Effects, exhaustion, and advantage still layer on top. PCs (no statblockId) and monsters without a listed save for the rolled ability fall through to the existing path unchanged. Six new tests cover Young Red Dragon's four listed saves, an unlisted STR save falling through, Mage's INT / WIS bonuses, the breakdown shape (single monster entry, no ability-mod or proficiency lines), Skeleton's pass-through (no savingThrows declared), and the PC pass-through. The fast-path is bounded to listed entries by design: the slice doesn't address the pre-existing fixture quirk where creature characters attach a fighter-L1 enrollment that incorrectly grants STR + CON proficiency for unlisted saves (that's a fixture / character-modeling gap, not a derivation gap).

*Magic Resistance primitive* (slice 131). RAW "the creature has advantage on saving throws against spells and other magical effects" is canonical across most CR 5+ Fiends / Fey / Dragons / spellcaster monsters; the Imp and Quasit shipped in monsters batch 1.7 were waiting on it. New marker `Effect` kind `GrantMagicResistance` (no params), `EffectAccumulator.markMagicResistance()` / `hasMagicResistance()`, and a `sourceIsMagical?: boolean` parameter on `ComputeSaveInput`. `computeSavingThrow` contributes advantage to the save when both flags are set; advantage / disadvantage cancellation against other sources still applies. `planCastSpell`'s save mechanic passes `sourceIsMagical: true` and now honors `hasAdvantage` / `hasDisadvantage` by rolling 2d20 take-max / take-min (pre-slice 131 it always rolled one d20, silently ignoring effect-stack save-advantage signals). Imp and Quasit traits gained `{ kind: 'GrantMagicResistance' }`. Tests: one accumulator unit, five new `computeSavingThrow` cases (Imp + sourceIsMagical → advantage, Imp + non-magical → no advantage, Imp + undefined → no advantage, PC + magical without trait → no advantage, Quasit + magical → advantage), and three cast-spell end-to-end cases (Fireball at Imp emits `used: 'advantage'` with two d20 rolls, Fireball at plain fighter stays single-d20 / `used: 'none'`, Fireball at mixed targets produces correct per-target shape). The other save planners (trap, recurring-save, reactive-spells, stunning-strike) don't yet pass `sourceIsMagical`; cast-spell is the canonical magical-save path and one-line follow-ups can extend the others as the content needs.

*Magic-item passive effects projection pass* (slice 132). The items lane named this gap as the single biggest blocker across ~20 wondrous items they authored in items batches 1.1 through 1.6. Pre-slice 132, `collectItemEffects` walked only `character.equipped.attuned`, so items that don't require attunement (Bag of Holding, Goggles of Night, Ring of Swimming, Bracers of Archery, Gloves of Swimming and Climbing, Stone of Good Luck, etc.) never reached the wearer's effect stack from inventory. The fix: extend the walk to iterate `character.inventory` too, gated on `def.requiresAttunement === false`, with a `Set` for dedup so an attuned item present in both lists doesn't double-fold. Items requiring attunement still only project via the existing attuned path. Canonical users wired in the same slice as the proof: Goggles of Night gains `{ kind: 'GrantSense', sense: 'darkvision', range: 60 }` and Ring of Swimming gains `{ kind: 'ModifySpeed', mode: 'swim', op: 'set', value: 40 }`. Both project from inventory without attunement, matching their RAW "you have darkvision while wearing these" and "your swim speed becomes 40 ft" shapes. Six new unit tests cover non-attunement projection, attunement-required gating, the existing attuned path (regression), dedup across inventory + attuned, non-magic items pass through, and the negative no-darkvision case for unattuned attunement-required items. The features-test snapshot moved (slice-126 design intent: 0 → 2 entries in `wiredIds`), validating the wire as a positive-signal event. The remaining ~18 wondrous items in the pack are now content-authoring follow-ups (the engine projection path is in place; what's left is wiring each item's `effects` array). Gaps doc updated to reclassify those items from "blocked on projection" to "content-authoring follow-up."

*sourceIsMagical rollout to non-cast-spell magical save planners* (slice 133). Slice 131 wired `sourceIsMagical` into `computeSavingThrow` plus the cast-spell save mechanic, but left three other magical-save planners untouched: `planTriggerTrap` (spell-armed traps: Glyph of Warding's Explosive Runes, Cordon of Arrows), `planTickRecurringSave` (recurring saves against spell-applied conditions: Hold Person's `held-paralyzed-active`, Hold Monster's `held-monster-active`, Hideous Laughter, Confusion, Bestow Curse's `cursed-inactive-active`), and `planSanctuaryWardSave` (Sanctuary's pre-attack WIS save). All three now pass `sourceIsMagical: true`. `planTriggerTrap` and `planTickRecurringSave` also gained 2d20-take-max / take-min support (they previously hard-coded single-d20 rolls and ignored `hasAdvantage` flags); `planSanctuaryWardSave` already honored advantage. Stunning Strike stays non-magical (Monk Discipline / ki is not a spell per RAW). Three end-to-end tests assert that an Imp triggering a glyph, an Imp saving against `held-paralyzed-active`, and an Imp attacking through Sanctuary all emit `SaveRolled` with `used: 'advantage'` and two d20 rolls.

*Cursed condition + Remove Curse planner* (slice 134). The Remove Curse spell shipped schema-only at alpha.5 because the engine had no way to identify which conditions are curses. Slice 134 adds a small fixed `category: 'curse' \| 'disease' \| 'poison'` enum to ConditionSchema (matching the 2024 PHB's three named removal interactions; future Lesser Restoration / Greater Restoration plug into the same taxonomy without new schema work). Bestow Curse's four `cursed-*-active` variants (ability-disadvantage, attack-disadvantage, inactive-turn, extra-damage) are all tagged `category: 'curse'`. New dedicated `planRemoveCurse` planner (3rd-level slot + action, touch range, no save / no roll): walks the touched target's appliedConditions and emits a `ConditionRemoved` per match, mirroring the RAW "any curse affecting that creature ends" semantics. `engine.plan.removeCurse` exposed on the public surface; spell-coverage retags the skip from "cursed condition itself not yet modeled" to "has dedicated planRemoveCurse." Seven tests cover single-curse strip, multi-curse strip (all four variants at once), non-curse condition pass-through (Bless + Prone untouched), no-curse target (slot still consumed, no removals), slotLevel < 3 rejection, unknown-spell rejection, and higher-level slot honor.

*Remote sensor primitive + Clairvoyance planner* (slice 135). Clairvoyance shipped schema-only because the engine had no entity for "an invisible sensor placed at a remote location that the caster perceives through." Slice 135 adds a `Sensor` runtime entity modeled on the slice-94 Trap shape: `state.sensors: Record<SensorId, Sensor>` where each sensor carries id + label + free-text location + casterId + sourceSpellId + sourceEffectInstanceId + sight/hearing mode. Three new event types (`RemoteSensorPlaced`, `RemoteSensorModeChanged`, `RemoteSensorRemoved`) drive the lifecycle; reducers in [src/engine/reducers/sensor.ts](../../src/engine/reducers/sensor.ts). New `planClairvoyance` (3rd-level slot + action + concentration + sensor place), `planSwitchSensorMode` (action + RemoteSensorModeChanged for the seeing/hearing toggle), and `planRemoveSensor` (voluntary spell end) all live in [src/engine/plan/sensor.ts](../../src/engine/plan/sensor.ts). `clearConcentrationEffect` extended to sweep sensors linked to a dropped concentration via `sourceEffectInstanceId`, mirroring the item-buff sweep added in slice 76. The engine doesn't model perception or remote position; `Sensor.location` is free-text consumer territory (the DM or VTT decides what's seen / heard through it). Eleven new tests cover the place / switch / remove lifecycle plus three structural cases: linked-EffectInstanceId tag on the sensor matches the ConcentrationStarted event, a second cast breaks prior concentration and the cleanup sweeps the old sensor, and switching to the same mode the sensor already has is rejected. Scrying composes the same primitive with the existing save mechanic; the target-WIS-save arm is a follow-up slice.

*Scrying planner composes the slice-135 sensor primitive with a target WIS save* (slice 136). RAW 2024 Scrying: 5th-level divination, 10-minute cast, Self / Concentration up to 10 minutes; the caster names a target and that creature makes a Wisdom save against the caster's spell save DC (adjusted by RAW's familiarity tier table). On failure a sensor appears near the target and follows it; on success the spell fails and the slot is still spent. New `planScrying` planner emits ActionEconomyConsumed (in encounter) → SpellCastDeclared → SpellSlotConsumed → SaveRolled (WIS, `sourceIsMagical: true` so Magic Resistance is honored). When the save fails, the events continue with ConcentrationBroken (if prior) → ConcentrationStarted → RemoteSensorPlaced; when the save succeeds the planner stops after the save event with `resisted: true` on the outcome. The familiarity-tier DC adjustment is an explicit `dcAdjustment?: number` field on the intent (RAW: -10 to +5 across five tiers); consumers / DMs supply the tier per their narrative. The engine doesn't track the "sensor follows target" position or the RAW 24-hour cooldown-per-target on a successful save; both are consumer territory. Seven tests cover the seed-walked fail-path full chain, the success-path fizzle (slot still spent, no concentration, no sensor), Imp Magic Resistance contributing advantage to the save, slot-level / unknown-spell rejection, the `dcAdjustment` actually shifting the SaveRolled.dc, and the consumer-display location on the placed sensor including the target name.

*Illusion-interaction primitive: Silent Image + Major Image + Investigate action* (slice 137). New `Illusion` runtime entity (`state.illusions: Record<IllusionId, Illusion>`) modeled on the slice-94 Trap / slice-135 Sensor pattern: id + label + free-text location + `kind: 'visual' \| 'audiovisual'` + casterId + sourceSpellId + sourceEffectInstanceId + a baked `investigationDC` (the caster's spell save DC at cast time) + `disbelievedBy: CharacterId[]` set. Three new event types (`IllusionCreated`, `IllusionInvestigated`, `IllusionDismissed`) drive the lifecycle; reducers in [src/engine/reducers/illusion.ts](../../src/engine/reducers/illusion.ts). Four planners in [src/engine/plan/illusion.ts](../../src/engine/plan/illusion.ts): `planSilentImage` (1st-level visual), `planMajorImage` (3rd-level audiovisual), `planInvestigateIllusion` (a creature spends an action to Study the illusion, rolls INT (Investigation) against the baked DC, success adds them to `disbelievedBy`), `planDismissIllusion` (caster ends spell voluntarily). `clearConcentrationEffect` sweeps illusions linked to a dropped concentration via `sourceEffectInstanceId`, mirroring the slice-135 sensor sweep. The engine surfaces the disbelief set but doesn't itself act on it; consumers / DMs decide what disbelieving creatures see and do. RAW edge cases not modeled this slice: the caster's bonus-action move (Silent Image) and action-recolour (Major Image) of the illusion's shape (a consumer can re-emit IllusionCreated with new label / location until a future schema extension lets the planner mutate the existing entity). Twelve tests cover both cast paths, both Investigate outcomes, the unknown-spell rejection, voluntary dismiss, concentration-drop cleanup, and the multi-creature belief state (two investigators on the same illusion track independently).

*SRD 5.2.1 duration-string normalization across 6 concentration spells* (slice 171). Audit of concentration-spell duration strings vs SRD format:

Six spells shipped with `duration: "Up to N minute"` (missing the `"Concentration, "` prefix that SRD 5.2.1 uses universally): Bless, Bane, Faerie Fire, Polymorph, Sleep, Spirit Guardians. All have `concentration: true` set correctly, so the engine treated them as concentration spells; only the human-readable string drifted from SRD format.

Fixed all 6 to `"Concentration, up to N minute(s)"`.

Engine ripple: the spell-duration parser at `src/internal/spell-duration.ts` only recognized `"up to N units"` and `"N units"` shapes. After the duration-string update, the parser saw `"concentration, up to N units"` and returned `undefined`, making the spells never expire. Updated the regex to optionally strip a leading `"concentration, "` prefix; the engine's concentration tracking still keys off the `concentration: true` flag, so the prefix is purely a display detail. Suite green.

17 spell drift fixes total across slices 167-171.

*SRD 5.2.1 content drift: 3 more spells (Chill Touch, Poison Spray, Black Tentacles)* (slice 170). Continued spell mining:

- **Chill Touch**: range `"120 feet"` → `"Touch"`; mechanicalEffects gained `attackKind: "melee"`; damage `1d8 necrotic` → `1d10 necrotic`; cantripScalingDice `1d8` → `1d10`. The 2024 PHB rebuilt Chill Touch from a ranged necrotic ray to a melee spell attack with a heavier damage die.
- **Poison Spray**: `kind: save` (CON) → `kind: attack` (ranged spell attack). 2024 PHB switched it from a save-or-take to a ranged spell attack. Damage 1d12 poison + cantrip scaling unchanged.
- **Black Tentacles**: `saveAbility: DEX` → `STR`. 2024 SRD: Strength save (was Dexterity in 2014). Damage / restrained-on-fail / area unchanged.

Test ripple: `spell-coverage.test.ts` `'poison-spray': { kind: 'save' }` → `{ kind: 'attack' }`. Suite green (1452 passed, 197 skipped).

14 spell drift fixes shipped across slices 167-170.

*SRD 5.2.1 content drift: 3 more spells (Hypnotic Pattern, Inflict Wounds, False Life)* (slice 169). Continued spell mechanicalEffects audit:

- **Hypnotic Pattern**: `conditionOnFail: incapacitated` → `charmed`. Per SRD 5.2.1, the primary condition is Charmed (with Incapacitated + Speed 0 as riders that the pack's single-condition schema doesn't model). The pack was applying just Incapacitated, missing the Charmed primary.
- **Inflict Wounds**: `kind: attack` → `kind: save` (CON, halfOnSuccess true). 2024 PHB changed Inflict Wounds from a melee spell attack to a Constitution save (still 2d10 necrotic, +1d10 per slot, half on success). Significant mechanical shift — the spell no longer hits/misses on an attack roll.
- **False Life**: `amountDice: "1d4"` → `"2d4"` (base temp HP) + duration `"1 hour"` → `"Instantaneous"`. 2024 PHB doubled the base dice; SRD also makes the duration instantaneous (the temp HP simply doesn't expire by spell duration; the engine drops temp HP normally on long rest).

Test ripple:
- `tests/unit/engine/spell-coverage.test.ts`: `'inflict-wounds': { kind: 'attack' }` → `{ kind: 'save' }`.
- `tests/unit/engine/plan-death-ward.test.ts`: the seed-search loop's "any damage" threshold was changed from "any positive damage" to "≥ 7 damage" because the 2024 save-based Inflict Wounds can produce smaller (half-damage) rolls that don't trigger the Death Ward intercept; the test now finds a failed-save roll specifically.

Continuing the mining: at this point ~11 spell drift fixes shipped across slices 167-169. Diminishing returns ahead; the remaining drift surface is mostly mechanic-shape rewrites (Sleep, Color Spray, Power Word Stun) that need new mechanicalEffect kinds rather than simple field swaps. Suite green (1452 passed, 197 skipped).

*SRD 5.2.1 content drift: 3 more spells refreshed* (slice 168). Continued spell mechanicalEffects audit:

- **Phantasmal Killer**: `halfOnSuccess: false` → `true`. Per SRD 5.2.1: "On a successful save, the target takes half as much damage, and the spell ends." Pack was using the 2014 shape where save = no damage.
